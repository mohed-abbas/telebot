---
phase: 09-spa-scaffold-auth-design-system
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/package.json
  - frontend/src/lib/http.ts
  - frontend/src/lib/queryClient.ts
  - frontend/src/auth/csrf.ts
  - frontend/src/auth/LoginView.tsx
autonomous: true
requirements: [SPA-03, SPA-04]
user_setup: []

must_haves:
  truths:
    - "Every fetch goes through one wrapper that echoes the readable telebot_csrf cookie as X-CSRF-Token on mutations and sends credentials:same-origin"
    - "The wrapper throws HttpError(status) on any non-2xx so both QueryCache and MutationCache see 401s"
    - "A single global onAuthError redirects to /app/login exactly once on 401, with a loop-break"
    - "The login view seeds the CSRF cookie via GET /api/v2/auth/csrf on mount, then POSTs login — cold login does not 403"
    - "Nothing is written to localStorage; telebot_session is never read in JS"
  artifacts:
    - path: "frontend/src/lib/http.ts"
      provides: "fetch wrapper + HttpError class + X-CSRF-Token echo + credentials:same-origin"
      exports: ["api", "HttpError"]
    - path: "frontend/src/lib/queryClient.ts"
      provides: "QueryClient with QueryCache+MutationCache onAuthError + keepPreviousData + refetchIntervalInBackground:false"
      exports: ["queryClient"]
    - path: "frontend/src/auth/csrf.ts"
      provides: "read telebot_csrf cookie + GET /auth/csrf seeding helper"
    - path: "frontend/src/auth/LoginView.tsx"
      provides: "login form (uncontrolled input) consuming /api/v2/auth/login + cold-start CSRF seed"
  key_links:
    - from: "frontend/src/lib/http.ts"
      to: "/api/v2 mutations"
      via: "X-CSRF-Token header read from telebot_csrf cookie on POST/PUT/PATCH/DELETE"
      pattern: "X-CSRF-Token"
    - from: "frontend/src/lib/queryClient.ts"
      to: "/app/login redirect"
      via: "onAuthError on QueryCache + MutationCache; window.location.assign on 401 with loop-break"
      pattern: "onError"
    - from: "frontend/src/auth/LoginView.tsx"
      to: "/api/v2/auth/csrf + /api/v2/auth/login"
      via: "GET csrf on mount seeds cookie, then POST login {password, csrf_token}"
      pattern: "auth/login"
---

<objective>
Build the data + auth layer the whole SPA inherits: the fetch wrapper that echoes CSRF and throws
`HttpError` on non-2xx, the QueryClient with the single global 401 handler and the inherited
polling defaults, the CSRF cold-start seeding helper, and the login view. This is the
security-critical core of SPA-03 (cookie auth, no localStorage) and SPA-04 (global 401 redirect,
no loop). It is a pure consumer of the already-shipped Phase 8 `/api/v2/auth/*` contract.

Purpose: One fetch path, one 401 redirect, one login flow — defined once, inherited by every later
page.
Output: http.ts, queryClient.ts, csrf.ts, LoginView.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-CONTEXT.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-RESEARCH.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-PATTERNS.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-UI-SPEC.md

# Phase 8 contract this layer consumes (read directly — DO NOT change the server):
@api/auth.py
@api/deps.py
@api/errors.py

<critical_overrides>
- REACT, NOT VUE. (Global CLAUDE.md Vue/Nuxt Figma rule does not apply.)
- SPA-03 SECURITY: NEVER read `telebot_session` in JS. NEVER write anything to `localStorage`/
  `sessionStorage`. `fetch(..., { credentials: "same-origin" })` sends the httpOnly session cookie
  automatically. A `localStorage.setItem` of any auth token is an outright failure.
- ALWAYS use relative `/api/v2/...` URLs — never absolute `http://localhost:8090/...` (breaks the
  same-origin cookie model; works identically through the dev proxy and prod nginx). Pitfall 4.
- The cyan `--primary` accent on the login CTA is the ONLY saturated element in the login view
  (UI-SPEC §Color focal point).
</critical_overrides>

<interfaces>
<!-- Phase 8 auth + CSRF + error contract, read from api/auth.py, api/deps.py, api/errors.py. -->
CSRF double-submit (api/deps.py):
  CSRF_COOKIE = "telebot_csrf"   (httponly=False, samesite="lax", path="/" — READABLE by JS)
  CSRF_HEADER = "x-csrf-token"
  state-changing methods = POST, PUT, PATCH, DELETE  → server compares cookie vs X-CSRF-Token header
    (secrets.compare_digest); mismatch/absent → 403. GET is exempt.
Session cookie: telebot_session  (httpOnly — set by SessionMiddleware; NEVER read in JS).

Auth endpoints (api/auth.py — all under /api/v2):
  GET  /api/v2/auth/csrf   → 200 {"csrf_token":"<t>"} + Set-Cookie telebot_csrf=<t> (readable). Body token == cookie value.
  GET  /api/v2/auth/me     → 200 {"user":"admin"} | 401 {"error":{"code":"unauthorized","message":"Session expired"}}
  POST /api/v2/auth/login  → body {"password":"...","csrf_token":"<echo of telebot_csrf cookie>"} + X-CSRF-Token header
                             200 {"user":"admin"} + Set-Cookie telebot_session (+ refreshed telebot_csrf)
                             401 {"error":{"code":"unauthorized","message":"invalid_credentials"}}
                             403 {"error":{"code":"forbidden","message":"CSRF token invalid"}}   ← cold-start (Pitfall 5)
                             429 {"error":{"code":"rate_limited","message":"rate_limited"}}
  POST /api/v2/auth/logout → requires X-CSRF-Token header; 200 {"ok":true}

Error envelope (api/errors.py): failures = {"error":{"code","message","fields"?}}; success bodies are BARE.
  status→code: 401→unauthorized, 403→forbidden, 422→validation_error, 429→rate_limited, 503→unavailable.

Login copy contract (UI-SPEC §Copywriting):
  title "Telebot" (Display 28px/600) · labels "Username" / "Password"
  CTA idle "Log in" → pending "Logging in…" (button disabled while pending)
  401 error "Incorrect username or password." (inline, no field-specific leak)
  network/500 error "Something went wrong. Please try again."
  optional session-expired banner (only when arrived via 401 redirect): "Your session expired — please log in again."

QueryClient defaults (D-09): placeholderData: keepPreviousData, refetchIntervalInBackground: false,
  global onAuthError on QueryCache + MutationCache. staleTime/retry = executor's call within this frame.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: http.ts fetch wrapper + queryClient.ts global 401 handler</name>
  <read_first>
    - api/deps.py (CSRF_COOKIE/CSRF_HEADER names + state-changing methods + verify_csrf_token)
    - api/errors.py (error envelope shape + status→code map)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-RESEARCH.md (Pattern 3, Pattern 4)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-PATTERNS.md (http.ts + queryClient.ts sections)
    - frontend/package.json (from Plan 01 — confirm TanStack not yet installed)
  </read_first>
  <files>frontend/package.json, frontend/src/lib/http.ts, frontend/src/lib/queryClient.ts</files>
  <action>
    First install the data/router deps: `cd frontend && npm install @tanstack/react-query@5 react-router-dom@7` (react-router is consumed in Plan 04 but install both now since package.json is owned here; pin to 5.101.0 / 7.17.0 per RESEARCH).
    Create `frontend/src/lib/http.ts`: export `class HttpError extends Error` carrying `status: number` and `body?: unknown`; export `async function api(path, init)`. The wrapper: read the readable `telebot_csrf` cookie (a small `readCookie(name)` parsing `document.cookie`), and on mutating methods ONLY (POST/PUT/PATCH/DELETE — matching api/deps.py `_STATE_CHANGING_METHODS`) set header `X-CSRF-Token` to that cookie value. Call `fetch(path, { ...init, headers, credentials: "same-origin" })`. On `!res.ok`, parse the JSON error envelope into `body` (tolerate non-JSON) and `throw new HttpError(res.status, body)`. Return `null` for 204, else `res.json()`. NEVER read `telebot_session`; NEVER touch localStorage.
    Create `frontend/src/lib/queryClient.ts`: define a shared `onAuthError(error)` that, when `error instanceof HttpError && error.status === 401`, does `window.location.assign("/app/login")` — but ONLY if `!window.location.pathname.startsWith("/app/login")` (the loop-break, SPA-04). Export `queryClient = new QueryClient({ queryCache: new QueryCache({ onError: onAuthError }), mutationCache: new MutationCache({ onError: onAuthError }), defaultOptions: { queries: { placeholderData: keepPreviousData, refetchIntervalInBackground: false, staleTime: <executor picks, e.g. 1000>, retry: <executor picks, e.g. false for a same-origin internal tool> } } })`. These defaults are the convention every Phase 10/11 page inherits (D-09).
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q 'X-CSRF-Token' src/lib/http.ts && grep -q "credentials.*same-origin" src/lib/http.ts && ! grep -rq 'localStorage' src/ && grep -q 'onError' src/lib/queryClient.ts && grep -q 'keepPreviousData' src/lib/queryClient.ts && grep -q "/app/login" src/lib/queryClient.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0.
    - `http.ts` exports `api` and `HttpError`; sets `X-CSRF-Token` only on POST/PUT/PATCH/DELETE; calls fetch with `credentials: "same-origin"`; throws `HttpError(status, body)` on non-2xx.
    - NO `localStorage`/`sessionStorage` anywhere under `frontend/src` (grep returns nothing).
    - `http.ts` does NOT reference `telebot_session`.
    - `queryClient.ts` wires `onError: onAuthError` on BOTH `QueryCache` and `MutationCache`; `onAuthError` redirects to `/app/login` on 401 with the `startsWith("/app/login")` loop-break.
    - `queryClient.ts` sets `placeholderData: keepPreviousData` and `refetchIntervalInBackground: false`.
  </acceptance_criteria>
  <done>One fetch wrapper (CSRF echo + same-origin cookie + HttpError), one global 401 handler with loop-break, inherited polling defaults; no localStorage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: csrf.ts cold-start seeding + LoginView.tsx</name>
  <read_first>
    - api/auth.py (login contract lines ~91–137, csrf seeding ~157–177)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-RESEARCH.md (Pitfall 5 cold-start, §Auth contract)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-UI-SPEC.md (§Copywriting login view, §Color login focal point, §Typography)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-PATTERNS.md (csrf.ts + LoginView.tsx section, login copy contract)
    - frontend/src/lib/http.ts (from Task 1 — the api() wrapper LoginView consumes)
    - frontend/src/components/ui/{button,input,label,card}.tsx (from Plan 01)
  </read_first>
  <files>frontend/src/auth/csrf.ts, frontend/src/auth/LoginView.tsx</files>
  <action>
    Create `frontend/src/auth/csrf.ts`: a `readCsrfCookie()` (read `telebot_csrf` from document.cookie — reuse the http.ts cookie reader or a shared util) and an async `seedCsrf()` that calls `api("/api/v2/auth/csrf")` (GET) to seed the readable cookie AND return the `{csrf_token}` body. This MUST run on the login view's mount BEFORE the first login or the cold-start POST 403s (Pitfall 5).
    Create `frontend/src/auth/LoginView.tsx`: a centered shadcn `<Card>` (background #1a1a2e on the #0f0f1a field) with the "Telebot" title (Display 28px/600), "Username" + "Password" `<Label>`/`<Input>` fields (UNCONTROLLED/local input — no react-hook-form; that's Phase 11), and the cyan "Log in" `<Button>` (the only saturated element — UI-SPEC focal point). On mount, call `seedCsrf()`. On submit: `api("/api/v2/auth/login", { method: "POST", body: JSON.stringify({ password, csrf_token: <readCsrfCookie()> }), headers: {"Content-Type":"application/json"} })` (the http wrapper adds X-CSRF-Token). While pending, disable the button and show "Logging in…". On `HttpError`: branch on `error.status` — 401 → inline "Incorrect username or password.", else (network/500/unknown) → "Something went wrong. Please try again." On success, redirect into the app (`window.location.assign("/app/")` or navigate — the shell/router gate is wired in Plan 04; a hard nav is acceptable here). Show the optional "Your session expired — please log in again." banner only when a session-expired flag/query param is present (derive from `window.location`).
    Username field: the backend login takes `password` only (single-operator); render Username for parity/legibility per UI-SPEC but it need not be submitted unless the contract requires it — confirm against api/auth.py LoginIn schema and submit exactly what the server expects.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q 'auth/csrf' src/auth/csrf.ts && grep -q 'auth/login' src/auth/LoginView.tsx && grep -q 'Logging in' src/auth/LoginView.tsx && grep -q 'Incorrect username or password' src/auth/LoginView.tsx && ! grep -rq 'localStorage' src/</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0.
    - `csrf.ts` calls `GET /api/v2/auth/csrf` and exposes a cookie reader.
    - `LoginView.tsx` calls `seedCsrf()` on mount (cold-start guard), then `POST /api/v2/auth/login` with the csrf_token echo; the http wrapper supplies `X-CSRF-Token`.
    - Button shows "Log in" idle / "Logging in…" while pending and is disabled while pending.
    - Error branches render exact copy: "Incorrect username or password." (401) and "Something went wrong. Please try again." (network/500).
    - Login submits exactly the fields api/auth.py `LoginIn` expects (password + csrf_token); no extra/missing fields.
    - Still no `localStorage` anywhere under `frontend/src`.
  </acceptance_criteria>
  <done>Cold-start CSRF seed wired; login form consumes the Phase 8 contract with exact copy, pending state, and error branches; no localStorage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser JS → /api/v2 (cookie auth) | the SPA submits credentials via the httpOnly session cookie + readable CSRF cookie |
| login form → /api/v2/auth/login | untrusted password input crosses to the server (argon2 verify server-side, Phase 8) |
| dev: Vite origin → dev container | cross-port risk mitigated by the same-origin /api proxy (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information Disclosure | auth token storage | mitigate | httpOnly `telebot_session` never read in JS; nothing in localStorage/sessionStorage (grep-enforced); `credentials:"same-origin"` only. (SPA-03) |
| T-09-07 | Tampering | CSRF on mutations | mitigate | http.ts echoes the readable `telebot_csrf` cookie as `X-CSRF-Token` on POST/PUT/PATCH/DELETE; cross-site can't set the custom header; server compares with compare_digest (Phase 8 D-15). |
| T-09-08 | Denial of Service | 401 redirect loop | mitigate | `onAuthError` skips the redirect when already on `/app/login`; hard-nav clears in-memory state (single bounce, SPA-04). |
| T-09-09 | Information Disclosure | dev cross-origin cookie leak | mitigate | Relative `/api/v2` URLs only (never absolute :8090); same-origin Vite proxy; never credentialed-wildcard CORS, never weaken cookie attrs (Pitfall 4). |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0.
- `grep -rq localStorage frontend/src` returns NOTHING (SPA-03 hard gate).
- http.ts: X-CSRF-Token on mutations + credentials:same-origin + HttpError throw.
- queryClient.ts: onAuthError on both caches + loop-break + keepPreviousData + refetchIntervalInBackground:false.
- LoginView: cold-start csrf seed + exact copy + pending/disabled + error branches.
- Manual (phase gate, browser): cold login succeeds (no 403); Object.keys(localStorage) has no auth token; single 401 redirect with no loop.
</verification>

<success_criteria>
- SPA-03: operator can log in through the SPA on the retained httpOnly session cookie; no auth token in localStorage.
- SPA-04: a global 401 handler redirects to login exactly once (loop-break), no repeated bounces.
</success_criteria>

<output>
Create `.planning/phases/09-spa-scaffold-auth-design-system/09-03-SUMMARY.md` when done.
</output>
