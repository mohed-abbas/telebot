---
phase: 09-spa-scaffold-auth-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/components.json
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/vite-env.d.ts
  - frontend/src/lib/utils.ts
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/sonner.tsx
autonomous: false
requirements: [SPA-01, SPA-02]
user_setup: []

must_haves:
  truths:
    - "A new frontend/ Vite 8 + React 19 + TypeScript project builds with `npm run build`"
    - "D-01: Vite base is /app/ so built asset URLs resolve under the nginx /app/ subpath"
    - "D-10: Tailwind v4 is wired via @tailwindcss/vite with NO tailwind.config.js"
    - "D-10: The dark brand palette (#0f0f1a/#1a1a2e/#252542 + cyan accent) maps to shadcn semantic roles in src/index.css"
    - "D-11: shadcn button, input, label, card, sonner — and only those — components exist and render opaque dark colors"
    - "D-12: vite.config.ts proxies /api → the dev container port 8090 to keep cookies same-origin in `npm run dev`"
  decisions:
    - "D-01: SPA under /app/ subpath — Vite base /app/ (this plan's build config; serving in 09-02)"
    - "D-10: dark palette → shadcn @theme semantic roles, no tailwind.config.js"
    - "D-11: minimal shadcn set (button/input/label/card/sonner) only"
    - "D-12: vite.config.ts dev proxy /api → 8090, same-origin cookies"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vite config with base:/app/, react()+tailwindcss() plugins, @ alias, /api dev proxy"
      contains: "base"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 import + :root/.dark/@theme inline brand-token block"
      contains: "@theme inline"
    - path: "frontend/components.json"
      provides: "shadcn config (cssVariables true, Tailwind v4)"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "shadcn Button (React 19, no forwardRef)"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() helper (clsx + tailwind-merge)"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "dashboard.py app.mount('/app')"
      via: "base:'/app/' baked into built asset URLs matching the uvicorn mount path"
      pattern: "base.*['\"]/app/['\"]"
    - from: "frontend/src/index.css"
      to: "shadcn ui components"
      via: "@theme inline maps --color-* to brand --background/--card/--primary/--destructive roles"
      pattern: "@theme inline"
---

<objective>
Scaffold the greenfield `frontend/` Vite 8 + React 19 + TypeScript SPA, wire Tailwind v4 (no
config file), map the dark brand palette to shadcn semantic roles, run `shadcn init`, and install
the minimal component set (button, input, label, card, sonner). This is the foundation every
later Phase 9 plan and every Phase 10/11 page inherits — get `base`, the v4 token model, and the
shadcn flow right once.

Purpose: Establish the build, the design tokens, and the component primitives. No auth, no data
layer, no shell yet — those land in Plans 03 and 04.
Output: A buildable `frontend/` project with brand-themed shadcn components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-CONTEXT.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-RESEARCH.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-PATTERNS.md
@.planning/phases/09-spa-scaffold-auth-design-system/09-UI-SPEC.md

<critical_overrides>
- REACT, NOT VUE. The global ~/.claude/CLAUDE.md "translate React→Vue 3 + Nuxt + shadcn-vue"
  Figma rule does NOT apply to this phase. This is a LOCKED React 19 + Vite 8 + Tailwind v4 +
  shadcn/ui (radix-ui, NOT radix-vue) SPA. Do not introduce Vue, Nuxt, shadcn-vue, or radix-vue.
- NO tailwind.config.js. Tailwind v4 tokens live in src/index.css via @theme. A tailwind.config.js
  is a failure (Pitfall 2).
- base MUST be "/app/" (Pitfall 3). At base "/" the built bundle 404s its assets under nginx.
- Do NOT modify any legacy file (templates/, static/css/input.css, tailwind.config.js at repo root,
  basecoat). The SPA uses the BRAND oklch roles, never basecoat's neutral grays.
</critical_overrides>

<interfaces>
<!-- Pinned versions VERIFIED against npm registry 2026-06-04 (RESEARCH Standard Stack). -->
<!-- Majors are LOCKED; these minor/patch are the current stable line. -->
Core:        react 19.2.7 · react-dom 19.2.7 · vite 8.0.16 · @vitejs/plugin-react 6.0.2 · typescript 6.0.3
Tailwind:    tailwindcss 4.3.0 · @tailwindcss/vite 4.3.0
shadcn CLI:  shadcn 4.10.0  (NOT a runtime dep — copies source into src/components/ui/)
shadcn deps: class-variance-authority 0.7.1 · clsx 2.1.1 · tailwind-merge 3.6.0 · lucide-react 1.17.0
Toast:       sonner 2.0.7

Brand palette → shadcn semantic role map (UI-SPEC §Color, D-10; hex from repo tailwind.config.js:16):
  --background        ← #0f0f1a  ≈ oklch(0.12 0.02 275)   (dominant 60% — app bg)
  --card/--popover/--sidebar ← #1a1a2e ≈ oklch(0.17 0.03 275)  (secondary 30%)
  --muted/--border/--input   ← #252542 ≈ oklch(0.24 0.04 275)  (elevated/muted)
  --primary/--ring/--sidebar-primary ← cyan oklch(0.7 0.13 200) (10% accent — CTA/active-nav/ring/wordmark ONLY)
  --primary-foreground ← near-black oklch(0.18 0.02 275)
  --foreground         ← near-white oklch(0.97 0 0)
  --destructive        ← red oklch(0.62 0.21 25)  (themed NOW, UNUSED in Phase 9 — ready for Phase 11)

Dev proxy target: http://localhost:8090  (docker-compose.dev.yml:39 DASHBOARD_HOST_PORT default 8090; 8080 collides with devdock-caddy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold frontend/ — Vite 8 + React 19 + TS, base /app/, dev proxy, @ alias</name>
  <read_first>
    - .planning/phases/09-spa-scaffold-auth-design-system/09-RESEARCH.md (§Installation, Pattern 1, Pitfall 3, Pitfall 4)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-PATTERNS.md (frontend/vite.config.ts section, dev-port fact)
    - docker-compose.dev.yml (confirm DASHBOARD_HOST_PORT default 8090 line ~39)
  </read_first>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/vite-env.d.ts</files>
  <action>
    Scaffold a new top-level `frontend/` dir: `npm create vite@latest frontend -- --template react-ts`, then `cd frontend && npm install`. Pin the locked stack to the versions in the <interfaces> block (react/react-dom 19.2.7, vite 8.0.16, @vitejs/plugin-react 6.0.2, typescript 6.0.3). Install Tailwind v4: `npm install tailwindcss@4 @tailwindcss/vite@4` and `npm install -D @types/node`.
    Configure `frontend/vite.config.ts` exactly per RESEARCH Pattern 1: `base: "/app/"` (Pitfall 3 — must match the dashboard.py /app mount path), `plugins: [react(), tailwindcss()]`, `resolve.alias` `@` → `./src`, and `server.proxy` with a single `/api` entry targeting `http://localhost:8090` and `changeOrigin: false` (Pitfall 4 — keeps the telebot_session cookie same-origin in dev). Only `/api` is proxied — do NOT add `/login` or `/logout` proxy lines (those are legacy HTMX routes; the SPA login is /api/v2/auth/login).
    Add the `@/*` path alias to BOTH `tsconfig.json` and `tsconfig.app.json` (`"paths": { "@/*": ["./src/*"] }` + `baseUrl: "."`) so editor + build agree.
    In `frontend/index.html` set the root element `<html lang="en" class="dark">` (dark-by-default — PATTERNS templates/base.html:2 anchor; shadcn switches :root vs .dark purely on this class, no toggle JS in Phase 9).
    Leave `src/App.tsx` as a trivial placeholder for now (boot guard + shell wiring land in Plans 03/04); keep `src/main.tsx` as the default ReactDOM root — providers are added in Plan 04. Do NOT introduce any `VITE_*` env var or absolute API URL (Pitfall 3/4 — relative `/api/v2/...` only; no secret may be baked into the bundle).
  </action>
  <verify>
    <automated>cd frontend && npm run build && test ! -f tailwind.config.js && grep -Eq 'base:\s*["'\'']/app/["'\'']' vite.config.ts && grep -q 'http://localhost:8090' vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 and emits `frontend/dist/index.html`.
    - `frontend/dist/index.html` references assets under `/app/assets/` (grep `dist/index.html` for `/app/assets/`).
    - `frontend/vite.config.ts` contains `base: "/app/"`, both `react()` and `tailwindcss()` in plugins, the `@`→`./src` alias, and a `/api` proxy to `http://localhost:8090` with `changeOrigin: false`.
    - NO `frontend/tailwind.config.js` exists.
    - `frontend/index.html` root `<html>` has `class="dark"`.
    - No `VITE_` env var appears in any frontend source file (grep `frontend/src` for `VITE_` returns nothing).
  </acceptance_criteria>
  <done>frontend/ scaffolds, builds clean, base is /app/, dev proxy targets 8090, no tailwind.config.js, dark-by-default.</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind v4 brand tokens + shadcn init + minimal component set</name>
  <read_first>
    - .planning/phases/09-spa-scaffold-auth-design-system/09-RESEARCH.md (Pattern 2, Pitfall 2, §Installation steps 3,5,6)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-UI-SPEC.md (§Color role table, §Typography font tokens)
    - .planning/phases/09-spa-scaffold-auth-design-system/09-PATTERNS.md (frontend/src/index.css section — brand hex source, dark-by-default)
    - frontend/vite.config.ts (from Task 1)
  </read_first>
  <files>frontend/components.json, frontend/src/index.css, frontend/src/lib/utils.ts, frontend/src/components/ui/button.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/sonner.tsx</files>
  <action>
    Run `npx shadcn@latest init` against the Vite + Tailwind v4 base (it writes `components.json` with `cssVariables: true`, generates `src/lib/utils.ts` `cn()`, and installs class-variance-authority/clsx/tailwind-merge/lucide-react). Use the current canonical flow only — do NOT paste any v3/React-18 shadcn snippet (Pitfall 2: those carry tailwind.config.js + @tailwind directives + forwardRef and break on v4/React 19).
    Edit `frontend/src/index.css`: keep `@import "tailwindcss";` and the `@custom-variant dark (&:is(.dark *));` line. In the `.dark { ... }` block, replace the shadcn default oklch values with the brand role map from <interfaces> / UI-SPEC §Color: `--background` ← #0f0f1a, `--card`/`--popover`/`--sidebar` ← #1a1a2e, `--muted`/`--border`/`--input` ← #252542, `--primary`/`--ring`/`--sidebar-primary` ← cyan oklch(0.7 0.13 200), `--primary-foreground` ← oklch(0.18 0.02 275), `--foreground` ← oklch(0.97 0 0), `--destructive` ← oklch(0.62 0.21 25). Derive `--muted-foreground`, `--accent`, `--card-foreground`, `--secondary` and any other roles shadcn references for WCAG-AA contrast on the dark field. Keep every role the installed components reference present in BOTH the `.dark` block and the `@theme inline` mapping (`--color-* : var(--*)`). Add the font tokens from UI-SPEC §Typography: `--font-sans` (system-ui stack) and `--font-mono` (ui-monospace stack).
    The cyan `--primary` is a 10% accent reserved ONLY for the login CTA, active nav indicator, focus rings, and the brand wordmark (UI-SPEC §Color) — do not apply it broadly. `--destructive` is themed but UNUSED in Phase 9.
    Install exactly the D-11 minimal set: `npx shadcn@latest add button input label card sonner`. Do NOT add any other component — later pages add their own.
  </action>
  <verify>
    <automated>cd frontend && npm run build && test ! -f tailwind.config.js && grep -q '@theme inline' src/index.css && grep -q -- '--destructive' src/index.css && for c in button input label card sonner; do test -f src/components/ui/$c.tsx || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 after token edits + component installs.
    - `frontend/components.json` exists (shadcn config); still NO `frontend/tailwind.config.js`.
    - `frontend/src/index.css` contains `@import "tailwindcss"`, an `@theme inline` block, and the brand role values: `--background` mapped from #0f0f1a, `--card` from #1a1a2e, `--muted` from #252542, a cyan `--primary`, and a red `--destructive`.
    - `frontend/src/index.css` contains `--font-sans` and `--font-mono` tokens.
    - All five component files exist: `src/components/ui/{button,input,label,card,sonner}.tsx`.
    - No `forwardRef` in the generated `button.tsx`/`input.tsx` (React 19 — `grep -L forwardRef` style check; current shadcn uses ref-as-prop).
  </acceptance_criteria>
  <done>shadcn initialized on Tailwind v4, brand tokens mapped to semantic roles in index.css, 5 minimal components installed and building.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The frontend/ scaffold with Tailwind v4 brand tokens and the 5 shadcn components. Before any
    further plan builds on these tokens, confirm (a) the components render OPAQUE dark brand colors
    (no transparent-popover regression — RESEARCH Pitfall 2/9), and (b) the post-`npm install`
    lockfile has no unexpected packages (RESEARCH Package Legitimacy Audit — slopcheck was
    unavailable; all package names are official-shadcn-doc-prescribed, this is a belt-and-suspenders
    confirmation, NOT a blocking [ASSUMED]/[SUS] gate).
  </what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` and open the printed localhost URL.
    2. Temporarily render a `<Card>` containing an `<Input>`, a `<Label>`, and a `<Button>`.
    3. Confirm: the Card and Input are OPAQUE (not see-through), the background reads near-black
       #0f0f1a, the card reads #1a1a2e, and the Button fill is the cyan accent — matching the
       UI-SPEC §Color table. No transparent popovers, no build errors about @tailwind/@config.
    4. Run `cd frontend && cat package.json` and skim dependencies — confirm only the expected
       names appear (react, react-dom, vite, @vitejs/plugin-react, tailwindcss, @tailwindcss/vite,
       @tanstack/react-query is NOT yet here, react-router NOT yet here, class-variance-authority,
       clsx, tailwind-merge, lucide-react, sonner). No surprise packages.
  </how-to-verify>
  <resume-signal>Type "approved" if components render opaque + correct brand colors and the lockfile is clean, or describe what looks wrong (transparent components, wrong colors, unexpected packages).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → runtime bundle | npm/Vite produce static `dist/` that ships to the browser; any secret baked here is world-readable |
| npm registry → frontend deps | third-party package install crosses into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Vite bundle | mitigate | No `VITE_*` env vars; relative `/api/v2/...` URLs only; no secret in source (verified by grep `frontend/src` for `VITE_`). |
| T-09-02 | Tampering | nginx asset path under /app/ | mitigate | `base:"/app/"` keeps assets under the subpath; no root catch-all introduced (Pitfall 3/6). |
| T-09-SC | Tampering | npm install (supply chain) | mitigate | All package identities are official-shadcn-doc-prescribed / locked-REQUIREMENTS-named (not search-discovered); blocking human-verify checkpoint reviews the post-install lockfile (slopcheck unavailable → belt-and-suspenders confirmation). |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0.
- No `frontend/tailwind.config.js`.
- `frontend/dist/index.html` references `/app/assets/`.
- Brand tokens present in `src/index.css` (`@theme inline`, `--background`/#0f0f1a, cyan `--primary`, red `--destructive`).
- 5 shadcn components present and rendering opaque dark brand colors (human-verify checkpoint).
</verification>

<success_criteria>
- SPA-01 (foundation): Vite 8 + React 19 + TS project builds, base /app/, dev proxy to 8090.
- SPA-02: Tailwind v4 (no config file) + shadcn components themed from the dark brand palette mapped to @theme semantic roles.
</success_criteria>

<output>
Create `.planning/phases/09-spa-scaffold-auth-design-system/09-01-SUMMARY.md` when done.
</output>
</output>
