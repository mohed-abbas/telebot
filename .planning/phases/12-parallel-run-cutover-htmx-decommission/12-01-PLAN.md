---
phase: 12-parallel-run-cutover-htmx-decommission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cutover_redirects.py
  - tests/test_post_teardown.py
  - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md
autonomous: true
requirements: [CUT-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The SPA (/app) and legacy HTMX dashboard (/) both reachable behind one nginx location, with /api/v2 never shadowed by the /app mount"
    - "An automated test guard exists that will assert each legacy page route 303-redirects to its /app/<page> target once cut over"
    - "An automated test guard exists that will assert deleted routes 404 and surviving routes 200 once teardown happens"
    - "A per-page operator sign-off checklist exists in D-05 cutover order mirroring 06-HUMAN-UAT.md"
  artifacts:
    - path: "tests/test_cutover_redirects.py"
      provides: "Parameterized 303-redirect-target guard for CUT-02 (red until each page is cut over)"
      contains: "follow_redirects=False"
    - path: "tests/test_post_teardown.py"
      provides: "Deleted-404 / surviving-200 + import-api guard for CUT-03 (red until teardown)"
      contains: "import api"
    - path: ".planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md"
      provides: "Per-page MT5-demo parity sign-off rows in D-05 order (D-04)"
      contains: "## Tests"
  key_links:
    - from: "tests/test_cutover_redirects.py"
      to: "dashboard.app"
      via: "client(api_app) conftest fixture"
      pattern: "TestClient\\(api_app\\)"
    - from: "tests/test_spa_serving.py::test_api_not_shadowed_by_spa_mount"
      to: "/api/v2 precedence over /app mount"
      via: "existing CUT-01 precedence assertion"
      pattern: "test_api_not_shadowed_by_spa_mount"
---

<objective>
Establish the Wave-0 automated guards and the operator sign-off doc that gate the entire phase, and formally confirm CUT-01 (parallel-run) is already satisfied by Phase 9 routing — no parallel-run plumbing is built here.

Purpose: The cutover (12-02) and teardown (12-03) plans both depend on these guards existing FIRST. `test_cutover_redirects.py` proves each per-page 303 swap; `test_post_teardown.py` proves the teardown deleted/survived the right routes; `12-CUTOVER-CHECKLIST.md` is the per-page MT5-demo parity sign-off the D-01 redirect commits reference. CUT-01 needs zero code change (RESEARCH §CUT-01) — its task is a documented confirmation that the existing precedence test passes.

Output: two new test files (initially red for the not-yet-cut-over / not-yet-torn-down assertions — that is expected and correct), one new checklist doc, and a confirmed-green CUT-01 precedence assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-VALIDATION.md
@.planning/phases/06-staged-entry-execution/06-HUMAN-UAT.md
@tests/test_spa_serving.py
@tests/test_auth_session.py
@tests/conftest.py

<interfaces>
<!-- Test client fixture the new test files MUST reuse — do NOT re-implement env-injection. -->
<!-- Source: tests/conftest.py:113 (api_app, module-scoped, pytest.skip if no Postgres) + tests/test_spa_serving.py:64-67 -->

conftest fixture: `api_app` — module-scoped dashboard.app (env-inject → sys.modules.pop → re-import → init_dashboard with DryRun executor → pytest.skip if Postgres absent).

New-file fixture pattern (copy verbatim):
  @pytest.fixture
  def client(api_app):
      return TestClient(api_app)

Redirect assertion form (tests/test_auth_session.py:37-42): client.get(path, follow_redirects=False); assert r.status_code == 303; assert r.headers["location"] == target. follow_redirects=False is load-bearing.

Surviving/404 assertion form (tests/test_spa_serving.py:94-119): surviving routes assert status_code in (200, 401) + "application/json" content-type; deleted routes assert status_code == 404 AND 'id="root"' not in r.text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CUT-01 confirmation + cutover redirect test scaffold</name>
  <files>tests/test_cutover_redirects.py</files>
  <read_first>
    - tests/test_spa_serving.py (the client(api_app) fixture at :64-67 and the precedence test test_api_not_shadowed_by_spa_mount)
    - tests/test_auth_session.py (the follow_redirects=False / status 303 / Location assertion at :37-42)
    - tests/conftest.py (the api_app module-scoped fixture at :113)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md (the test_cutover_redirects.py skeleton)
  </read_first>
  <action>
    Create tests/test_cutover_redirects.py. Reuse the api_app conftest fixture via a local `client(api_app)` fixture returning TestClient(api_app) — do NOT re-implement env-injection or re-import dashboard (conftest already does it). Write a parametrized test over the D-05 page list mapping each legacy GET route to its /app/<page> target: /analytics→/app/analytics, /signals→/app/signals, /history→/app/history, /staged→/app/staged, /overview→/app/overview, /settings→/app/settings, /positions→/app/positions. For each, assert client.get(legacy, follow_redirects=False) returns status_code == 303 and headers["location"] == target. Mark each parametrized case xfail-or-not is NOT needed — these assertions are RED now and go GREEN one row at a time as 12-02 commits each page's redirect; that staged-red behavior is the intended CUT-02 guard. Add test_unauth_redirects_to_app_login asserting an unauth GET to a surviving route bounces 303 to a Location starting "/app/login" (this is RED until 12-03 Commit 1 repoints _verify_auth per Pitfall 4). Add a docstring at module top noting the file is the CUT-02 progress guard and that cases go green incrementally. For CUT-01: confirm (do not modify code) that tests/test_spa_serving.py::test_api_not_shadowed_by_spa_mount passes — this IS the CUT-01 evidence (RESEARCH §CUT-01: zero code change). Record the CUT-01 confirmation in the plan SUMMARY, not in source.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -m pytest tests/test_cutover_redirects.py --collect-only -q && python -m pytest tests/test_spa_serving.py::test_api_not_shadowed_by_spa_mount -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_cutover_redirects.py` collects without import/syntax error (pytest --collect-only exits 0).
    - The file contains a parametrized case for each of the 7 legacy pages mapping to its /app/<page> target, plus test_unauth_redirects_to_app_login asserting a Location starting "/app/login".
    - The file uses `client(api_app)` fixture + `TestClient` + `follow_redirects=False` (no raw httpx.AsyncClient, no in-file env-injection).
    - `tests/test_spa_serving.py::test_api_not_shadowed_by_spa_mount` passes (CUT-01 precedence intact) OR pytest.skips because Postgres is absent locally — a skip is acceptable; a FAIL is not.
  </acceptance_criteria>
  <done>tests/test_cutover_redirects.py exists, collects clean, and the CUT-01 precedence test is green-or-skipped; CUT-01 is confirmed satisfied by existing routing with no code change.</done>
</task>

<task type="auto">
  <name>Task 2: Post-teardown test scaffold</name>
  <files>tests/test_post_teardown.py</files>
  <read_first>
    - tests/test_spa_serving.py (the surviving-200 / 404 assertions at :94-119)
    - tests/test_auth_session.py (the /health 200 assertion at :32-34)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md (§Phase Requirements → Test Map line 431 + §Safety smoke list lines 364-375 — the deleted-404 / surviving-200 / import-guard targets)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md (the test_post_teardown.py specialization notes)
  </read_first>
  <action>
    Create tests/test_post_teardown.py reusing the same `client(api_app)` fixture (TestClient(api_app)). Assert deleted-legacy routes return 404 with 'id="root"' not in r.text (proving a real 404, not the SPA shell catch-all): GET /overview → 404, GET /stream → 404, GET /partials/positions → 404. Assert surviving routes: GET /health → 200; GET /app/ → 200; GET /api/v2/trading-status returns application/json (status in (200, 401)); GET / (post-final-cutover) → 303 with Location == "/app/". Add test_api_imports_resolve doing `import api` inside the test body (cheap in-suite guard for the 6 MUST-SURVIVE dashboard.py symbols the /api/v2 layer imports). These assertions are RED now and go GREEN only after 12-03 teardown lands — that staged-red behavior is the intended CUT-03 guard. Add a module-top docstring stating the file is the CUT-03 teardown guard and is expected red until teardown completes.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -m pytest tests/test_post_teardown.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_post_teardown.py` collects without import/syntax error (pytest --collect-only exits 0).
    - The file asserts GET /overview → 404, GET /stream → 404, GET /partials/positions → 404 (each with 'id="root"' not in r.text).
    - The file asserts GET /health → 200, GET /app/ → 200, GET /api/v2/trading-status JSON, GET / → 303 Location "/app/".
    - The file contains test_api_imports_resolve that executes `import api` (the MUST-SURVIVE-symbol guard).
    - Uses the shared `client(api_app)` fixture (no in-file env-injection).
  </acceptance_criteria>
  <done>tests/test_post_teardown.py exists and collects clean; it is the standing CUT-03 acceptance guard (red until teardown, green after).</done>
</task>

<task type="auto">
  <name>Task 3: Per-page MT5-demo parity checklist (D-04)</name>
  <files>.planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/06-staged-entry-execution/06-HUMAN-UAT.md (the named D-04 precedent — frontmatter + ## Tests numbered rows + ## Summary tally)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md (the 12-CUTOVER-CHECKLIST.md structure + D-04/D-05 specialization)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CONTEXT.md (D-04 parity items + D-05 order)
  </read_first>
  <action>
    Create .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md mirroring 06-HUMAN-UAT.md exactly: YAML frontmatter (status: partial, phase, source: [12-RESEARCH.md, 12-CONTEXT.md], started, updated), a "## Current Test" placeholder, a "## Tests" section with one numbered row PER PAGE in D-05 order — 1. analytics (pilot), 2. signals, 3. history, 4. staged, 5. overview, 6. settings, 7. positions, 8. kill-switch — each row carrying an `expected:` line listing the four D-04 parity items (SPA data matches legacy on live data; live-money actions behave correctly against the demo broker; no console errors; poll-safe modals/drilldowns) and a `result: [pending]` line extended to a dated sign-off form `[pending — sign: YYYY-MM-DD operator]`. Note in the kill-switch row that it has NO legacy GET page — it is verified-then-decommissioned (its parity row gates the CUT-03 Commit-1 deletion of /api/emergency-preview, not a redirect). Close with a "## Summary" tally (total: 8, passed: 0, issues: 0, pending: 8) and a "## Gaps" section. Each 12-02 redirect commit message will reference its row number.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && grep -v '^#' .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md | grep -c 'expected:' | grep -qx 8 && echo "8 parity rows present"</automated>
  </verify>
  <acceptance_criteria>
    - `12-CUTOVER-CHECKLIST.md` exists in the phase directory with YAML frontmatter (status/phase/source/started/updated).
    - Exactly 8 numbered parity rows in D-05 order (analytics → signals → history → staged → overview → settings → positions → kill-switch), each with an `expected:` line covering the 4 D-04 items and a dated `result:` sign-off line.
    - The kill-switch row documents it is verified-then-decommissioned (no redirect; gates /api/emergency-preview deletion).
    - A `## Summary` tally (total: 8) and a `## Gaps` section are present.
  </acceptance_criteria>
  <done>12-CUTOVER-CHECKLIST.md exists with 8 per-page parity rows in D-05 order, ready for operator sign-off that gates each 12-02 redirect commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → nginx → dashboard.app | Untrusted session cookie crosses here; /api/v2 must keep precedence over /app static mount |
| test harness → dashboard.app | Tests drive the real app object; a wrong fixture could mask a real precedence regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | /api/v2 vs /app mount precedence (CUT-01) | mitigate | Task 1 confirms tests/test_spa_serving.py::test_api_not_shadowed_by_spa_mount green — the API router must register before the SPA mount so /api/v2 is never shadowed |
| T-12-02 | Tampering | Cutover guard correctness | mitigate | test_cutover_redirects.py asserts exact Location targets with follow_redirects=False so a redirect typo (e.g. /app/analytic) is caught, not silently followed |
| T-12-03 | Repudiation | Per-page parity sign-off | accept | Operator-dated sign-off rows in 12-CUTOVER-CHECKLIST.md provide audit trail; no automated mitigation possible for a live-data eyeball check |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pytest + vitest already present per RESEARCH §Package Legitimacy Audit — N/A); nothing to verify |
</threat_model>

<verification>
- `python -m pytest tests/test_cutover_redirects.py tests/test_post_teardown.py --collect-only -q` collects both new files clean.
- `python -m pytest tests/test_spa_serving.py::test_api_not_shadowed_by_spa_mount -x` is green or skipped (CUT-01 precedence; never FAIL).
- `12-CUTOVER-CHECKLIST.md` has 8 parity rows in D-05 order.
- Note: the per-page redirect assertions and post-teardown assertions are intentionally RED until 12-02 / 12-03 land — collection-clean is the bar for this plan, not all-green.
</verification>

<success_criteria>
- CUT-01 confirmed satisfied by existing Phase-9 routing with zero code change (precedence test green/skipped).
- tests/test_cutover_redirects.py exists as the standing CUT-02 guard (7 page rows + unauth→/app/login).
- tests/test_post_teardown.py exists as the standing CUT-03 guard (deleted-404 + surviving-200 + import-api).
- 12-CUTOVER-CHECKLIST.md exists with 8 D-05-ordered operator parity rows.
</success_criteria>

<output>
Create `.planning/phases/12-parallel-run-cutover-htmx-decommission/12-01-SUMMARY.md` when done. Record the CUT-01 confirmation (parallel-run satisfied by Phase 9, no code change) explicitly in the summary.
</output>
