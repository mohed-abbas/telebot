---
phase: 12-parallel-run-cutover-htmx-decommission
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - dashboard.py
  - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md
autonomous: false
requirements: [CUT-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Each legacy page route 303-redirects to its /app/<page> SPA equivalent, but only after that page's MT5-demo parity row is operator-signed"
    - "Each page's redirect is its own single revertable commit (git revert = one-page rollback)"
    - "Pages cut over in D-05 order: analytics → signals → history → staged → overview → settings → positions, then kill-switch verified-only, then root / flips LAST"
    - "An unauthenticated hit to a cut-over page still bounces to login (Depends(_verify_auth) preserved on every redirected route)"
    - "The kill-switch parity is signed off (verified-then-decommission) even though it has no GET page to redirect"
  artifacts:
    - path: "dashboard.py"
      provides: "Per-page RedirectResponse('/app/<page>', status_code=303) bodies replacing the TemplateResponse bodies, root flipped last"
      contains: "RedirectResponse(url=\"/app/"
    - path: ".planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md"
      provides: "Operator-signed parity rows gating each redirect commit"
      contains: "signed:"
  key_links:
    - from: "dashboard.py legacy page routes"
      to: "/app/<page> SPA routes"
      via: "RedirectResponse 303"
      pattern: "RedirectResponse\\(url=\"/app/"
    - from: "each redirect commit"
      to: "12-CUTOVER-CHECKLIST.md row"
      via: "operator-dated sign-off referenced in commit message"
      pattern: "signed:"
---

<objective>
Cut the operator from the legacy HTMX dashboard to the already-built React SPA one page at a time (D-01), gated on a live MT5-demo parity sign-off per page (D-03/D-04), in the D-05 order with root `/` flipping LAST (D-02). Each page's redirect is a single revertable commit; no nginx change, no deletion (legacy stays reachable by direct URL for the bake window).

Purpose: This is the CUT-02 verification-and-flip phase. Phase 12 OWNS the still-open Phase 10/11 MT5-demo UAT (D-03) — verifying a page on the demo is exactly what unblocks its redirect commit. The live-money control surface must never regress, so read-only pages flip first and the destructive kill-switch is the last verification gate. No new page, endpoint, or trading-core change (hard out-of-scope per CONTEXT §domain).

Output: dashboard.py legacy page route bodies swapped to `RedirectResponse('/app/<page>', status_code=303)` (one commit each), root flipped last, and 8 operator-signed parity rows in 12-CUTOVER-CHECKLIST.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md

<interfaces>
<!-- The per-page redirect-target map (RESEARCH §Pattern 2). Line numbers drift on edit — re-grep before each commit. -->

| Legacy route | Current line | 303 target | D-05 order |
|--------------|--------------|------------|------------|
| GET /analytics | dashboard.py:993 | /app/analytics | 1 (pilot) |
| GET /signals   | dashboard.py:475 | /app/signals   | 2 |
| GET /history   | dashboard.py:422 | /app/history   | 3 |
| GET /staged    | dashboard.py:589 | /app/staged    | 4 |
| GET /overview  | dashboard.py:387 | /app/overview  | 5 |
| GET /settings  | dashboard.py:624 | /app/settings  | 6 |
| GET /positions | dashboard.py:411 | /app/positions | 7 |
| kill-switch (no GET page; /api/emergency-preview) | dashboard.py:1350 | /app/emergency (verify only — no redirect) | 8 |
| GET / (root)   | dashboard.py:382-384 | /app/ (flip from /overview) | FINAL (D-02) |

Existing redirect call style to mirror (dashboard.py:362): `RedirectResponse(url=next_path, status_code=303)` — use this keyword form, not a bare positional string.
Root route now (dashboard.py:384): `return RedirectResponse(url="/overview")` → flip to `RedirectResponse(url="/app/", status_code=303)` LAST.
Auth dep to PRESERVE on every redirected route: `user: str = Depends(_verify_auth)` (so unauth still bounces to login).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap read-only page bodies to /app redirects (analytics → signals → history → staged), one commit each, gated per checklist row</name>
  <files>dashboard.py, .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md</files>
  <read_first>
    - dashboard.py (the 4 read-only page routes: analytics :993, signals :475, history :422, staged :589 — re-grep exact lines before editing as numbers drift)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md (Pattern 1 + the dashboard.py self-analog redirect call style)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md (rows 1-4 must be operator-signed before each commit)
    - tests/test_cutover_redirects.py (the guard that goes green per page)
  </read_first>
  <action>
    For each of the 4 read-only pages in D-05 order (analytics, then signals, then history, then staged), and ONLY after that page's 12-CUTOVER-CHECKLIST.md parity row is operator-signed: replace the route body's TemplateResponse return with `return RedirectResponse(url="/app/<page>", status_code=303)`, keeping the `@app.get(...)` decorator, the function signature, and `user: str = Depends(_verify_auth)` intact (the response_class=HTMLResponse decorator arg becomes cosmetic — leave it; Commit 1 of 12-03 deletes the route entirely). Targets: /analytics→/app/analytics, /signals→/app/signals, /history→/app/history, /staged→/app/staged. Re-grep the route's current line before editing (numbers drift after each prior edit in this plan). Commit ONE PAGE PER COMMIT (D-01) with a message referencing the signed checklist row, e.g. `feat(12): cut over analytics → /app/analytics (CHECKLIST row 1)`. Do NOT touch nginx, do NOT delete the TemplateResponse imports or templates (that is 12-03), do NOT batch multiple pages into one commit.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -m pytest "tests/test_cutover_redirects.py::test_legacy_page_redirects_to_spa[/analytics-/app/analytics]" "tests/test_cutover_redirects.py::test_legacy_page_redirects_to_spa[/signals-/app/signals]" "tests/test_cutover_redirects.py::test_legacy_page_redirects_to_spa[/history-/app/history]" "tests/test_cutover_redirects.py::test_legacy_page_redirects_to_spa[/staged-/app/staged]" -x</automated>
  </verify>
  <acceptance_criteria>
    - GET /analytics → 303, Location: /app/analytics (test green or Postgres-skip).
    - GET /signals → 303, Location: /app/signals.
    - GET /history → 303, Location: /app/history.
    - GET /staged → 303, Location: /app/staged.
    - Each page is its own commit; commit message references its signed checklist row.
    - `Depends(_verify_auth)` still present on all 4 routes (grep `dashboard.py` shows each redirected route retains the auth dep).
    - No nginx/Dockerfile/template change in these commits.
  </acceptance_criteria>
  <done>The 4 read-only pages 303-redirect to their /app/ targets, each as a separate revertable commit gated on a signed parity row; cutover_redirects read-only cases green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Read-only pages (analytics, signals, history, staged) now redirect to the SPA; live-money pages (overview, settings, positions) are next and require live MT5-demo parity verification before their redirect commits.</what-built>
  <how-to-verify>
    On the VPS with the MT5 demo connected, for each live-money page in D-05 order (overview, then settings, then positions, then the kill-switch flow):
    1. Open the legacy page (e.g. https://&lt;host&gt;/overview) and the SPA page (https://&lt;host&gt;/app/overview) side by side.
    2. Confirm SPA numbers match legacy on live data (balances, equity, open P&L, positions, pending stages, analytics figures).
    3. Exercise the live-money actions against the demo broker: positions close / modify SL+TP / partial-close (absolute volume), kill-switch two-step preview→confirm, settings validate→confirm-diff→revert. Confirm each behaves correctly and the UI only updates on server-confirmed success.
    4. Open browser devtools: confirm NO console errors and that a background poll through ≥2 cycles never clobbers an open modal/drilldown.
    5. Sign the corresponding row in 12-CUTOVER-CHECKLIST.md with today's date (`[signed: YYYY-MM-DD operator]`). For kill-switch, sign its verified-then-decommission row (it has no redirect — its sign-off gates the 12-03 Commit-1 deletion of /api/emergency-preview).
  </how-to-verify>
  <resume-signal>Type "approved" once overview, settings, positions, and kill-switch rows are all signed, or describe parity issues to fix before cutover.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Swap live-money page bodies to /app redirects (overview → settings → positions), one commit each, then flip root LAST</name>
  <files>dashboard.py, .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md</files>
  <read_first>
    - dashboard.py (the live-money page routes: overview :387, settings :624, positions :411, root :382-384 — re-grep exact lines before editing)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-CUTOVER-CHECKLIST.md (rows 5-8 must be operator-signed before each commit)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md (§Pattern 2 kill-switch nuance — no GET page; verified-then-decommission)
  </read_first>
  <action>
    Only after the checkpoint sign-off, and respecting D-05 order: (a) swap /overview body → `RedirectResponse(url="/app/overview", status_code=303)`; (b) swap /settings → /app/settings; (c) swap /positions → /app/positions — each its own commit referencing its signed checklist row, each preserving `Depends(_verify_auth)`. (d) Kill-switch: do NOT add a redirect — it has no `@app.get` page route; confirm its parity row is signed (its removal of /api/emergency-preview happens in 12-03 Commit 1). (e) FINAL (D-02): flip the root `/` route body (dashboard.py:384) from `RedirectResponse(url="/overview")` to `RedirectResponse(url="/app/", status_code=303)` as the LAST commit, only after all 7 page rows + the kill-switch row are signed. Re-grep each route's line before editing. One page per commit; no nginx/Dockerfile/template/deletion changes here.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -m pytest tests/test_cutover_redirects.py -x</automated>
  </verify>
  <acceptance_criteria>
    - GET /overview → 303 Location /app/overview; GET /settings → 303 /app/settings; GET /positions → 303 /app/positions.
    - GET / → 303 Location /app/ (root flipped LAST, after every page).
    - All of tests/test_cutover_redirects.py's 7 page-redirect cases pass (or Postgres-skip) — the full CUT-02 guard is green except test_unauth_redirects_to_app_login (which stays red until 12-03 Commit-1 repoints _verify_auth — note this in the summary).
    - Each live-money page + the root flip is a separate revertable commit referencing its signed checklist row.
    - `Depends(_verify_auth)` retained on every redirected route.
  </acceptance_criteria>
  <done>All 7 pages redirect to /app, kill-switch parity is signed (verified-then-decommission), and root flips to /app/ as the final commit; CUT-02 cutover complete and fully reversible per-commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → dashboard.py page route | Untrusted session cookie; a redirected route must still gate on auth before bouncing |
| legacy page route → /app SPA route | The live-money control surface — a wrong redirect target or dropped auth dep exposes/breaks money controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Spoofing | Redirected route drops Depends(_verify_auth) | mitigate | Every swap preserves `user: str = Depends(_verify_auth)`; an unauth hit still 303s to login, never into the SPA unauthenticated — asserted by acceptance_criteria grep |
| T-12-05 | Tampering | Wrong /app/<page> redirect target sends operator to wrong control surface | mitigate | test_cutover_redirects.py asserts exact Location per page with follow_redirects=False; a typo fails the test |
| T-12-06 | Elevation | Live-money page (positions/kill-switch) cut over before MT5-demo parity proven | mitigate | D-05 order (live-money last) + blocking checkpoint requiring signed parity rows before the overview/settings/positions/kill-switch commits |
| T-12-07 | Denial of Service | Root flips before pages proven, stranding operator on an unverified SPA landing | mitigate | D-02: root `/` flips LAST, only after all 8 rows signed |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH §Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `python -m pytest tests/test_cutover_redirects.py -x` — all 7 page-redirect cases green (or Postgres-skip); test_unauth_redirects_to_app_login remains red until 12-03 (documented).
- `grep -c 'Depends(_verify_auth)'` against each redirected route confirms the auth dep survived every swap.
- `git log --oneline` shows one commit per page in D-05 order with root last, each referencing a signed checklist row.
- 12-CUTOVER-CHECKLIST.md: all 8 rows operator-signed with dates.
</verification>

<success_criteria>
- All 7 legacy page routes 303-redirect to their /app/<page> equivalents; root `/` redirects to /app/ as the final commit.
- Each redirect is an independently revertable single commit gated on a signed MT5-demo parity row (D-01/D-03/D-04).
- D-05 order honored (read-only first, kill-switch verified last, root flipped last per D-02).
- Live-money control surface never regressed — every redirected route retains its auth dep; legacy pages remain reachable by direct URL for the bake window (no deletion).
</success_criteria>

<output>
Create `.planning/phases/12-parallel-run-cutover-htmx-decommission/12-02-SUMMARY.md` when done. Note that test_unauth_redirects_to_app_login stays red until 12-03 Commit 1 repoints _verify_auth, and that legacy pages remain reachable (redirect only, no deletion) pending the 7-day bake.
</output>
