---
phase: 12-parallel-run-cutover-htmx-decommission
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - dashboard.py
  - templates/
  - static/vendor/
  - static/js/
  - static/css/input.css
  - tailwind.config.js
  - scripts/build_css.sh
  - Dockerfile
  - nginx/telebot.conf
  - tests/test_ui_substrate.py
  - tests/test_pending_stages_sse.py
  - tests/test_settings_form.py
  - tests/test_login_flow.py
  - tests/test_auth_session.py
autonomous: false
requirements: [CUT-03]
nyquist_compliant: true

must_haves:
  truths:
    - "After teardown, every deleted legacy HTML/partial/SSE route returns 404 and the dashboard still boots"
    - "dashboard.py survives as the FastAPI app host: app factory, /api/v2 router, /app + /static mounts, auth, /health, and / → /app/ all intact"
    - "The 6 api/-imported helpers (validate_settings_form, _compute_dry_run, _enrich_stage_for_ui, _client_ip, _password_hasher, app_settings) survive — `import api` resolves"
    - "docker build succeeds (no dangling COPY from a deleted stage/dir) and the SPA still builds"
    - "Unauthenticated hits bounce to /app/login (legacy /login deleted, _verify_auth repointed)"
    - "The nginx /login rate-limit block survives; only the SSE block is removed, and only after /stream is gone"
    - "D-06: teardown runs only after a live bake window — legacy HTMX stayed dormant-but-reachable during the bake so a single redirect revert restores it; deletion happens only once the SPA runs clean in production"
    - "D-09: dashboard.py reduced to surviving wiring (app factory + /api/v2 + /app + /static mounts + auth + /health + / → /app/ + the 6 api/-imported helpers); bot.py import unchanged"
    - "D-10: teardown landed in 4 grouped, independently-revertable commits (routes+SSE+Jinja / templates+vendor / Dockerfile-Stage-1+CSS-CLI+nginx-SSE / HTMX tests)"
  artifacts:
    - path: "dashboard.py"
      provides: "Reduced-to-wiring app host (HTML/partial/SSE routes + Jinja setup deleted, 6 helpers + auth + mounts kept)"
      contains: "app.include_router(api_router)"
    - path: "Dockerfile"
      provides: "Stage 1 css-build removed + Stage-3 templates/css COPY lines removed; Stage 2 spa-build + SPA overlay kept"
      contains: "spa-build"
    - path: "nginx/telebot.conf"
      provides: "SSE block removed; /login rate-limit block preserved"
      contains: "location = /login"
    - path: "tests/test_post_teardown.py"
      provides: "Green teardown guard (deleted-404 + surviving-200 + import-api)"
      exists: true
  key_links:
    - from: "api/auth.py, api/settings.py, api/stages.py"
      to: "dashboard.py (6 MUST-SURVIVE symbols)"
      via: "from dashboard import ..."
      pattern: "from dashboard import"
    - from: "_verify_auth unauth bounce"
      to: "/app/login"
      via: "repointed Location header"
      pattern: "/app/login"
    - from: "Dockerfile Stage 3"
      to: "spa-build dist"
      via: "COPY --from=spa-build"
      pattern: "COPY --from=spa-build"
---

<objective>
After a 7-day live bake and an explicit operator GO (D-07/D-08), tear down the now-dead HTMX/Jinja stack in 4 grouped, independently-revertable commits (D-10), reducing dashboard.py to its surviving wiring while keeping the live /api/v2 layer, SPA mount, auth, and the 6 api/-imported helpers intact (D-09). The live-money control surface must never regress.

Purpose: This is CUT-03. It is GATED — it must NOT autochain off CUT-02 and must NOT run inside the same uninterrupted session as the cutover (D-07/D-08). Two RESEARCH completeness corrections to CONTEXT's D-10 are mandatory: (1) Commit 1 MUST preserve the 6 api/-imported symbols (three — validate_settings_form, _compute_dry_run, _enrich_stage_for_ui — LOOK HTMX-era but are imported by the live /api/v2 layer; deleting any = ImportError → dashboard down); (2) Commit 3 MUST also delete the Dockerfile Stage-3 COPY lines (:63, 68-69) that break `docker build` once Stage 1 is gone.

Output: dashboard.py reduced to wiring; templates/ + Basecoat + HTMX JS bridge deleted; Dockerfile Stage 1 + Tailwind config/input/build-script removed (+ Stage-3 COPY fixes) ; nginx SSE block removed; HTMX-era tests pruned; tests/test_post_teardown.py green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md
@.planning/phases/12-parallel-run-cutover-htmx-decommission/12-VALIDATION.md
@tests/test_post_teardown.py
@tests/test_spa_serving.py

<interfaces>
<!-- The 6 MUST-SURVIVE dashboard.py symbols imported by the /api/v2 layer (RESEARCH lines 262-269). Deleting any = ImportError at boot. -->

from dashboard import _client_ip, _password_hasher, app_settings    # api/auth.py:100
from dashboard import validate_settings_form, _compute_dry_run      # api/settings.py:125,204
dashboard._enrich_stage_for_ui(...)                                 # api/stages.py:73
from dashboard import get_executor, get_settings_store              # api/deps.py
# Their transitive deps also survive: _SETTINGS_HARD_CAPS_INT, _SettingsValidationError,
# _get_settings_store, _accounts_by_name, _get_all_positions, _get_accounts_overview.

<!-- Pitfall-4 repoint: dashboard.py:146 — headers={"Location": f"/login?next={next_path}"} → "/app/login?next=..." -->

<!-- Dockerfile half-removal trap (Pitfall 1) — Commit 3 must delete these Stage-3 lines too: -->
#  Dockerfile:63  COPY templates/ ./templates/                          (templates/ deleted in Commit 2)
#  Dockerfile:68  COPY --from=css-build /build/static/css/app.*.css ...  (no css-build stage)
#  Dockerfile:69  COPY --from=css-build /build/static/css/manifest.json (no css-build stage)
#  KEEP Dockerfile:72  COPY --from=spa-build /spa/dist/ ./static/app/   (SPA overlay survives)

<!-- nginx SSE block to delete (nginx/telebot.conf:54-59): proxy_buffering off / proxy_cache off /
     proxy_http_version 1.1 / proxy_set_header Connection '' / proxy_read_timeout 86400s.
     KEEP location = /login rate-limit block (:36-45) and the base proxy_pass/proxy_set_header (:47-53). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CUT-02 cut every page over to the SPA; legacy HTMX has been running dormant-but-reachable during the bake window. Teardown deletes it permanently — it must wait for the bake + explicit GO (D-07/D-08).</what-built>
  <how-to-verify>
    Confirm the bake gate is satisfied BEFORE any deletion:
    1. All 8 pages have served from the SPA (/app) in production for at least 7 days of live trading (covers daily signal flow + a weekend) with NO operator-reported regression on any page, especially the live-money control surface (close / modify SL+TP / partial-close / kill-switch / settings).
    2. During the bake, legacy was still reachable by direct URL (only redirects were added in CUT-02, nothing deleted) — confirm no rollback was needed.
    3. Confirm no external script/probe/bookmark depends on the legacy `/login`, `/api/emergency-close`, `/api/resume-trading`, or `/api/trading-status` bare paths (RESEARCH Assumptions A3/A6) before they are deleted.
    Once satisfied, give the explicit go-ahead to run teardown.
  </how-to-verify>
  <resume-signal>Type "GO" to authorize the CUT-03 teardown after 7 clean bake days, or describe a regression to roll back first.</resume-signal>
</task>

<task type="auto">
  <name>Commit 1: dashboard.py surgery — delete HTML/partial/SSE routes + Jinja setup, KEEP the 6 api/-imported helpers, repoint _verify_auth → /app/login</name>
  <files>dashboard.py</files>
  <read_first>
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md (§Commit 1 — the authoritative line-by-line DELETE list AND the MUST-SURVIVE block at lines 262-269; Pitfall 3 + Pitfall 4)
    - dashboard.py (re-grep every target before deleting — line numbers drift; verify the MUST-SURVIVE symbol bodies are NOT in the delete ranges)
    - api/auth.py, api/settings.py, api/stages.py, api/deps.py (the `from dashboard import` lines — confirm which symbols must survive)
    - tests/test_post_teardown.py (the guard this commit starts turning green)
  </read_first>
  <action>
    In dashboard.py, delete the legacy presentation surface per RESEARCH §Commit 1: split the `from fastapi.responses import HTMLResponse, StreamingResponse, RedirectResponse` import to keep ONLY RedirectResponse; remove `from fastapi.templating import Jinja2Templates`, the `templates = Jinja2Templates(...)` setup, the asset-manifest machinery (`_asset_manifest`, `_load_manifest` def+call, `asset_url` def + globals registration, `_slug` def + filter registration), `_render_login`, and the dead HTMX CSRF dep `_verify_csrf`. Delete the HTML page routes (/overview, /positions, /history, /signals, /staged, /settings, /analytics — now redirect stubs from 12-02), the HTML partial routes (/partials/pending_stages, /partials/positions, /partials/position_drilldown/{...}, /partials/edit-levels/{...}, /partials/overview), the HTML settings POST handlers (/settings/{a}, /settings/{a}/confirm, /settings/{a}/revert) and their HTML-only render helpers (_append_to_response_body, _render_toast_oob, _render_tab_partial), the legacy HTML trade-action routes (/api/close, /api/modify-sl, /api/modify-tp, _render_edit_modal_with_error, /api/modify-levels, /api/close-partial, /api/emergency-preview, /api/trading-status, /api/emergency-close, /api/resume-trading), the SSE /stream route, and the staged HTML-only label helpers (_RESOLVED_STATUS_LABELS, _label_resolved_stage — only after grep-confirming api/stages.py has its own _enrich_resolved and does NOT import them, RESEARCH A7). Also delete the legacy GET+POST /login routes + login.html coupling. REPOINT _verify_auth's `headers={"Location": f"/login?next=..."}` to `/app/login?next=...` (Pitfall 4) and repoint /logout's redirect to /app/login. MUST NOT DELETE the 6 MUST-SURVIVE symbols and their transitive deps: validate_settings_form, _compute_dry_run, _enrich_stage_for_ui, _client_ip, _password_hasher, app_settings, _SETTINGS_HARD_CAPS_INT, _SettingsValidationError, _get_settings_store, _accounts_by_name, _get_all_positions, _get_accounts_overview. MUST NOT touch the app factory, lifespan, include_router(api_router), register_error_handlers, SessionMiddleware, /static + /app (SpaStaticFiles) mounts, /health, the surviving `/` → RedirectResponse('/app/'), the accessors (init_dashboard, get_executor, get_notifier, get_settings, get_settings_store), or bot.py's import. Prune the now-dead test files in the same commit IF their targets are deleted here (test_pending_stages_sse.py, test_settings_form.py, test_login_flow.py — see Commit 4) to keep the suite from going red, OR sequence so Commit 4 lands the prune; either way the plan's final gate must be green.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -c "import dashboard" && python -c "import api" && python -m pytest tests/test_api_settings.py tests/test_stages_contract.py tests/test_api_csrf.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import dashboard"` exits 0 (no syntax error, no dangling reference to a deleted helper).
    - `python -c "import api"` exits 0 — forces the lazy `from dashboard import ...` lines to resolve, proving all 6 MUST-SURVIVE symbols still exist (the Pitfall-3 guard).
    - `grep -n 'def validate_settings_form\|def _compute_dry_run\|def _enrich_stage_for_ui\|def _client_ip\|app_settings\|_password_hasher' dashboard.py` shows all 6 symbols still defined.
    - `grep -n '/app/login' dashboard.py` shows _verify_auth (and /logout) repointed; no remaining `"/login?next='` Location.
    - `grep -c 'TemplateResponse\|Jinja2Templates\|@app.get("/stream"\|@app.get("/partials' dashboard.py` (with `grep -v '^#'` filter) returns 0.
    - `pytest tests/test_api_settings.py tests/test_stages_contract.py tests/test_api_csrf.py -x` green or Postgres-skip (the /api/v2 settings/stages/csrf paths still work via the surviving helpers).
  </acceptance_criteria>
  <done>dashboard.py is reduced to wiring + the 6 surviving helpers + auth; deleted routes will 404; _verify_auth bounces to /app/login; `import dashboard` and `import api` both resolve.</done>
</task>

<task type="auto">
  <name>Commit 2: delete templates/ + static/vendor/ (Basecoat) + static/js/ (HTMX bridge)</name>
  <files>templates/, static/vendor/, static/js/</files>
  <read_first>
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md (§Commit 2 — the 21-file templates/ list + static/vendor/ + the static/js/htmx_basecoat_bridge.js addition CONTEXT omits)
  </read_first>
  <action>
    Delete the entire templates/ directory (all 21 Jinja files: base.html, login.html, overview.html, positions.html, history.html, signals.html, staged.html, settings.html, analytics.html, and the partials/ subdir). Delete static/vendor/ (Basecoat: basecoat.css, basecoat.min.js). Delete static/js/htmx_basecoat_bridge.js (the HTMX-after-swap re-init bridge — RESEARCH adds this to Commit 2; CONTEXT's D-10 omitted it); if static/js/ is then empty, remove the dir. KEEP static/app/ (the built SPA served at /app) and static/css/ (handled in Commit 3). This commit only removes files — no dashboard.py edit (Commit 1 already removed every reference to these templates).
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && test ! -d templates && test ! -d static/vendor && test ! -f static/js/htmx_basecoat_bridge.js && test -d static/app && python -c "import dashboard" && echo "templates+vendor+bridge gone, static/app + import intact"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/` directory no longer exists.
    - `static/vendor/` no longer exists.
    - `static/js/htmx_basecoat_bridge.js` no longer exists.
    - `static/app/` still exists (SPA bundle preserved).
    - `python -c "import dashboard"` still exits 0 (no template reference left dangling — Commit 1 already cleaned them).
  </acceptance_criteria>
  <done>The Jinja template tree, Basecoat vendor assets, and the HTMX JS bridge are deleted; the SPA bundle and dashboard import remain intact.</done>
</task>

<task type="auto">
  <name>Commit 3: Dockerfile Stage 1 + Tailwind config/input + build script + nginx SSE block (with the Stage-3 COPY corrections)</name>
  <files>Dockerfile, nginx/telebot.conf, tailwind.config.js, static/css/input.css, scripts/build_css.sh</files>
  <read_first>
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md (§Commit 3 + Pitfall 1 — the Stage-3 COPY lines :63,68,69 CONTEXT omits; §Runtime State Inventory build-artifacts row)
    - Dockerfile (re-grep Stage 1 :1-40, Stage-3 COPY :63/64/65/68/69/72, Stage 2 :42-52 — verify line numbers)
    - nginx/telebot.conf (the SSE block :54-59 inside location /; the /login rate-limit block :36-45 that MUST survive)
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-PATTERNS.md (Dockerfile + nginx self-analog notes)
  </read_first>
  <action>
    Dockerfile: delete Stage 1 `css-build` entirely (FROM debian:bookworm-slim AS css-build through RUN bash scripts/build_css.sh, incl. the WR-06 comment block). Per Pitfall 1 (CONTEXT omits these — REQUIRED): also delete the Stage-3 lines `COPY templates/ ./templates/` (:63), `COPY --from=css-build /build/static/css/app.*.css ./static/css/` (:68), and `COPY --from=css-build /build/static/css/manifest.json ./static/css/` (:69) — they reference the deleted templates/ dir and the deleted css-build stage. KEEP Stage 2 `spa-build` (:42-52), the Stage-3 SPA overlay `COPY --from=spa-build /spa/dist/ ./static/app/` (:72), `COPY static/ ./static/` (:64, now copies less), `COPY *.py *.json` (:61), `COPY api/` (:62), `COPY scripts/` (:65). Repo files: delete tailwind.config.js, static/css/input.css, scripts/build_css.sh, static/css/_compat.css (RESEARCH A4 — no remaining reference), and any committed hashed static/css/app.*.css + static/css/manifest.json artifacts. nginx/telebot.conf: delete ONLY the SSE sub-block inside `location /` (proxy_buffering off / proxy_cache off / proxy_http_version 1.1 / proxy_set_header Connection '' / proxy_read_timeout 86400s, :54-59). KEEP the `location = /login` rate-limit block (:36-45) and the base proxy_pass + proxy_set_header lines (:47-53). Ordering: this commit lands AFTER Commit 1 deleted /stream (Pitfall 2 — never remove the SSE directives while /stream is live). The nginx file edit is git-only; flag the VPS deploy (copy telebot.conf to /home/murx/shared/nginx/conf.d/ + `docker exec shared-nginx nginx -s reload`) as an operator copy-paste step in the SUMMARY (do not run locally).
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && test ! -f tailwind.config.js && test ! -f static/css/input.css && test ! -f scripts/build_css.sh && ! grep -q 'AS css-build' Dockerfile && ! grep -q 'from=css-build' Dockerfile && grep -q 'spa-build' Dockerfile && grep -q 'location = /login' nginx/telebot.conf && ! grep -q 'proxy_read_timeout 86400s' nginx/telebot.conf && docker build -t telebot:teardown-check .</automated>
  </verify>
  <acceptance_criteria>
    - `tailwind.config.js`, `static/css/input.css`, `scripts/build_css.sh` no longer exist.
    - Dockerfile contains no `AS css-build` stage and no `--from=css-build` COPY (the Stage-3 trap lines removed); still contains the `spa-build` stage and the `--from=spa-build` SPA overlay COPY.
    - nginx/telebot.conf still contains `location = /login` (rate-limit preserved) and no longer contains `proxy_read_timeout 86400s` (SSE block removed).
    - `docker build -t telebot:teardown-check .` succeeds (proves the half-removal trap is avoided — Pitfall 1).
  </acceptance_criteria>
  <done>Tailwind CLI build stage + config/input/script gone, Dockerfile Stage-3 COPY corrected, nginx SSE block removed with /login rate-limit preserved, and `docker build` is green.</done>
</task>

<task type="auto">
  <name>Commit 4: prune HTMX-era tests + final green gate</name>
  <files>tests/test_ui_substrate.py, tests/test_pending_stages_sse.py, tests/test_settings_form.py, tests/test_login_flow.py, tests/test_auth_session.py</files>
  <read_first>
    - .planning/phases/12-parallel-run-cutover-htmx-decommission/12-RESEARCH.md (§Commit 4 — the per-file disposition table + the surgical-prune list for test_auth_session.py; A6 login-coverage confirm)
    - tests/test_auth_session.py (the tests to surgically remove vs keep)
    - tests/test_post_teardown.py (the guard that must be fully green at the plan gate)
  </read_first>
  <action>
    Delete the HTMX-era test files whose targets no longer exist: tests/test_ui_substrate.py (asserts Basecoat/tailwind.config.js/input.css/htmx_basecoat_bridge.js), tests/test_pending_stages_sse.py (asserts /stream + /staged Jinja + /partials/pending_stages), tests/test_settings_form.py (asserts GET /settings Jinja + POST /settings HTML modal — JSON coverage survives in tests/test_api_settings.py). Delete tests/test_login_flow.py ONLY after confirming a /api/v2/auth/* login regression test exists (tests/test_api_csrf.py + the auth contract — RESEARCH A6); if absent, port the missing coverage to a surviving file before deleting. SURGICALLY prune tests/test_auth_session.py (do NOT delete the whole file): KEEP test_health_route_open and test_session_middleware_registered; REMOVE test_page_route_redirects_on_missing_session, test_htmx_route_returns_401_on_missing_session, test_asset_url_helper_registered, test_base_html_has_no_play_cdn (all reference deleted routes/templates/helpers). Optionally add an /app/login-bounce test there mirroring the Pitfall-4 repoint. KEEP test_spa_serving.py, test_simulator.py, all test_api_*.py / *_contract.py / test_rate_limit.py / test_settings_store.py, conftest.py, _bot_core_diff_guard.py untouched. After pruning, the full suite + the post-teardown guard must be green.
  </action>
  <verify>
    <automated>test ! -f tests/test_ui_substrate.py && test ! -f tests/test_pending_stages_sse.py && test ! -f tests/test_settings_form.py && test ! -f tests/test_login_flow.py && python -m pytest tests/ -x && python -c "import api"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ui_substrate.py, tests/test_pending_stages_sse.py, tests/test_settings_form.py, tests/test_login_flow.py no longer exist.
    - tests/test_auth_session.py still exists and retains test_health_route_open + test_session_middleware_registered; no longer contains test_page_route_redirects_on_missing_session / test_htmx_route_returns_401_on_missing_session / test_asset_url_helper_registered / test_base_html_has_no_play_cdn.
    - `python -m pytest tests/ -x` is fully green (or Postgres-skips) — no red test at the plan gate.
    - `python -m pytest tests/test_post_teardown.py -x` is green: deleted routes 404, surviving routes 200/JSON, `/` → 303 /app/, `import api` resolves.
    - `python -c "import api"` exits 0 (the 6 MUST-SURVIVE symbols still resolve).
    - `cd frontend && npm run build` succeeds (no shared CSS/asset broke).
  </acceptance_criteria>
  <done>HTMX-era tests pruned (whole-file deletes + the surgical test_auth_session.py prune), the full backend suite + test_post_teardown.py are green, `import api` resolves, and the SPA still builds — CUT-03 teardown complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api/ layer → dashboard.py | The /api/v2 layer imports 6 symbols from dashboard.py; deleting any breaks the live JSON API at boot |
| operator browser → surviving auth | Legacy /login deleted — unauth bounces must route to the surviving /app/login, not a 404 |
| nginx → dashboard.app | The /login rate-limit block and base proxy must survive; only the dead SSE block is removed |
| Dockerfile build stages | Stage-3 COPY must not reference a deleted stage/dir or the image won't build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Denial of Service | Deleting a MUST-SURVIVE dashboard.py helper → ImportError at boot → dashboard down → bot.py launch fails | mitigate | Commit 1 gated by `python -c "import dashboard" && python -c "import api"` + pytest of the /api/v2 settings/stages/csrf paths; the 6-symbol KEEP list is load-bearing (Pitfall 3) |
| T-12-09 | Elevation/Spoofing | Auth bypass via _verify_auth redirecting to a deleted /login (404) | mitigate | Commit 1 repoints _verify_auth + /logout → /app/login (Pitfall 4); test_post_teardown unauth-bounce + SPA global 401 loop-break (SPA-04) |
| T-12-10 | Tampering | Silently dropping server-side settings hard caps by deleting validate_settings_form | mitigate | It SURVIVES (MUST-SURVIVE list); tests/test_api_settings.py exercises the caps via /api/v2 and stays green through teardown |
| T-12-11 | Denial of Service | Removing the nginx /login rate-limit block by accident | mitigate | Commit 3 KEEPS `location = /login` (:36-45); only the SSE block (:54-59) is removed — asserted by `grep -q 'location = /login'` |
| T-12-12 | Tampering | Removing nginx SSE directives while /stream still live → frozen legacy page during bake | mitigate | Ordering: Commit 1 (delete /stream) lands before Commit 3 (remove SSE block) — Pitfall 2 |
| T-12-13 | Tampering | A dead-but-reachable legacy money endpoint surviving post-teardown | mitigate | Commit 1 deletes /api/close|modify-*|close-partial|emergency-close|resume-trading; test_post_teardown asserts they 404 |
| T-12-14 | Denial of Service | Half-removed Dockerfile (Stage 1 gone, Stage-3 COPY left) → docker build fails | mitigate | Commit 3 deletes Dockerfile :63,68,69 and is gated by `docker build .` (Pitfall 1) |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — this phase only deletes (RESEARCH §Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `python -c "import dashboard" && python -c "import api"` exits 0 after every commit (the 6-helper guard).
- `docker build -t telebot:teardown-check .` succeeds after Commit 3 (Dockerfile half-removal trap avoided).
- `python -m pytest tests/ -x` fully green after Commit 4; `python -m pytest tests/test_post_teardown.py -x` green (deleted-404 / surviving-200 / import-api).
- `cd frontend && npm run build` succeeds.
- `grep -q 'location = /login' nginx/telebot.conf` (rate-limit preserved) and `! grep -q 'proxy_read_timeout 86400s' nginx/telebot.conf` (SSE removed).
- Each of the 4 commits is independently revertable (`git revert` of any one leaves a buildable tree).
- VPS deploy of nginx/telebot.conf (copy to /home/murx/shared/nginx/conf.d/ + `docker exec shared-nginx nginx -s reload`) handed to the operator as copy-paste text — NOT run locally.
</verification>

<success_criteria>
- dashboard.py reduced to wiring (D-09): app factory + /api/v2 router + /app + /static mounts + auth + /health + / → /app/ + the 6 api/-imported helpers SURVIVE; all TemplateResponse/partial/SSE routes + Jinja setup + asset-manifest machinery + legacy /login + dead legacy money routes DELETED.
- templates/, static/vendor/, static/js/htmx_basecoat_bridge.js deleted; static/app/ + the SPA build preserved.
- Dockerfile Stage 1 css-build + tailwind.config.js + input.css + build_css.sh removed, Stage-3 COPY corrected, `docker build` green; Stage 2 spa-build + SPA overlay survive.
- nginx SSE block removed (after /stream deletion), /login rate-limit block preserved.
- HTMX-era tests pruned; full suite + test_post_teardown.py green; `import api` resolves; SPA builds.
- Teardown ran only AFTER the 7-day bake + explicit operator GO (D-07/D-08); 4 independently-revertable commits (D-10).
</success_criteria>

<output>
Create `.planning/phases/12-parallel-run-cutover-htmx-decommission/12-03-SUMMARY.md` when done. Include the operator VPS deploy step for nginx/telebot.conf (copy to /home/murx/shared/nginx/conf.d/ + `docker exec shared-nginx nginx -s reload`) as copy-paste text, and confirm the bake-gate GO was received before teardown ran.
</output>
