---
phase: 10-read-only-page-migration-analytics-pilot-signals-history-sta
plan: 06
type: execute
wave: 4
depends_on: ["10-02", "10-04", "10-05"]
files_modified:
  - frontend/src/lib/useElapsed.ts
  - frontend/src/routes/StagedView.tsx
  - frontend/src/routes/router.tsx
  - frontend/src/components/shell/Sidebar.tsx
autonomous: true
requirements: [PAGE-04]
must_haves:
  truths:
    - "Staged page polls every ~3s via refetchInterval (D-07); only the staged page polls"
    - "Active stages render as card-per-account (D-08) showing Stages (filled/total — correct values, D-13), Target Band, Current Price, Elapsed"
    - "Elapsed is a client-side per-second ticking timer off the server started_at epoch (D-06) — smooth between polls, not jumpy"
    - "Recently-resolved stages render in the shared DataTable with the status_label map applied client-side"
    - "Loading/Empty/inline Error states; all money/price from server _display strings (Pitfall 5)"
    - "Pending Stages sidebar link active; route mounted under the boot guard"
    - "ProbeView throwaway diagnostic is removed"
  artifacts:
    - path: "frontend/src/lib/useElapsed.ts"
      provides: "Per-second ticking elapsed hook off a server ISO epoch (D-06)"
      contains: "setInterval"
    - path: "frontend/src/routes/StagedView.tsx"
      provides: "PAGE-04 staged page (active cards + resolved table)"
  key_links:
    - from: "frontend/src/routes/StagedView.tsx"
      to: "/api/v2/stages"
      via: "useQuery refetchInterval 3000 (D-07)"
      pattern: "refetchInterval"
    - from: "frontend/src/routes/StagedView.tsx"
      to: "useElapsed(started_at)"
      via: "client timer counts off the server started_at from the 10-02 widening"
      pattern: "useElapsed"
---

<objective>
Migrate the STAGED-ENTRIES page (PAGE-04) — the ONLY page that background-polls (~3s, D-07).
Active stages render as cards-per-account (D-08) with a smooth client-side ticking elapsed
timer (D-06/`useElapsed`) counting off the server `started_at` epoch from the 10-02 widening;
recently-resolved stages render in the shared `DataTable`. Renders the CORRECT filled/total/
distance values (D-13), documenting the legacy blank-cell bug as a parity exception. This is
the LAST page of the read-only migration — it also removes the throwaway ProbeView.

Purpose: SC#4 (staged parity with live polling + elapsed) and closes PAGE-04.
Output: useElapsed hook + StagedView + route/sidebar wiring + ProbeView removal.
Depends on 10-02 (started_at API), 10-04 (primitives), 10-05 (router/sidebar baseline — serial file ownership).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md

<interfaces>
<!-- Inherited primitives + the 10-02 stages payload. Read before building. -->
frontend/src/components/data/DataTable.tsx — Column<Row> + DataTable (Plan 04) for the resolved table.
frontend/src/components/state/{Loading,Empty,ErrorPanel}.tsx — state trio (Plan 04).
frontend/src/lib/http.ts — api(path); GET needs no CSRF.
frontend/src/lib/queryClient.ts — inherited: refetchIntervalInBackground:false (so the 3s poll
  auto-pauses on a hidden tab), keepPreviousData, staleTime 1000, retry false, global onAuthError.
frontend/src/routes/ProbeView.tsx — THROWAWAY diagnostic (its banner says "removed in Phase 10") — DELETE it.
frontend/src/routes/router.tsx — post-Plan-05: analytics/signals/history children + index ProbeView.
frontend/src/components/shell/Sidebar.tsx — post-Plan-05: Analytics/Signal Log/Trade History active;
  Pending Stages still a disabled span (flip here); Positions/Settings stay disabled (Phase 11).
10-02 stages payload (PAGE-04): active rows carry account_name, symbol, direction, filled, total,
  band_low(+_display), band_high(+_display), current_price(+_display), distance_str, status,
  started_at (ISO+offset), started_at_display. resolved rows carry account, symbol, direction,
  stage_number, status, created_at(+_display), filled_at(+_display), cancelled_reason.
Legacy parity (RESEARCH §5):
  active cards (pending_stages.html): symbol+BUY/SELL badge+account, Stages filled/total, Target
    Band (band_low–band_high), Current Price, Elapsed. (Legacy renders Stages/Distance BLANK due to a
    field-name bug — D-13: the SPA shows CORRECT values; documented parity exception.)
  resolved table (staged.html:29-69) columns: Account, Symbol, Direction, Stage, Status(status_label),
    Reason(cancelled_reason), Time. status_label map (dashboard.py:489 _RESOLVED_STATUS_LABELS):
    cancelled_by_kill_switch -> "Kill-switch drain", cancelled_stage1_closed -> "Stage 1 exited",
    cancelled_target_reached -> "Target/SL reached", abandoned_reconnect -> "Abandoned (reconnect)",
    failed -> "Failed", capped -> "Capped", filled -> "Filled".
useElapsed pattern (PATTERNS.md section useElapsed lines 361-382 / RESEARCH Pattern 2 lines 288-302)
  — Pitfall-5-EXEMPT (relative duration off a server epoch, not money/price re-rounding).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the useElapsed ticking-timer hook</name>
  <files>frontend/src/lib/useElapsed.ts</files>
  <read_first>
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (useElapsed section lines 361-382 — copy verbatim)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md (Pattern 2 lines 282-304 — why Pitfall-5-exempt)
  </read_first>
  <action>
    Create frontend/src/lib/useElapsed.ts copying the PATTERNS.md useElapsed hook verbatim
    (lines 369-381): useElapsed(startedAtIso: string): string — a 1s setInterval updating a
    now state, returning MM:SS / H:MM:SS computed from now minus Date.parse(startedAtIso),
    clamped at 0. The epoch is the server started_at (10-02). This is the ONE client-side number
    computation allowed (a duration is not money/price — D-06). Clean up the interval on unmount.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot/frontend && npm run build && grep -q setInterval frontend/src/lib/useElapsed.ts</automated>
  </verify>
  <acceptance_criteria>
    - cd frontend && npm run build exits 0
    - grep -q setInterval frontend/src/lib/useElapsed.ts AND grep -q clearInterval frontend/src/lib/useElapsed.ts
    - grep -q "Date.parse" frontend/src/lib/useElapsed.ts (counts off the server ISO epoch)
  </acceptance_criteria>
  <done>useElapsed ticks per-second off a server ISO epoch; cleans up on unmount.</done>
</task>

<task type="auto">
  <name>Task 2: Build StagedView (active cards + resolved table + polling)</name>
  <files>frontend/src/routes/StagedView.tsx</files>
  <read_first>
    - frontend/src/routes/ProbeView.tsx (the refetchInterval:3000 useQuery analog — exact polling pattern)
    - frontend/src/lib/useElapsed.ts (Task 1) + components/data/DataTable.tsx + state/* (primitives)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (_enrich_active field correction D-13 lines 245-254; Resolved status_label lines 250-254)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md (§5 staged reconciliation lines 192-198; Code Examples StageCard lines 409-425)
  </read_first>
  <action>
    Build StagedView with useQuery queryKey ["stages"], queryFn api("/api/v2/stages"), and
    refetchInterval: 3000 — the ONLY polling page (D-07); background pause is free via the
    inherited refetchIntervalInBackground:false. Render active as a StageCard per account
    (D-08, RESEARCH Code Examples lines 409-425): symbol + BUY/SELL badge + account_name;
    fields Stages (s.filled/s.total — the CORRECT keys, D-13; NOT filled_count/total_stages),
    Target Band (band_low_display – band_high_display, mono), Current Price
    (current_price_display fallback "—", mono), Elapsed (useElapsed(s.started_at), mono). Render
    resolved via the shared DataTable with legacy columns: Account, Symbol, Direction (badge),
    Stage (stage_number), Status (apply the _RESOLVED_STATUS_LABELS map CLIENT-SIDE per
    PATTERNS.md Resolved status_label — pure presentation strings), Reason (cancelled_reason),
    Time (created_at_display or filled_at_display). Query states: isPending -> Loading,
    isError -> ErrorPanel onRetry={refetch}, both lists empty -> Empty (use the legacy empty-state
    copy). No manual Refresh needed (it polls). Render only _display strings; the ONLY client
    computation is the elapsed duration.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot/frontend && npm run build && grep -rn "toFixed\|Intl.NumberFormat\|filled_count\|total_stages" frontend/src/routes/StagedView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - cd frontend && npm run build exits 0
    - grep -rn "toFixed\|Intl.NumberFormat\|filled_count\|total_stages" frontend/src/routes/StagedView.tsx returns NO matches (Pitfall 5 + D-13 — correct keys, no legacy blank-cell names)
    - grep -q refetchInterval frontend/src/routes/StagedView.tsx (D-07 — this page polls)
    - grep -q useElapsed frontend/src/routes/StagedView.tsx AND grep -q started_at frontend/src/routes/StagedView.tsx
    - grep -q "Kill-switch drain" frontend/src/routes/StagedView.tsx (status_label map applied)
  </acceptance_criteria>
  <done>Staged page reaches parity: polling, card-per-account, smooth elapsed, correct counts, resolved table.</done>
</task>

<task type="auto">
  <name>Task 3: Wire staged route, enable Pending Stages link, remove ProbeView</name>
  <files>frontend/src/routes/router.tsx, frontend/src/components/shell/Sidebar.tsx</files>
  <read_first>
    - frontend/src/routes/router.tsx (post-Plan-05 state — analytics/signals/history children + index ProbeView)
    - frontend/src/components/shell/Sidebar.tsx (post-Plan-05 — Pending Stages still disabled)
    - frontend/src/routes/ProbeView.tsx (the throwaway to delete)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (wire-in lines 386-397; Open Question 2 — index landing)
  </read_first>
  <action>
    ROUTER: add { path: "stages", element: <StagedView/> } under path "/". Remove the
    import of ProbeView and its index child usage. For the index route (Open Question 2 —
    Overview is Phase 11), set the index to redirect to a shipped page rather than the deleted
    ProbeView: make the index element redirect to /analytics using react-router-dom Navigate
    (e.g. element: <Navigate to="/analytics" replace/>) so /app/ lands on the pilot. Import
    StagedView and Navigate.
    SIDEBAR: flip the "Pending Stages" entry to <NavLink to="/stages"> (active cyan-accent
    treatment, copying the live Overview/Analytics NavLink block). Positions/Settings remain
    disabled spans (Phase 11). Label stays "Pending Stages".
    CLEANUP: delete frontend/src/routes/ProbeView.tsx (the diagnostic scaffold; its own banner
    says it is removed in Phase 10). Ensure no remaining import references it.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot/frontend && npm run build && test ! -f frontend/src/routes/ProbeView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - cd frontend && npm run build exits 0
    - grep -q "path: \"stages\"" frontend/src/routes/router.tsx (route mounted)
    - grep -q "to=\"/stages\"" frontend/src/components/shell/Sidebar.tsx (active NavLink)
    - test ! -f frontend/src/routes/ProbeView.tsx (throwaway removed) AND grep -rL ProbeView frontend/src shows no remaining references
    - Positions and Settings remain disabled spans (Phase 11)
  </acceptance_criteria>
  <done>Staged page reachable at /app/stages from an active sidebar link; /app/ lands on analytics; ProbeView gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser SPA → /api/v2/stages | Authenticated GET via api() (session cookie); polled every 3s |
| server started_at epoch → client timer | The elapsed duration is computed client-side off a server ISO timestamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Spoofing | stages poll | mitigate | api() credentials same-origin httpOnly cookie; 401 → inherited global onAuthError redirect |
| T-10-16 | Information Disclosure | rendered prices | mitigate | band/current-price rendered from server `_display` strings only (Pitfall 5); only the elapsed duration is client-computed (D-06, exempt) |
| T-10-17 | Denial of Service | 3s poll load | accept | Single-operator tool; refetchIntervalInBackground:false pauses on a hidden tab (inherited); 3s interval matches legacy hx-trigger cadence |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (zero new deps this phase) |
</threat_model>

<verification>
- cd frontend && npm run build exits 0.
- grep -rn "toFixed\|Intl.NumberFormat" frontend/src/routes/StagedView.tsx returns no matches on money/price cells.
- Manual (SC#4 + SC#5): open /app/stages beside legacy /staged on the same DB; compare active cards and resolved rows field-by-field. Document the legacy blank-cell Stages/Distance bug (D-13) as an accepted parity exception (SPA correct, legacy buggy). Watch a card ≥10s: elapsed increments smoothly per-second, not in 3s poll jumps (D-06). /app/ redirects to /app/analytics; ProbeView removed.
</verification>

<success_criteria>
- PAGE-04 reaches parity: live polling, card-per-account, smooth elapsed, correct stage counts, resolved table.
- All four read-only pages now ship; ProbeView removed; /app/ lands on the analytics pilot.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-06-SUMMARY.md` when done
</output>
