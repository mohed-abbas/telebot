---
phase: 10-read-only-page-migration-analytics-pilot-signals-history-sta
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/src/components/data/DataTable.tsx
  - frontend/src/components/state/Loading.tsx
  - frontend/src/components/state/Empty.tsx
  - frontend/src/components/state/ErrorPanel.tsx
  - frontend/src/lib/useUrlFilters.ts
  - frontend/src/routes/AnalyticsView.tsx
  - frontend/src/routes/router.tsx
  - frontend/src/components/shell/Sidebar.tsx
autonomous: true
requirements: [PAGE-01]
must_haves:
  truths:
    - "Analytics page renders summary KPIs (win rate, profit factor, gross P/L, net P&L), the Performance-by-Source DataTable, extremes, and a conditional Avg-Stages card"
    - "Range tabs (7d/30d/90d/all) and source filter live in the URL (?range=&source=); default = all-time, no source"
    - "Clicking a by-source row sets ?source=<name> and re-queries (push navigation)"
    - "Avg-Stages card renders ONLY when a source filter is active (avg_stages truthy — Pitfall 3)"
    - "Loading shows skeleton, empty shows Empty panel, error shows inline ErrorPanel with Retry (D-11), 401 redirects via inherited global handler"
    - "All numbers render server _display strings — no client toFixed/Intl on money/price (Pitfall 5)"
    - "Analytics nav link is active in the Sidebar; route mounted under the boot guard"
  artifacts:
    - path: "frontend/src/components/data/DataTable.tsx"
      provides: "Shared column-driven table (sticky header, align, mono, color-by-sign) — Phase 11 inherits"
    - path: "frontend/src/components/state/ErrorPanel.tsx"
      provides: "Inline error + Retry (D-11)"
    - path: "frontend/src/lib/useUrlFilters.ts"
      provides: "useSearchParams-backed URL filter helper shared by analytics + history"
    - path: "frontend/src/routes/AnalyticsView.tsx"
      provides: "PAGE-01 analytics page (the pilot)"
  key_links:
    - from: "frontend/src/routes/AnalyticsView.tsx"
      to: "/api/v2/analytics"
      via: "useQuery queryKey ['analytics', filters], queryFn api(...)"
      pattern: "api\\(.*api/v2/analytics"
    - from: "frontend/src/routes/AnalyticsView.tsx"
      to: "useUrlFilters"
      via: "range/source filter state from the URL drives the query key"
      pattern: "useUrlFilters"
    - from: "frontend/src/routes/router.tsx"
      to: "AnalyticsView"
      via: "route child under the App boot guard"
      pattern: "AnalyticsView"
---

<objective>
The ANALYTICS PILOT (PAGE-01) — the first real SPA page, and the one that establishes the
shared primitives every later page (signals/history/staged here, positions/history in
Phase 11) reuses: the `DataTable` (D-10), the Loading/Empty/ErrorPanel state trio
(D-10/D-11), and the `useUrlFilters` hook (D-02/D-05). It proves the full
API+SPA+auth+nginx stack end-to-end on a no-live-money page (SC#1). Consumes the
10-01 analytics widening.

Purpose: SC#1 + establish reusable primitives; the pilot de-risks the three dependent pages.
Output: 4 shared primitive files + AnalyticsView + route/sidebar wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md

<interfaces>
<!-- Inherited Phase-9 conventions + the 10-01 API shape. Read before building. -->
frontend/src/routes/ProbeView.tsx — the canonical useQuery+api() page; server-state/
  form-state isolation (lines 38-46). THROWAWAY — leave the index route as ProbeView for now
  (Overview is Phase 11); add AnalyticsView at its own path /analytics.
frontend/src/lib/http.ts — api(path, init) → Promise<unknown> (throws HttpError(status, body)
  on non-2xx); HttpError has .status and .body ({error:{code,message}} envelope). GET needs
  NO CSRF (http.ts attaches X-CSRF-Token on mutating methods only).
frontend/src/lib/queryClient.ts — inherited defaults: placeholderData=keepPreviousData,
  refetchIntervalInBackground:false, staleTime:1000, retry:false, global onAuthError (401 → /app/login).
frontend/src/components/shell/Sidebar.tsx — FUTURE_LINKS array (lines 21-28) currently renders
  disabled <span>; the live Overview <NavLink> block (lines 69-83) is the analog to copy
  (incl. the isActive cyan-accent class). Labels already match legacy: "Analytics".
frontend/src/routes/router.tsx — createBrowserRouter, basename /app; children under path "/"
  (the <App/> boot guard). Add route children WITHOUT the /app prefix.
frontend/src/lib/utils.ts — cn() classname helper.
shadcn ui present: button, input, label, card, sonner. tabs/select/skeleton/badge NOT yet added.
10-01 analytics payload (PAGE-01): {total_trades,wins,losses,win_rate,profit_factor,
  total_profit(+_display),gross_profit(+_display),gross_loss(+_display),
  by_source:[{source_name,total_trades,wins,losses,win_rate,profit_factor,net_pnl(+_display),
  best_trade(+_display),worst_trade(+_display)}], extremes:{best_trade(+_display),
  worst_trade(+_display)}, avg_stages:float|null, sources:[str]}.
Legacy parity (RESEARCH §1): KPI cards Total Trades(+W/L sub), Win Rate(green≥50/red),
  Profit Factor(green>1.0/red), Net P&L(green>0/red); P&L panel Gross Profit/Gross Loss/
  Best-Worst; Avg-Stages card ONLY when avg_stages truthy. By-source table columns in order:
  Source | Trades | W/L | Win Rate | PF | Net P&L | Best/Worst; row clickable → ?source=<name>;
  active source row bg-primary/10. Range tabs 7d/30d/90d/all.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared primitives — DataTable, Loading/Empty/ErrorPanel, useUrlFilters</name>
  <files>frontend/src/components/data/DataTable.tsx, frontend/src/components/state/Loading.tsx, frontend/src/components/state/Empty.tsx, frontend/src/components/state/ErrorPanel.tsx, frontend/src/lib/useUrlFilters.ts</files>
  <read_first>
    - frontend/src/components/shell/Sidebar.tsx + AppShell.tsx (Tailwind token conventions: bg-card, border, text-muted-foreground, hover:bg-muted/30, font-mono; lucide-react icon import)
    - frontend/src/lib/http.ts (HttpError.status/.body shape for ErrorPanel message)
    - frontend/src/lib/utils.ts (cn helper)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§DataTable 302-315, §state trio 318-332, §useUrlFilters 334-357 — copy the hook verbatim)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md (§"Code Examples" DataTable 372-407; Pattern 1 useUrlFilters 263-280)
  </read_first>
  <action>
    DATATABLE: implement the column-driven hand-rolled table from RESEARCH §"Code Examples"
    (lines 372-407) — `Column<Row>{header, cell:(row)=>ReactNode, align?, mono?, sign?}` and
    `DataTable<Row>({columns, rows})`. The `cell` returns the server `_display` string (the
    caller passes it; the table NEVER formats). Sticky header `sticky top-0`, right-align +
    `font-mono` for numerics, color-by-sign green/red via the optional `sign` accessor. Use the
    Sidebar/AppShell tokens. NO @tanstack/react-table (not installed). NO toFixed/Intl in the table.
    STATE TRIO: `Loading.tsx` = skeleton rows (use the shadcn `skeleton` if added in Task 2, else
    plain `animate-pulse` divs with border tokens); `Empty.tsx` = lucide icon + copy panel;
    `ErrorPanel.tsx` (D-11) = inline panel (NOT a sonner toast) with the message read from
    `HttpError.body` (`{error:{code,message}}`, fall back to a generic string) and a Retry button
    calling an `onRetry` prop. 401 never reaches ErrorPanel (inherited global onAuthError redirects first).
    USEURLFILTERS: copy the PATTERNS.md §useUrlFilters hook verbatim (lines 341-356) —
    react-router 7 `useSearchParams`, generic over a `Record<string,string>` key set, `replace:true`
    on filter edits and `push` on explicit navigation (by-source row click).
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 (type-check + bundle)
    - `grep -rn "toFixed\|Intl.NumberFormat" frontend/src/components/data` returns NO matches
    - `grep -q "useSearchParams" frontend/src/lib/useUrlFilters.ts`
    - `grep -q "onRetry" frontend/src/components/state/ErrorPanel.tsx` (Retry wired) and ErrorPanel imports no `toast` from sonner
  </acceptance_criteria>
  <done>Five reusable primitive files build clean; Phase 11 will inherit them.</done>
</task>

<task type="auto">
  <name>Task 2: Build AnalyticsView (KPIs + range tabs + by-source DataTable + states)</name>
  <files>frontend/src/routes/AnalyticsView.tsx</files>
  <read_first>
    - frontend/src/routes/ProbeView.tsx (useQuery+api pattern; server/form-state isolation)
    - frontend/src/components/data/DataTable.tsx + state/* + lib/useUrlFilters.ts (the Task-1 primitives)
    - frontend/src/components/ui/* (button/card available; add tabs/select/badge/skeleton via shadcn CLI if needed)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§route/page 271-298 — polling rules: analytics adds NO refetchInterval)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md (§1 legacy KPI/column parity 156-160; Pitfall 3 avg_stages 354-357)
  </read_first>
  <action>
    Add tabs/select/badge/skeleton shadcn components first if used:
    `cd frontend && npx shadcn@latest add tabs select badge skeleton` (per-page, keep lean — only
    add what AnalyticsView actually renders). Build `AnalyticsView` using `useUrlFilters<{range,source}>`
    (keys ["range","source"]); the query is
    `useQuery({ queryKey:["analytics", filters], queryFn:()=>api(\`/api/v2/analytics?range=${...}&source=${...}\`) })`
    with NO refetchInterval (D-03) — map the empty `range` to all-time and empty `source` to no filter
    (default load). Render: range tabs (7d=7/30d=30/90d=90/all="") writing `?range=` via setFilter
    (replace); the four KPI cards (Total Trades + W/L sub, Win Rate green≥50/red, Profit Factor
    green>1.0/red, Net P&L green>0/red — format win_rate/PF CLIENT-SIDE as they are raw ratios per D-14;
    render Net P&L from `total_profit_display`); the P&L panel (gross_profit_display/gross_loss_display/
    extremes best_trade_display–worst_trade_display); the Avg-Stages card rendered ONLY when
    `data.avg_stages` is truthy (Pitfall 3); the Performance-by-Source `DataTable` with columns in legacy
    order (Source | Trades | W/L | Win Rate | PF | Net P&L | Best/Worst) where Net P&L cell uses
    `net_pnl_display` with `sign:(r)=>r.net_pnl` color-by-sign and each row onClick calls
    `setFilter({source: r.source_name},{push:true})` (active row gets bg-primary/10). Wire the query
    states: `isPending`→`<Loading/>`, `isError`→`<ErrorPanel onRetry={refetch}/>`,
    `by_source.length===0`→`<Empty/>`, else render. Add a manual Refresh button (D-04) calling `refetch()`.
    NEVER toFixed/Intl on money/price — render the `_display` strings (win_rate/PF percent formatting is
    a ratio, allowed client-side per D-14).
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot/frontend && npm run build && grep -rn "toFixed\|Intl.NumberFormat" frontend/src/routes/AnalyticsView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0
    - `grep -rn "toFixed\|Intl.NumberFormat" frontend/src/routes/AnalyticsView.tsx` returns NO matches on money/price cells (win_rate/PF ratio formatting allowed — verify only money uses _display)
    - `grep -q "useUrlFilters" frontend/src/routes/AnalyticsView.tsx` and `grep -q "api/v2/analytics" frontend/src/routes/AnalyticsView.tsx`
    - `grep -q "avg_stages" frontend/src/routes/AnalyticsView.tsx` AND the card is guarded by a truthy check (Pitfall 3)
    - `grep -q "refetchInterval" frontend/src/routes/AnalyticsView.tsx` returns NO match (D-03 — no background poll)
  </acceptance_criteria>
  <done>AnalyticsView renders full legacy parity with URL filters, states, and Pitfall-5-safe numbers.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the analytics route + enable the Sidebar link</name>
  <files>frontend/src/routes/router.tsx, frontend/src/components/shell/Sidebar.tsx</files>
  <read_first>
    - frontend/src/routes/router.tsx (children under path "/", lines 29-37)
    - frontend/src/components/shell/Sidebar.tsx (FUTURE_LINKS 21-28; live Overview NavLink 69-83 analog)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§wire-in 386-397)
  </read_first>
  <action>
    ROUTER: add a route child `{ path: "analytics", element: <AnalyticsView/> }` under the existing
    `path: "/"` (App boot guard) children array (alongside the index ProbeView). Import AnalyticsView.
    Leave the index as ProbeView (Open Question 2 — Overview is Phase 11). Paths are written WITHOUT
    the /app prefix (basename adds it).
    SIDEBAR: convert ONLY the "Analytics" entry from the disabled-span treatment to an active
    `<NavLink to="/analytics">` by copying the live Overview NavLink block (lines 69-83) incl. the
    isActive cyan-accent class. Keep the other FUTURE_LINKS (Positions/Trade History/Signal Log/
    Pending Stages/Settings) as disabled spans — Plans 05/06 enable the read-only ones in their waves.
    Do this without breaking the existing map structure (e.g. split Analytics out of FUTURE_LINKS or
    special-case it in the map). Label stays "Analytics".
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot/frontend && npm run build && grep -q "AnalyticsView" frontend/src/routes/router.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0
    - `grep -q "path: \"analytics\"" frontend/src/routes/router.tsx` (route mounted)
    - `grep -q "to=\"/analytics\"" frontend/src/components/shell/Sidebar.tsx` (active NavLink)
    - The other future links (e.g. "Pending Stages") remain disabled spans (not yet NavLinks)
  </acceptance_criteria>
  <done>Analytics page reachable at /app/analytics from an active sidebar link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser SPA → /api/v2/analytics | Authenticated GET via the api() wrapper (session cookie, same-origin) |
| URL search params → query key | range/source filter values flow from the URL into the request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Spoofing | analytics fetch | mitigate | api() sends credentials same-origin httpOnly cookie; no token in localStorage (inherited SPA-03); 401 → global onAuthError redirect (inherited) |
| T-10-09 | Tampering | range/source URL params | accept | The SPA passes only the known keys (range/source); server coerces range via int() and parameterizes source (Plan 01 threat model) |
| T-10-10 | Information Disclosure | rendered numbers | mitigate | Money/price rendered from server _display strings only (Pitfall 5) — no client precision derivation |
| T-10-SC | Tampering | shadcn CLI source-gen | accept | `npx shadcn@latest add` copies vetted component SOURCE into src/components/ui (not an npm dependency); RESEARCH §Package Legitimacy confirms no new runtime dep |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0.
- `grep -rn "toFixed\|Intl.NumberFormat" frontend/src/components/data frontend/src/routes/AnalyticsView.tsx` returns no matches on money/price cells.
- Manual (golden-number, SC#5): open /app/analytics and legacy /analytics on the same DB; compare KPIs, by-source rows, extremes field-by-field; verify a by-source row click sets ?source= and re-queries; verify the Avg-Stages card appears only under a source filter.
</verification>

<success_criteria>
- PAGE-01 reaches full legacy parity; shared primitives built and reusable.
- URL-driven filters, inline error/empty/loading states, Pitfall-5-safe numbers.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-04-SUMMARY.md` when done
</output>
