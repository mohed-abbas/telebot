---
phase: 10-read-only-page-migration-analytics-pilot-signals-history-sta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/schemas.py
  - api/analytics.py
  - tests/test_analytics_contract.py
autonomous: true
requirements: [PAGE-01]
must_haves:
  truths:
    - "GET /api/v2/analytics returns by_source[] with per-source net_pnl_display, best/worst, win_rate, profit_factor (D-01 — full legacy parity widening)"
    - "GET /api/v2/analytics returns extremes (best/worst + _display), avg_stages, and sources list"
    - "avg_stages is non-null only when a source filter is active (mirrors legacy {% if avg_stages %})"
    - "money fields carry server-formatted _display twins; win_rate/profit_factor stay raw (D-14)"
  artifacts:
    - path: "api/schemas.py"
      provides: "AnalyticsBySource + AnalyticsExtremes models; widened Analytics model"
      contains: "class AnalyticsBySource"
    - path: "api/analytics.py"
      provides: "Route surfacing by_source/extremes/avg_stages/sources (sources no longer discarded)"
      contains: "by_source"
    - path: "tests/test_analytics_contract.py"
      provides: "Contract test for the widened analytics payload"
      contains: "def test_"
  key_links:
    - from: "api/analytics.py"
      to: "db.get_analytics_with_filters / db.get_analytics_sources"
      via: "capture sources + map by_source/extremes/avg_stages from already-returned dict"
      pattern: "sources\\s*=\\s*await db.get_analytics_sources"
    - from: "api/analytics.py"
      to: "api/formatting.py money_display"
      via: "money _display twins on net_pnl/best_trade/worst_trade"
      pattern: "money_display"
---

<objective>
Widen `/api/v2/analytics` to full legacy parity (read-only, D-01) — the FIRST backend
widening, gating the analytics SPA pilot (PAGE-01). The `Analytics` schema currently
exposes only the flat summary; the route already `await`s `db.get_analytics_sources()`
and DISCARDS it. Surface the per-source deep-dive (`by_source[]`), `extremes`,
`avg_stages`, and the `sources` list — all of which `db.get_analytics_with_filters()`
already computes. Zero query work; additive serialization only.

Purpose: SC#1 requires the per-source deep-dive — the page cannot be trimmed, the API gap must close.
Output: Widened `Analytics` schema + route + a Wave-0 contract test gating it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md

<interfaces>
<!-- Current state the executor must extend. Read the files; do not re-derive. -->
api/schemas.py — Analytics model at lines 146-157 (flat summary only); dual-value rule
  docstring at lines 6-9; Position dual-value analog at lines 39-51.
api/analytics.py — get_analytics route; the discard to fix is line 47
  (`await db.get_analytics_sources()` result thrown away).
db.get_analytics_with_filters(range_days, source_name) returns (RESEARCH §1, db.py:647-783):
  {summary{total_trades,wins,losses,win_rate(pct,ROUND .1),profit_factor(ROUND .2),
   gross_profit,gross_loss,net_pnl}, by_source[{source_name,total_trades,wins,losses,
   win_rate,profit_factor,net_pnl,best_trade,worst_trade}], avg_stages(None unless source set),
   extremes{best_trade,worst_trade}}.
db.get_analytics_sources() → list[str] (already called, currently discarded).
api/formatting.py — money_display(value) (formatting.py:35-37).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnalyticsBySource + AnalyticsExtremes models, widen Analytics</name>
  <files>api/schemas.py</files>
  <read_first>
    - api/schemas.py (lines 6-9 dual-value rule; 39-51 Position twin analog; 146-157 existing Analytics)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§"widen Analytics (D-01)" — exact field list at lines 70-96)
  </read_first>
  <behavior>
    - AnalyticsBySource has money _display twins on net_pnl/best_trade/worst_trade; win_rate/profit_factor are bare float|None (no _display, D-14).
    - AnalyticsExtremes has best_trade/worst_trade each with a _display twin.
    - Analytics gains by_source: list[AnalyticsBySource] = [], extremes: AnalyticsExtremes, avg_stages: float | None = None, sources: list[str] = [].
  </behavior>
  <action>
    Add two new BaseModel classes ABOVE the existing `Analytics` (schemas.py:146) following
    the PATTERNS.md §"widen Analytics" field list exactly: `AnalyticsBySource` with
    `source_name: str`, `total_trades/wins/losses: int`, `win_rate: float | None = None`
    and `profit_factor: float | None = None` (BARE — ratios per D-14, no `_display`),
    `net_pnl: float` + `net_pnl_display: str`, `best_trade: float | None = None` +
    `best_trade_display: str | None = None`, `worst_trade: float | None = None` +
    `worst_trade_display: str | None = None`; and `AnalyticsExtremes` with
    `best_trade`/`worst_trade` each `float | None = None` plus matching `_display: str | None = None`.
    Then add to `Analytics`: `by_source: list[AnalyticsBySource] = []`,
    `extremes: AnalyticsExtremes`, `avg_stages: float | None = None`, `sources: list[str] = []`.
    Keep all existing Analytics fields. Obey the schemas.py:6-9 rule — only money/price/volume/time
    get `_display`; ratios stay bare.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -c "import ast; ast.parse(open('api/schemas.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class AnalyticsBySource" api/schemas.py` == 1
    - `grep -c "class AnalyticsExtremes" api/schemas.py` == 1
    - `grep -E "by_source:\s*list\[AnalyticsBySource\]" api/schemas.py` matches
    - `grep -E "win_rate_display|profit_factor_display" api/schemas.py` returns NO matches (ratios stay raw, D-14)
  </acceptance_criteria>
  <done>The widened Analytics + two nested models parse and follow the dual-value rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface by_source/extremes/avg_stages/sources in the route</name>
  <files>api/analytics.py</files>
  <read_first>
    - api/analytics.py (whole file — the summary mapping at 49-65, the discard at 47)
    - api/history.py (the _enrich_trade per-row money/price formatting pattern, history.py:34-62)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§"widen get_analytics route (D-01)", lines 140-172)
  </read_first>
  <behavior>
    - Route captures `sources = await db.get_analytics_sources()` (no longer discarded) and returns it as `sources`.
    - Each by_source row maps to AnalyticsBySource with money_display() on net_pnl/best_trade/worst_trade twins (None-guard best/worst); win_rate/profit_factor passed through raw.
    - extremes maps to AnalyticsExtremes with money_display() twins (None-guard).
    - avg_stages passed through unchanged (None on all-source view — Pitfall 3).
  </behavior>
  <action>
    Change line 47 from discarding to `sources = await db.get_analytics_sources()`. Build
    `by_source` by iterating `data.get("by_source", [])`, constructing `AnalyticsBySource`
    per row: copy `source_name/total_trades/wins/losses/win_rate/profit_factor/net_pnl`,
    set `net_pnl_display=money_display(net_pnl)`, and for `best_trade`/`worst_trade` set the
    raw value plus `*_display=money_display(v) if v is not None else None`. Build `extremes`
    from `data.get("extremes", {})` as `AnalyticsExtremes` with the same None-guarded
    money_display twins. Pass `avg_stages=data.get("avg_stages")` straight through (may be
    None — do NOT default to 0; the SPA renders the Avg-Stages card only when truthy, Pitfall 3).
    Add the four new kwargs (`by_source`, `extremes`, `avg_stages`, `sources`) to the
    `Analytics(...)` return alongside the existing summary fields. Import
    `AnalyticsBySource, AnalyticsExtremes` from `api.schemas`.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -c "import ast; ast.parse(open('api/analytics.py').read())" && grep -q "sources = await db.get_analytics_sources" api/analytics.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sources = await db.get_analytics_sources" api/analytics.py` == 1 (no longer discarded)
    - `grep -c "by_source=" api/analytics.py` >= 1 and `grep -c "extremes=" api/analytics.py` >= 1
    - `grep -c "money_display" api/analytics.py` >= 4 (net_pnl + best/worst per-source + extremes)
    - No `:.2f`/`:.1f`/`toFixed` literal in api/analytics.py (formatting via money_display only)
  </acceptance_criteria>
  <done>GET /api/v2/analytics returns by_source/extremes/avg_stages/sources with correct _display twins.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 analytics contract test</name>
  <files>tests/test_analytics_contract.py</files>
  <read_first>
    - tests/test_api_contract.py (the `_login` helper + `session_client` fixture + `api_app` fixture pattern, lines 59-92)
    - api/formatting.py (money_display, to assert _display equals the formatter output)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-VALIDATION.md (PAGE-01 rows)
  </read_first>
  <action>
    Create `tests/test_analytics_contract.py` reusing the `api_app`/`session_client` fixtures
    (import or re-instantiate the same way `tests/test_api_contract.py` does — copy the
    `_login` + fixture pattern). Assert GET /api/v2/analytics 200 returns keys
    `by_source`, `extremes`, `avg_stages`, `sources`. Assert `by_source` is a list and, when
    non-empty, each row has `net_pnl_display` (str) and bare `win_rate`/`profit_factor`
    with NO `win_rate_display`/`profit_factor_display` keys (D-14). Assert each present
    `net_pnl_display` equals `money_display(row["net_pnl"])`. Assert `extremes` has the
    best/worst + _display twin shape. Assert the all-source default load (`?` no source)
    yields `avg_stages is None` (Pitfall 3), and document that a source-filtered call
    (`?source=<first source>` when sources non-empty) MAY return a non-null avg_stages.
    Use `-x` one-shot semantics; no watch flags.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && pytest tests/test_analytics_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_analytics_contract.py -x` exits 0
    - Test file asserts presence of by_source/extremes/avg_stages/sources keys
    - Test asserts net_pnl_display == money_display(net_pnl) for a sampled row
    - Test asserts avg_stages is None on the all-source default load
  </acceptance_criteria>
  <done>Contract test green; gates PAGE-01 backend parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v2/analytics | Authenticated GET; filter params (range/source) cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | GET /api/v2/analytics | mitigate | Route stays `Depends(require_user)` — session-gated (inherited Phase 8/9); widening adds fields only, no new auth surface |
| T-10-02 | Tampering (SQLi) | range/source filter params | accept | `range` is `int()`-coerced in `_parse_range`; `source_name` is parameterized asyncpg `$n` (verified db.py); no new query construction added |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (RESEARCH §Package Legitimacy: zero new deps this phase) |
</threat_model>

<verification>
- `pytest tests/test_analytics_contract.py -x` exits 0.
- `git diff --stat executor.py trade_manager.py db.py mt5_connector.py` shows ZERO changes (bot-core untouched).
- No `:.Nf` literal added to api/analytics.py (formatting via api/formatting.py only).
</verification>

<success_criteria>
- /api/v2/analytics is parity-complete: by_source deep-dive, extremes, avg_stages (conditional), sources.
- Contract test green. Bot core byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-01-SUMMARY.md` when done
</output>
