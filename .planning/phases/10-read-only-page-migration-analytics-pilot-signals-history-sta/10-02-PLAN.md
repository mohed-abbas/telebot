---
phase: 10-read-only-page-migration-analytics-pilot-signals-history-sta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/stages.py
  - tests/test_stages_contract.py
autonomous: true
requirements: [PAGE-04]
must_haves:
  truths:
    - "GET /api/v2/stages active rows carry started_at (ISO-8601 + UTC offset)"
    - "started_at is sourced from the RAW get_pending_stages().created_at, not the enriched dict that dropped it (Pitfall 4)"
    - "active rows surface the correct filled/total/distance_str (D-13 — not the legacy blank-cell fields)"
    - "existing band_low/band_high/current_price _display twins are preserved"
  artifacts:
    - path: "api/stages.py"
      provides: "_enrich_active widened with started_at; raw row zipped through"
      contains: "started_at"
    - path: "tests/test_stages_contract.py"
      provides: "Contract test asserting started_at present + ISO + survives enrichment chain"
      contains: "started_at"
  key_links:
    - from: "api/stages.py list_stages"
      to: "db.get_pending_stages() raw created_at"
      via: "zip raw_active with enriched dict so created_at survives to _enrich_active"
      pattern: "zip\\(active, raw_active\\)|zip\\(raw_active, active\\)"
    - from: "api/stages.py _enrich_active"
      to: "api/formatting.py ts_machine"
      via: "started_at = ts_machine(raw['created_at'])"
      pattern: "ts_machine"
---

<objective>
Enrich the `/api/v2/stages` ACTIVE payload with a machine start-timestamp (D-09) so the
SPA's client-side elapsed timer (D-06) has a server epoch to count from. This is a
read-only serialization widening (same category as D-01). The NON-TRIVIAL detail
(Pitfall 4): `dashboard._enrich_stage_for_ui` DROPS the raw `created_at` while computing
its `elapsed` string — the timestamp lives ONLY in the raw `get_pending_stages()` row, so
`_enrich_active` must receive the raw row, not just the enriched dict.

Purpose: PAGE-04 needs a server epoch; the legacy active payload has no machine timestamp.
Output: Widened `_enrich_active` + `list_stages` zip + a Wave-0 contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md

<interfaces>
<!-- Current state — read before editing. -->
api/stages.py — full file (62 lines). `_enrich_active(stage)` adds price _display twins
  (lines 28-36); `_enrich_resolved(row)` is the timestamp-twin ANALOG (lines 39-47:
  ts_machine/ts_display on created_at/filled_at). `list_stages` builds
  active = [dashboard._enrich_stage_for_ui(s, positions) for s in raw_active] (line 57)
  then [_enrich_active(s) for s in active] (line 60) — the enriched `s` has NO created_at.
dashboard._enrich_stage_for_ui (dashboard.py:567-581) output keys:
  account_name, symbol, direction, filled, total, band_low, band_high, current_price,
  distance_str, elapsed, status — NO created_at (it is consumed to make `elapsed` then dropped).
db.get_pending_stages() — SELECTs created_at (TIMESTAMPTZ) on every active row (verified db.py:1057-1079).
api/formatting.py — ts_machine(dt) (ISO-8601 + offset, formatting.py:45-47),
  ts_display(dt) (formatting.py:50-52). Both already imported at stages.py:23.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plumb raw created_at into _enrich_active as started_at</name>
  <files>api/stages.py</files>
  <read_first>
    - api/stages.py (whole file — _enrich_active 28-36, _enrich_resolved 39-47 analog, list_stages 50-62)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§"_enrich_active + started_at (D-09)", lines 211-254 — incl. the Pitfall 4 zip fix)
  </read_first>
  <behavior>
    - _enrich_active receives BOTH the enriched dict and the raw stage row.
    - When raw['created_at'] is a datetime, output gains started_at = ts_machine(raw['created_at']) and started_at_display = ts_display(raw['created_at']).
    - Existing band_low/band_high/current_price _display twins are preserved unchanged.
    - The correct filled/total/distance_str keys from the enriched dict pass through (D-13 — these already exist; no blank-cell legacy field names introduced).
  </behavior>
  <action>
    Change `_enrich_active(stage: dict)` to `_enrich_active(stage: dict, raw: dict)`. Keep
    the existing band/price _display loop. Add: pull `created_at = raw.get("created_at")`;
    if it is a `datetime`, set `out["started_at"] = ts_machine(created_at)` and
    `out["started_at_display"] = ts_display(created_at)` (mirror the `_enrich_resolved`
    ts_machine/ts_display shape at stages.py:39-47). In `list_stages`, change the active
    comprehension to zip the enriched dicts with their raw rows so created_at survives —
    `"active": [_enrich_active(enriched, raw) for enriched, raw in zip(active, raw_active)]`
    (PATTERNS.md lines 237-242; raw_active and active are positionally aligned because
    `active` is built by mapping over `raw_active`). Do NOT touch `_enrich_resolved` or the
    `resolved` list. Do NOT add the legacy blank-cell keys (filled_count/total_stages/
    distance_to_band, D-13) — the enriched dict's real keys filled/total/distance_str already flow.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -c "import ast; ast.parse(open('api/stages.py').read())" && grep -Eq "zip\(active, raw_active\)|zip\(raw_active, active\)" api/stages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "started_at" api/stages.py` >= 2 (started_at + started_at_display)
    - `grep -Eq "zip\(active, raw_active\)|zip\(raw_active, active\)" api/stages.py` matches (raw row plumbed through)
    - `grep -c "ts_machine" api/stages.py` >= 2 (resolved + new active use)
    - `grep -E "filled_count|total_stages|distance_to_band" api/stages.py` returns NO matches (D-13 — legacy blank-cell fields not introduced)
  </acceptance_criteria>
  <done>active rows serialize started_at (ISO+offset) from the raw created_at; band/price twins intact.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 stages contract test (started_at survives the chain)</name>
  <files>tests/test_stages_contract.py</files>
  <read_first>
    - tests/test_api_contract.py (api_app/session_client fixtures + _login, lines 59-133 — note the existing stages key assertions)
    - api/stages.py (the widened route)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-VALIDATION.md (PAGE-04 rows — started_at ISO + Pitfall 4 + correct filled/total)
  </read_first>
  <action>
    Create `tests/test_stages_contract.py` reusing the `api_app`/`session_client`/`_login`
    fixture pattern from `tests/test_api_contract.py`. GET /api/v2/stages → assert 200 and
    keys `active`, `resolved`. For each active row (when the live DB has any), assert
    `started_at` is present and is an ISO-8601 string carrying a UTC offset (regex e.g.
    `r"T.*([+-]\d{2}:\d{2}|Z)$"` — match how ts_machine formats; verify against
    api/formatting.py ts_machine output shape). Assert the active row also has correct
    `filled`/`total` keys (D-13) and does NOT have the legacy `filled_count`/`total_stages`
    keys. CRITICAL Pitfall-4 guard: assert that when `elapsed` is populated on a row,
    `started_at` is ALSO populated (they derive from the same created_at — a null started_at
    beside a populated elapsed proves the raw row was lost). If the DB has no active stages,
    the test must still pass (assert the keys/shape on resolved + skip-or-pass the active
    loop). One-shot `-x`, no watch flags.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && pytest tests/test_stages_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_stages_contract.py -x` exits 0
    - Test asserts active[].started_at is ISO-8601 with offset (when active rows exist)
    - Test asserts the Pitfall-4 invariant: populated elapsed ⇒ populated started_at
    - Test asserts correct filled/total keys present and legacy filled_count/total_stages absent
  </acceptance_criteria>
  <done>Contract test green; PAGE-04 server epoch + correct stage counts proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v2/stages | Authenticated GET; no input params (full active+resolved snapshot) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | GET /api/v2/stages | mitigate | Route stays `Depends(require_user)`; widening adds a timestamp field only |
| T-10-04 | Tampering | stages read query | accept | No new query construction; `get_pending_stages()` unchanged; serialization-only widening |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (zero new deps this phase) |
</threat_model>

<verification>
- `pytest tests/test_stages_contract.py -x` exits 0.
- `git diff --stat executor.py trade_manager.py db.py mt5_connector.py dashboard.py` shows ZERO changes (bot-core + legacy dashboard untouched — only api/stages.py edited).
</verification>

<success_criteria>
- /api/v2/stages active payload carries a server ISO started_at the SPA timer can count from.
- Pitfall 4 avoided (timestamp sourced from raw row). Bot core unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-02-SUMMARY.md` when done
</output>
