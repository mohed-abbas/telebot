---
phase: 10-read-only-page-migration-analytics-pilot-signals-history-sta
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - api/schemas.py
  - api/signals.py
  - api/history.py
  - tests/test_signals_contract.py
  - tests/test_history_contract.py
autonomous: true
requirements: [PAGE-02, PAGE-03]
must_haves:
  truths:
    - "GET /api/v2/signals surfaces entry_zone_low/high (+_display), sl/tp (+_display), details, source_name"
    - "GET /api/v2/history surfaces sl/tp (+_display), status, source_name (full legacy column parity, D-12)"
    - "GET /api/v2/history round-trips all 5 filter params (account/source/symbol/from_date/to_date) with AND logic"
    - "price fields carry server-formatted _display twins via price_display; details/status/source_name stay bare"
  artifacts:
    - path: "api/schemas.py"
      provides: "Widened Signal + HistoryTrade models"
      contains: "entry_zone_low"
    - path: "api/signals.py"
      provides: "_enrich_signal surfacing zone/sl/tp/details/source_name"
      contains: "price_display"
    - path: "api/history.py"
      provides: "_enrich_trade surfacing sl/tp/status/source_name"
      contains: "source_name"
    - path: "tests/test_signals_contract.py"
      provides: "Contract test for widened signal fields"
    - path: "tests/test_history_contract.py"
      provides: "Contract test for widened history columns + 5-param round-trip"
  key_links:
    - from: "api/signals.py _enrich_signal"
      to: "api/formatting.py price_display"
      via: "price _display twins on zone/sl/tp (None-guarded)"
      pattern: "price_display"
    - from: "api/history.py _enrich_trade"
      to: "get_filtered_trades row keys sl/tp/status/source_name"
      via: "map already-returned columns onto the widened HistoryTrade"
      pattern: "source_name"
---

<objective>
Close the two latent Phase-8 schema parity gaps (D-12, RESEARCH §3/§4): the shipped
`Signal` schema drops `entry_zone_low/high`, `sl`, `tp`, `details`, `source_name`; the
`HistoryTrade` schema drops `sl`, `tp`, `status`, `source_name`. The underlying db queries
(`get_recent_signals` `SELECT *`; `get_filtered_trades` selecting `t.sl, t.tp, t.status,
COALESCE(s.source_name,'Unknown')`) already return everything. Operator-confirmed FULL
legacy parity (D-12) — widen both read schemas + their route mappers. Read-only,
serialization-only, same category as D-01/D-09.

Purpose: PAGE-02 and PAGE-03 cannot match their legacy templates without these fields.
Output: Widened Signal + HistoryTrade schemas + mappers + two contract tests.
Depends on 10-01 because both edit api/schemas.py (serialize after the Analytics widening lands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md
@.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-RESEARCH.md

<interfaces>
<!-- Current state — read before editing. -->
api/schemas.py — Signal at lines 113-121 (drops zone/sl/tp/details/source_name);
  HistoryTrade at lines 83-99 (has open_price/_display + opened_at/_display twin shapes;
  drops sl/tp/status/source_name); dual-value rule docstring 6-9; Position price-twin 39-51.
api/signals.py — _enrich_signal mapper (signals.py:22-36); imports ts_display, ts_machine
  at signals.py:16 (add price_display). db.get_recent_signals(100) does SELECT * → row carries
  entry_zone_low/high, sl, tp, details, source_name.
api/history.py — _enrich_trade (history.py:34-62) — canonical per-row money+price+timestamp
  twin mapper (price_display/money_display/ts pair). GET /history params
  account/source/symbol/from_date/to_date already wired; get_filtered_trades (db.py:500-566)
  uses parameterized asyncpg $n and returns t.sl, t.tp, t.status, COALESCE(s.source_name,'Unknown').
api/formatting.py — price_display(symbol, value) (formatting.py:29-32).
Legacy parity columns (RESEARCH §3/§4):
  signals.html: Time,Type,Symbol,Direction,Zone(low–high),SL,TP,Action,Details(details or raw_text[:80]).
  history_table.html: Time,Account,Source,Symbol,Direction,Entry,SL,TP,Lots,Status,P&L.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen Signal + HistoryTrade schemas and their mappers</name>
  <files>api/schemas.py, api/signals.py, api/history.py</files>
  <read_first>
    - api/schemas.py (Signal 113-121; HistoryTrade 83-99; Position price-twin 39-51; rule 6-9)
    - api/signals.py (full file — _enrich_signal 22-36, import line 16)
    - api/history.py (full file — _enrich_trade 34-62 price/money/ts pattern)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-PATTERNS.md (§"widen Signal (D-12)" 100-118, §"widen HistoryTrade (D-12)" 122-136, §"widen _enrich_signal" 175-188, §"widen _enrich_trade" 191-207)
  </read_first>
  <behavior>
    - Signal gains entry_zone_low/high (+_display), sl/tp (+_display), details (bare str|None), source_name (bare str|None).
    - HistoryTrade gains sl/tp (+_display), status (bare str|None), source_name (bare str|None).
    - _enrich_signal sets price_display(symbol, v) twins for zone/sl/tp (None → None); passes details/source_name bare.
    - _enrich_trade sets price_display twins for sl/tp (None-guarded); maps status from row, source_name from row defaulting "Unknown".
  </behavior>
  <action>
    SCHEMAS: To `Signal` (schemas.py:113-121) add `entry_zone_low/entry_zone_high:
    float | None = None` each with a `*_display: str | None = None` twin, `sl`/`tp` same
    price-twin shape, `details: str | None = None` (BARE), `source_name: str | None = None`
    (BARE) — per PATTERNS.md §"widen Signal". To `HistoryTrade` (schemas.py:83-99) add `sl`/`tp`
    `float | None = None` + `*_display: str | None = None` twins, `status: str | None = None`
    (BARE), `source_name: str | None = None` (BARE) — per PATTERNS.md §"widen HistoryTrade".
    Keep all existing fields. Obey the schemas.py:6-9 rule (price/money/time → _display; strings bare).
    SIGNALS ROUTE: in api/signals.py add `price_display` to the formatting import (line 16). In
    `_enrich_signal` (22-36) set `entry_zone_low/high`, `sl`, `tp` from the row plus
    `*_display = price_display(symbol, v) if v is not None else None`; pass `details` and
    `source_name` through bare (`row.get(...)`).
    HISTORY ROUTE: in api/history.py `_enrich_trade` (34-62) mirror its existing price-twin
    usage: set `sl`/`tp` from the row with `sl_display/tp_display = price_display(symbol, v)
    if v is not None else None`; set `status=row.get("status")` and
    `source_name=row.get("source_name") or "Unknown"` (bare).
    NO db query changes anywhere.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && python -c "import ast; ast.parse(open('api/schemas.py').read()); ast.parse(open('api/signals.py').read()); ast.parse(open('api/history.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "entry_zone_low:" api/schemas.py` matches AND `grep -E "details:\s*str \| None" api/schemas.py` matches
    - `grep -E "status:\s*str \| None|source_name:\s*str \| None" api/schemas.py` matches in HistoryTrade region
    - `grep -c "price_display" api/signals.py` >= 3 (zone/sl/tp)
    - `grep -q "source_name" api/history.py` AND `grep -q "status" api/history.py`
    - `grep -E "entry_zone_low_display|sl_display|tp_display" api/schemas.py` matches (price twins declared); NO `details_display`/`status_display`/`source_name_display` anywhere (strings stay bare)
  </acceptance_criteria>
  <done>Both schemas + mappers reach full legacy column parity with correct twin discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 signals contract test</name>
  <files>tests/test_signals_contract.py</files>
  <read_first>
    - tests/test_api_contract.py (fixtures + _login, 59-92)
    - api/formatting.py (price_display, for the _display equality assertion)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-VALIDATION.md (PAGE-02 row)
  </read_first>
  <action>
    Create `tests/test_signals_contract.py` reusing the api_app/session_client/_login pattern.
    GET /api/v2/signals → 200, list. When non-empty, assert each row exposes keys
    `entry_zone_low`, `entry_zone_high`, `sl`, `tp`, `details`, `source_name` and the price
    `_display` twins (`entry_zone_low_display`, `sl_display`, `tp_display`). Assert NO
    `details_display`/`source_name_display` keys exist (strings stay bare). For a row where
    `sl` is non-null, assert `sl_display == price_display(row_symbol, row_sl)`. XSS note
    (V5): the test only inspects JSON — confirm `details`/`raw_text` are returned as plain
    strings (no HTML escaping pre-applied server-side; the SPA renders them as React text
    children). One-shot `-x`. Tolerate an empty signals table (pass on shape of the response).
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && pytest tests/test_signals_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_signals_contract.py -x` exits 0
    - Test asserts presence of entry_zone_low/high, sl, tp, details, source_name + price _display twins
    - Test asserts sl_display == price_display(symbol, sl) for a sampled non-null row
    - Test asserts details/source_name have no _display twin
  </acceptance_criteria>
  <done>Signals contract green; PAGE-02 parity fields proven present.</done>
</task>

<task type="auto">
  <name>Task 3: History contract test (widened columns + 5-param round-trip)</name>
  <files>tests/test_history_contract.py</files>
  <read_first>
    - tests/test_api_contract.py (fixtures + _login; existing /history/filter-options assertion at line 127)
    - api/history.py (the widened mapper + the 5 query params)
    - .planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-VALIDATION.md (PAGE-03 rows — 5-param AND logic + widened columns)
  </read_first>
  <action>
    Create `tests/test_history_contract.py` reusing the fixture pattern. GET /api/v2/history
    → 200, list; when non-empty assert each row exposes `sl`, `tp`, `sl_display`,
    `tp_display`, `status`, `source_name` (the D-12 widened columns) and NO
    `status_display`/`source_name_display`. Assert price `_display` equals
    `price_display(symbol, value)` for a sampled non-null sl/tp. Round-trip the 5 filter
    params: GET /api/v2/history/filter-options to obtain valid `accounts`/`symbols`/`sources`;
    then GET /api/v2/history with `?account=<a>&symbol=<sym>` (and a `from_date`/`to_date`
    pair) and assert 200 and that every returned row matches the filters (AND logic) — i.e.
    `row["account"] == a` and `row["symbol"] == sym` for all rows; if filter-options are
    empty (empty DB) assert the unfiltered call returns a list and skip the AND assertions.
    One-shot `-x`.
  </action>
  <verify>
    <automated>cd /Users/murx/Developer/personal/telebot && pytest tests/test_history_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_history_contract.py -x` exits 0
    - Test asserts sl/tp/sl_display/tp_display/status/source_name present on history rows
    - Test round-trips account+symbol filters and asserts AND-logic on returned rows
    - Test asserts status/source_name have no _display twin
  </acceptance_criteria>
  <done>History contract green; PAGE-03 column parity + filter round-trip proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v2/signals | Authenticated GET; no params |
| browser → /api/v2/history | Authenticated GET; 5 string filter params cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (SQLi) | history filter params | accept | All 5 params are parameterized asyncpg `$n` (verified db.py:526-568); no new query construction in this plan |
| T-10-06 | Tampering/Info (reflected XSS) | signals details/raw_text | mitigate | Returned as plain JSON strings; the SPA (Plan 04/05) renders them as React text children, NEVER dangerouslySetInnerHTML — asserted in the consuming frontend plan's grep guard |
| T-10-07 | Information Disclosure | signals/history reads | mitigate | Both routes stay `Depends(require_user)` (session-gated, inherited) |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (zero new deps this phase) |
</threat_model>

<verification>
- `pytest tests/test_signals_contract.py tests/test_history_contract.py -x` exits 0.
- `git diff --stat executor.py trade_manager.py db.py mt5_connector.py` shows ZERO changes.
- No `:.Nf` literal added to api/signals.py or api/history.py (formatting via price_display only).
</verification>

<success_criteria>
- /api/v2/signals and /api/v2/history reach full legacy column parity (D-12).
- Both contract tests green; filter AND-logic round-trips. Bot core unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-page-migration-analytics-pilot-signals-history-sta/10-03-SUMMARY.md` when done
</output>
