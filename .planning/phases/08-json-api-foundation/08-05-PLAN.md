---
phase: 08-json-api-foundation
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - api/settings.py
  - tests/test_api_settings.py
autonomous: true
requirements: [API-02]
must_haves:
  truths:
    - "GET /api/v2/settings/{account} returns effective settings + audit timeline as JSON"
    - "POST /api/v2/settings/{account}/validate returns {valid, errors, diff, dry_run_text} as JSON, not an HTML modal"
    - "POST /api/v2/settings/{account} (confirm) and /revert return a structured JSON envelope"
    - "Settings mutations are CSRF-guarded (403 without a valid X-CSRF-Token)"
    - "Server-side hard caps are enforced verbatim via validate_settings_form; bot core shows zero git diff"
  artifacts:
    - path: "api/settings.py"
      provides: "GET /settings/{account}, POST validate/confirm/revert as JSON"
      contains: "validate_settings_form"
    - path: "tests/test_api_settings.py"
      provides: "validate returns JSON diff; invalid -> errors; confirm/revert envelopes; CSRF gate"
  key_links:
    - from: "api/settings.py"
      to: "validate_settings_form / SettingsStore"
      via: "accessor (require_settings_store); JSON body -> dict shape validate expects"
      pattern: "validate_settings_form"
    - from: "api/settings.py mutations"
      to: "verify_csrf_token"
      via: "Depends on validate/confirm/revert POSTs"
      pattern: "Depends\\(verify_csrf_token\\)"
---

<objective>
Port the settings surface into `/api/v2` as JSON (API-02): read effective settings + audit, and validate/confirm/revert mutations returning structured JSON instead of HTML modals/partials. The validation core (server-side hard caps via `validate_settings_form`) is ported verbatim; only the response shape and the request body (JSON instead of form-dict) change. Plus the settings contract test.

Purpose: Settings is one of the two HIGH-complexity pages (Phase 11). Shipping its JSON contract now — including the two-step validate->confirm diff flow and revert — gives Phase 11 a clean, curl-testable surface. The hard caps stay server-side; the SPA's zod mirror (Phase 11) is defense-in-depth on top, never a replacement.

Output: Four settings routes returning JSON (read + validate/confirm/revert) and a green settings test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-json-api-foundation/08-CONTEXT.md
@.planning/phases/08-json-api-foundation/08-RESEARCH.md
@.planning/phases/08-json-api-foundation/08-PATTERNS.md
@.planning/phases/08-json-api-foundation/08-01-SUMMARY.md
@.planning/phases/08-json-api-foundation/08-02-SUMMARY.md

<interfaces>
From api/deps.py: require_user, verify_csrf_token, require_settings_store (Plan 01).
From api/schemas.py: SettingsValidateIn, SettingsConfirmIn, SettingsRevertIn, SettingsView (effective + audit), MutationResult (Plan 01).
From api/formatting.py: money_display / ts_display for any money/timestamp fields in the settings view + audit timeline.

Existing logic to PORT VERBATIM (call-only; never edit these files):
  store = require_settings_store()                       (503 if None: dashboard.py:749-750)
  current = store.effective(account_name)                dashboard.py:751-755 (404 on KeyError: unknown account)
  validate_settings_form(form_dict, max_lot_size=...)    dashboard.py:632, called dashboard.py:759-760 -> {valid, errors, diff, dry_run_text}
  SettingsStore.update(...) per changed field            dashboard.py:811-869 (confirm)
  inverted-diff persist                                  dashboard.py:872-914 (revert)
  db.get_settings_audit(name, 50)                        db.py:937 (audit timeline)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api/settings.py — GET settings + validate/confirm/revert as JSON</name>
  <files>api/settings.py</files>
  <read_first>
    - dashboard.py:632 (validate_settings_form signature + returns {valid,errors,diff,dry_run_text}), dashboard.py:744-808 (settings_validate: form=dict(await request.form()) then validate; 503/404 guards at :749-755), dashboard.py:811-869 (settings_confirm: SettingsStore.update per changed field), dashboard.py:872-914 (settings_revert: inverted-diff persist)
    - dashboard.py:686 (_get_settings_store), db.py:937 (get_settings_audit)
    - api/schemas.py (SettingsValidateIn/ConfirmIn/RevertIn, SettingsView), api/deps.py (require_settings_store, verify_csrf_token)
    - .planning/phases/08-json-api-foundation/08-PATTERNS.md → "api/settings.py" (port validation core; swap form-dict -> Pydantic body; reuse 503/404 guards verbatim)
  </read_first>
  <action>
    Create `api/settings.py`. `GET /settings/{account}` (gated `require_user`): `store = require_settings_store()` (503 if None); `current = store.effective(account)` wrapped in try/except `KeyError` -> `HTTPException(404, f"Unknown account: {account}")`; `audit = await db.get_settings_audit(account, 50)`; return a `SettingsView` JSON ({effective fields + audit timeline}), routing any money/timestamp fields through api/formatting.py for `_display` twins.

    `POST /settings/{account}/validate` (gated `require_user` + `verify_csrf_token`): accept a `SettingsValidateIn` JSON body; convert it to the dict shape `validate_settings_form` expects (OQ2 recommendation: JSON in, not form) and call `validate_settings_form(form_dict, max_lot_size=...)` exactly as dashboard.py:759-760; return `{valid, errors, diff, dry_run_text}` as JSON (NOT the HTML modal / 422 partial). Reuse the 503/404 guards verbatim from dashboard.py:749-755.

    `POST /settings/{account}` confirm (gated `require_user` + `verify_csrf_token`): port the per-changed-field `SettingsStore.update(...)` loop from dashboard.py:811-869; return a JSON `MutationResult` envelope. `POST /settings/{account}/revert` (gated `require_user` + `verify_csrf_token`): port the inverted-diff persist from dashboard.py:872-914; return a JSON envelope.

    Add handlers to the existing `router` in api/settings.py (already included by api/router.py — do NOT edit api/router.py).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_api_settings.py -k "get or validate" -x</automated>
  </verify>
  <acceptance_criteria>
    - `api/settings.py` calls `validate_settings_form` (ported, not re-implemented) and `SettingsStore` via `require_settings_store()`; contains `Depends(verify_csrf_token)` on validate/confirm/revert; contains NO `dict(await request.form())` HTML-modal response path.
    - `GET /api/v2/settings/{account}` returns 404 for an unknown account and a JSON `SettingsView` (effective + audit) for a known one.
    - validate returns `{valid, errors, diff, dry_run_text}` JSON, never an HTML fragment.
    - `git diff --exit-code db.py mt5_connector.py executor.py trade_manager.py` exits 0.
  </acceptance_criteria>
  <done>Settings read + validate/confirm/revert routes return CSRF-guarded JSON, porting validate_settings_form + SettingsStore verbatim, no HTML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_api_settings.py — validate/confirm/revert contract</name>
  <files>tests/test_api_settings.py</files>
  <behavior>
    - GET /settings/{known} -> 200 JSON with effective fields + audit list; GET /settings/{unknown} -> 404.
    - POST validate with a valid change -> {valid: true, diff non-empty, dry_run_text present}.
    - POST validate breaching a server hard cap -> {valid: false, errors non-empty} (HTTP 200 JSON, not an HTML 422 modal).
    - POST confirm -> JSON envelope; the change is persisted (reflected on a subsequent GET) and an audit row is added.
    - POST revert -> JSON envelope inverting the prior change.
    - validate/confirm/revert without X-CSRF-Token -> 403.
  </behavior>
  <read_first>
    - tests/test_login_flow.py:64-88 (TestClient round-trip), tests/conftest.py (api_app + authed_client + seeded_account fixtures; db_pool truncates settings_audit/account_settings)
    - dashboard.py:632 (the hard-cap rules validate_settings_form enforces — to pick a cap-breaching value)
    - .planning/phases/08-json-api-foundation/08-VALIDATION.md → API-02 mutation-returns-JSON row
  </read_first>
  <action>
    Create `tests/test_api_settings.py` against dev Postgres using `api_app` + a seeded account (reuse the `seeded_account`/`seeded_settings` conftest fixtures; settings tables are truncated by clean_tables). `test_get_settings`: GET a known account -> 200 JSON with effective fields + an `audit` list; GET an unknown account -> 404. `test_validate_valid_change`: POST validate with an in-range change -> `valid: true`, non-empty `diff`, `dry_run_text` present. `test_validate_breaches_cap`: POST validate with a value above the server hard cap (e.g. risk_value beyond the per-account/mode cap) -> `valid: false` + non-empty `errors`, as JSON (assert content-type JSON, not HTML). `test_confirm_persists`: POST confirm a change -> JSON envelope; a subsequent GET reflects it and `db.get_settings_audit` has a new row. `test_revert`: POST revert -> JSON envelope. `test_settings_mutation_requires_csrf`: POST validate/confirm without `X-CSRF-Token` -> 403.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_api_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_api_settings.py` asserts validate returns JSON `{valid, errors, diff, dry_run_text}` (content-type JSON, never HTML).
    - A cap-breaching validate yields `valid: false` + non-empty `errors`.
    - A confirm is persisted (reflected on a later GET) and adds an audit row.
    - A settings mutation without `X-CSRF-Token` returns 403.
    - `pytest tests/test_api_settings.py -x` is green (skips cleanly if dev Postgres absent).
  </acceptance_criteria>
  <done>The settings suite proves JSON validate/confirm/revert, server-cap enforcement, persistence + audit, and the CSRF gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/curl → /api/v2/settings | Untrusted settings mutations (risk caps, lot sizes) cross here |
| api/settings.py → validate_settings_form / SettingsStore | Server-side hard-cap enforcement; ported verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Tampering | settings beyond server hard caps | mitigate | `validate_settings_form` enforces server-side caps verbatim; invalid -> `{valid:false,errors}`; SPA zod mirror (Phase 11) is defense-in-depth only (V5). Tested (test_validate_breaches_cap). |
| T-08-19 | Spoofing/Tampering | CSRF on settings mutations | mitigate | `Depends(verify_csrf_token)` on validate/confirm/revert; 403 without `X-CSRF-Token`. Tested. |
| T-08-20 | Repudiation | settings change with no trail | mitigate | Confirm/revert write to `settings_audit` (existing audit timeline reused); GET returns the timeline. Tested (test_confirm_persists). |
| T-08-21 | Information Disclosure | settings read without auth | mitigate | `require_user` 401 gate on GET. |
| T-08-SC | Tampering | npm/pip installs | accept | Zero new packages (RESEARCH audit). No install task. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_api_settings.py -x`
- `.venv/bin/python -m pytest -m "not integration"`
- `git diff --exit-code executor.py trade_manager.py db.py mt5_connector.py mt5-rest-server/`
</verification>

<success_criteria>
- Settings read + validate/confirm/revert return JSON, never HTML; CSRF-guarded.
- Server hard caps enforced via validate_settings_form (ported verbatim); invalid -> {valid:false,errors}.
- Confirm persists + audits; revert inverts. Bot core diff empty.
</success_criteria>

<output>
Create `.planning/phases/08-json-api-foundation/08-05-SUMMARY.md` when done.
</output>
