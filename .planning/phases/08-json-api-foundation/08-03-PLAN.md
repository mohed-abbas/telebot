---
phase: 08-json-api-foundation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - api/accounts.py
  - api/positions.py
  - api/history.py
  - api/signals.py
  - api/stages.py
  - api/analytics.py
  - api/meta.py
  - tests/test_api_contract.py
autonomous: true
requirements: [API-01, API-04]
must_haves:
  truths:
    - "Every read view (accounts, positions, history, signals, stages, analytics, overview meta, trading-status, emergency preview) is retrievable via GET /api/v2/... returning Pydantic-modeled JSON"
    - "Each read route wraps its existing dashboard.py/db helper verbatim — no new query, no recomputation"
    - "Every numeric/price/volume/timestamp field returns both a raw value AND a server-formatted _display twin via api/formatting.py — parallel suffixed fields, ISO-8601 UTC-offset raw timestamps with absolute UTC display strings (D-05, D-06, D-07)"
    - "A XAUUSD position returns pip-sized open_price_display (2dp) with no client re-derivation needed"
    - "Read routes require a session (401 without one) and bot core shows zero git diff"
  artifacts:
    - path: "api/positions.py"
      provides: "GET /positions, GET /positions/{account}/{ticket}"
      contains: "_get_all_positions"
    - path: "api/meta.py"
      provides: "GET /overview, GET /trading-status, GET /emergency/preview"
    - path: "tests/test_api_contract.py"
      provides: "Read-route shape + 401 + dual-value field assertions (API-01, API-04)"
      contains: "_display"
  key_links:
    - from: "api/positions.py"
      to: "dashboard._get_all_positions"
      via: "accessor import; map dict -> Position with _display via api/formatting.py"
      pattern: "_get_all_positions"
    - from: "read routes"
      to: "api/formatting.py"
      via: "price_display/money_display/volume_display/ts_machine/ts_display"
      pattern: "from api.formatting import|api\\.formatting"
---

<objective>
Mirror the full read surface into `/api/v2`: accounts, positions (+ drilldown), history (+ filter options), signals, stages, analytics, overview meta, trading-status, and emergency preview — each wrapping its existing in-process helper verbatim and adding the D-05 dual-value `_display` twins through the shared formatter (API-01 + API-04). Plus the read-route contract test.

Purpose: This is a serialization refactor, not a logic rewrite — the computation already exists. The trap is treating any read endpoint as new business logic (it would duplicate a helper and risk divergence). Every route calls the existing helper and re-shapes its dict.

Output: 12 read routes returning Pydantic JSON with display-ready + machine-precise fields, and a green contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-json-api-foundation/08-CONTEXT.md
@.planning/phases/08-json-api-foundation/08-RESEARCH.md
@.planning/phases/08-json-api-foundation/08-PATTERNS.md
@.planning/phases/08-json-api-foundation/08-01-SUMMARY.md

<interfaces>
From api/deps.py: require_user (gate every read route).
From api/schemas.py: Position, AccountOverview, HistoryTrade, FilterOptions, Signal, Stage, Analytics, OverviewMeta, TradingStatus, EmergencyPreview (defined in Plan 01).
From api/formatting.py: price_display, money_display, volume_display, ts_machine, ts_display (route adds _display via these).
api/router.py (Plan 01) ALREADY includes every resource stub router — add  handlers to your own resource module (api/positions.py, api/accounts.py, etc.) only; do NOT edit api/router.py.

Existing read helpers to wrap (call unchanged, via accessor where they read _executor):
  _get_accounts_overview()            dashboard.py:1457  -> GET /accounts
  _get_all_positions()                dashboard.py:1401  -> GET /positions
  db.get_position_drilldown(t, acct)  db.py:1199         -> GET /positions/{account}/{ticket}
  db.get_filtered_trades(...)         db.py:500          -> GET /history (+ query)
  db.get_trade_filter_options()       db.py:477          -> GET /history/filter-options
  db.get_recent_signals(100)          db.py:572          -> GET /signals
  db.get_pending_stages() + db.get_recently_resolved_stages(50) + _enrich_stage_for_ui  -> GET /stages
  db.get_analytics_with_filters(...) + db.get_analytics_sources()  -> GET /analytics (+ query)
  overview composition dashboard.py:311-332              -> GET /overview
  _executor._trading_paused / list(_executor._reconnecting)  dashboard.py:1325-1331 -> GET /trading-status
  _get_all_positions() + connector.get_pending_orders()  dashboard.py:1274-1298 -> GET /emergency/preview
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Position/account/meta read routes + drilldown + emergency preview</name>
  <files>api/positions.py, api/accounts.py, api/meta.py</files>
  <read_first>
    - dashboard.py:1401-1454 (_get_all_positions dict shape — fields to model + which get _display), dashboard.py:1457-1510 (_get_accounts_overview, money fields at :1493-1508)
    - dashboard.py:311-332 (overview composition), dashboard.py:1325-1331 (trading-status), dashboard.py:1274-1298 (emergency preview)
    - db.py:1199 (get_position_drilldown)
    - api/schemas.py (Position, AccountOverview, OverviewMeta, TradingStatus, EmergencyPreview — defined Plan 01)
    - .planning/phases/08-json-api-foundation/08-PATTERNS.md → "api/positions.py + api/accounts.py + read routes" and "api/meta.py"
  </read_first>
  <action>
    Create `api/positions.py`: `GET /positions` -> `rows = await _get_all_positions()` (accessor import; inherits the stale-while-revalidate cache for free) -> return `list[Position]`, mapping each dict to the model and computing `volume_display=volume_display(r["volume"])`, `open_price_display=price_display(r["symbol"], r["open_price"])`, `profit_display=money_display(r["profit"])` via api/formatting.py. `GET /positions/{account}/{ticket}` -> `await db.get_position_drilldown(ticket, account)` (404 if None) with the same `_display` enrichment. Gate both with `Depends(require_user)`.

    Create `api/accounts.py`: `GET /accounts` -> `await _get_accounts_overview()` -> `list[AccountOverview]`, adding `balance_display/equity_display/margin_display/profit_display` via `money_display`. Gate with `require_user`.

    Create `api/meta.py`: `GET /overview` -> compose the dashboard.py:311-332 aggregate (accounts + positions + top-5 stages + flags `get_settings().trading_enabled`, `require_executor()._trading_paused`) into `OverviewMeta`. `GET /trading-status` -> `{paused: require_executor()._trading_paused, reconnecting: list(require_executor()._reconnecting)}` as `TradingStatus`. `GET /emergency/preview` -> `_get_all_positions()` + `connector.get_pending_orders()` shaped as `EmergencyPreview`. Use accessors (`require_executor`), never `from dashboard import _executor`. Gate all with `require_user`.

    Add handlers to the existing `router` in each of api/positions.py, api/accounts.py, api/meta.py (already included by api/router.py — do NOT edit api/router.py).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_api_contract.py -k "positions or accounts or overview or trading_status or emergency" -x</automated>
  </verify>
  <acceptance_criteria>
    - `api/positions.py` calls `_get_all_positions` (not a re-implemented query) and routes `price_display`/`volume_display`/`money_display` through `api/formatting.py`.
    - `GET /api/v2/positions` returns 401 without a session and a `list[Position]` (each item has `open_price` AND `open_price_display`) with a session.
    - `api/meta.py` uses `require_executor()` accessor and contains no `from dashboard import _executor`.
    - `git diff --exit-code db.py mt5_connector.py executor.py trade_manager.py` exits 0.
  </acceptance_criteria>
  <done>Positions/accounts/meta/drilldown/emergency-preview read routes return Pydantic JSON with dual-value fields, gated on session, wrapping existing helpers.</done>
</task>

<task type="auto">
  <name>Task 2: History / signals / stages / analytics read routes</name>
  <files>api/history.py, api/signals.py, api/stages.py, api/analytics.py</files>
  <read_first>
    - db.py:500 (get_filtered_trades + query params account/source/symbol/from_date/to_date), db.py:477 (get_trade_filter_options), db.py:572 (get_recent_signals)
    - db.py:1057,1144 (get_pending_stages / get_recently_resolved_stages), dashboard.py:424 (_enrich_stage_for_ui)
    - db.py:647,786 (get_analytics_with_filters + get_analytics_sources, query params range/source)
    - api/schemas.py (HistoryTrade, FilterOptions, Signal, Stage, Analytics — defined Plan 01)
    - .planning/phases/08-json-api-foundation/08-PATTERNS.md → read-route section + formatting section
  </read_first>
  <action>
    Create `api/history.py`: `GET /history` with optional query params `account, source, symbol, from_date, to_date` -> `await db.get_filtered_trades(...)` -> `list[HistoryTrade]` with money/price/timestamp `_display` twins via api/formatting.py (timestamps -> `ts_machine` raw + `ts_display`). `GET /history/filter-options` -> `await db.get_trade_filter_options()` -> `FilterOptions`.

    Create `api/signals.py`: `GET /signals` -> `await db.get_recent_signals(100)` -> `list[Signal]` (timestamp dual-value via ts_machine/ts_display).

    Create `api/stages.py`: `GET /stages` -> compose `await db.get_pending_stages()` + `await db.get_recently_resolved_stages(50)`, each enriched through `_enrich_stage_for_ui` (accessor import) -> a stages payload with active+resolved lists; price/volume/timestamp fields get `_display` twins.

    Create `api/analytics.py`: `GET /analytics` with optional query `range, source` -> `await db.get_analytics_with_filters(...)` + `await db.get_analytics_sources()` -> `Analytics`; money/percent fields get `_display` twins where they are price/money (win-rate/profit-factor are ratios — format as needed via money/number display, keep raw).

    Gate every route with `Depends(require_user)`. Add handlers to the existing `router` in each of api/history.py, api/signals.py, api/stages.py, api/analytics.py (already included by api/router.py — do NOT edit api/router.py).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_api_contract.py -k "history or signals or stages or analytics" -x</automated>
  </verify>
  <acceptance_criteria>
    - Each route calls its named existing helper (`get_filtered_trades`, `get_recent_signals`, `get_pending_stages`, `get_analytics_with_filters`) — no re-implemented query.
    - Timestamp fields carry both `ts_machine` ISO-8601-with-offset raw and a `ts_display` absolute-UTC string.
    - `GET /api/v2/history`, `/signals`, `/stages`, `/analytics` each return 401 without a session and Pydantic JSON with a session.
    - `git diff --exit-code db.py mt5_connector.py executor.py trade_manager.py` exits 0.
  </acceptance_criteria>
  <done>History/signals/stages/analytics read routes wrap their existing helpers and return dual-value Pydantic JSON gated on session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_api_contract.py — read-route shapes, 401 paths, dual-value assertions</name>
  <files>tests/test_api_contract.py</files>
  <behavior>
    - Each GET read route returns 200 (authed) with the expected top-level keys / list-of-model shape.
    - Each GET read route returns 401 without a session.
    - A positions item carries both `open_price` (raw float) and `open_price_display` (string); a XAUUSD item formats `open_price_display` to 2dp.
    - Timestamp-bearing items (history/signals) carry both an ISO-8601-with-offset raw field and an absolute-UTC display string.
    - Read routes return JSON, never HTML.
  </behavior>
  <read_first>
    - tests/test_login_flow.py:64-88 (TestClient round-trip), tests/conftest.py (api_app + authed_client + DryRunConnector stub from Plan 01)
    - .planning/phases/08-json-api-foundation/08-VALIDATION.md → API-01 + API-04 rows
    - .planning/phases/08-json-api-foundation/08-PATTERNS.md → "tests/test_api_*.py" (DryRunConnector deterministic rows via init_dashboard)
  </read_first>
  <action>
    Create `tests/test_api_contract.py` using the `api_app` fixture (DryRunConnector-backed `_get_all_positions` returns deterministic rows). Write tests: (1) parametrized `test_read_route_requires_session` over the read routes (`/accounts`, `/positions`, `/history`, `/signals`, `/stages`, `/analytics`, `/overview`, `/trading-status`) asserting 401 with no session. (2) `test_read_routes_return_json_with_expected_keys` — authed GET each route, assert 200, assert `content-type` is JSON, assert the model keys present. (3) `test_positions_dual_value` — authed GET `/positions`, assert each item has `open_price` (number) AND `open_price_display` (str); for a XAUUSD row assert `open_price_display` is 2dp. (4) `test_mutations_return_json` placeholder hook referenced by VALIDATION (assert a mutation route — once Plan 04/05 land — returns a JSON envelope not HTML; structure the test so it is collected and passes when actions routes exist, otherwise skip with a clear reason). Provide deterministic XAUUSD rows through the DryRunConnector stub.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_api_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_api_contract.py` contains `_display`, asserts 401 for the no-session case across the read routes, and asserts a 2dp XAUUSD `open_price_display`.
    - All read routes assert JSON content-type (never HTML).
    - `pytest tests/test_api_contract.py -x` is green (skips cleanly if dev Postgres absent).
  </acceptance_criteria>
  <done>The contract test proves read-route shapes, 401 gating, dual-value fields, and JSON-not-HTML across the read surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/curl → /api/v2 read routes | Session-gated reads; no mutation, but data exposure surface |
| api/ read routes → dashboard/db helpers | Call-only; never modify bot core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure | read routes without auth | mitigate | `require_user` 401 gate on every read route; tested (test_read_route_requires_session). |
| T-08-12 | Tampering | client re-deriving price precision | mitigate | Server-side dual-value only; SPA reads `*_display`, never recomputes (API-04, Pitfall 5). Tested. |
| T-08-13 | Information Disclosure | drilldown leaking other accounts | accept | Single-operator tool; all accounts belong to the one operator (PROJECT.md usage model). |
| T-08-SC | Tampering | npm/pip installs | accept | Zero new packages (RESEARCH audit). No install task. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_api_contract.py -x`
- `.venv/bin/python -m pytest tests/test_api_formatting.py -x` (still green — formatter unchanged)
- `git diff --exit-code executor.py trade_manager.py db.py mt5_connector.py mt5-rest-server/`
</verification>

<success_criteria>
- All 12 read views retrievable via GET /api/v2/... returning Pydantic JSON wrapping existing helpers.
- Every price/money/volume/timestamp field carries a raw value + a server-formatted _display twin.
- Read routes are session-gated (401 without); bot core diff empty.
- Contract test green.
</success_criteria>

<output>
Create `.planning/phases/08-json-api-foundation/08-03-SUMMARY.md` when done.
</output>
