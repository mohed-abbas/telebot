---
phase: 13-staged-entry-execution-correctness-and-direct-zone-multi-sta
plan: 03
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - trade_manager.py
  - api/stages.py
autonomous: true
requirements: [EXEC2-02, EXEC2-03, EXEC2-04]
must_haves:
  truths:
    - "In percent mode a staged sequence submits risk_value / max_stages per stage (not full risk_value)"
    - "/staged target_lot equals the actually-submitted per-stage volume"
    - "A standalone OPEN with no SL is skipped cleanly through the skip-result path, no EXECUTION ERROR"
    - "The D-08 sl<=0 guard still backstops the SL-less path"
  artifacts:
    - path: "trade_manager.py"
      provides: "percent branch divides risk by max_stages; signal.sl-is-None early skip; create-site persists signal_sl/signal_tp"
      contains: "per_stage_risk"
  key_links:
    - from: "trade_manager._execute_open_on_account percent branch"
      to: "calculate_lot_size risk_percent"
      via: "risk_pct / max_stages when staged"
      pattern: "max_stages"
    - from: "trade_manager._handle_open (sl-less detect)"
      to: "skip-result stream"
      via: "signal.sl is None early return before calculate_sl_distance"
      pattern: "signal.sl is None"
---

<objective>
Three localized trade_manager.py fixes that converge on money-correctness and a clean skip — none touch executor.py (so this plan runs parallel to Plan 02):

1. **EXEC2-02 — percent risk split (D2-06):** The percent branch at trade_manager.py:696-709 submits `calculate_lot_size(risk_percent=full risk_value)` with no `/max_stages`, giving N× exposure. `fixed_lot` already splits via `stage_lot_size`. Fix: in the percent branch, divide `risk_pct` by `snapshot.max_stages` for staged sequences (guard `max_stages>0`), gated on `staged` so the v1.0 non-staged path is unaffected.

2. **EXEC2-03 — display agreement (D2-08):** Convergent — `target_lot` is already written from `stage_lot_size()` and read straight through to `/staged`. Once EXEC2-02 lands, the submitted percent volume equals the persisted `target_lot`. No recompute; add the contract test assertion + confirm api/stages.py reads the persisted column unchanged.

3. **EXEC2-04 — SL-less clean skip (D2-13):** A standalone OPEN with `signal.sl is None` crashes at `calculate_sl_distance(entry, None)` (trade_manager.py:687) BEFORE the D-08 `sl<=0` backstop at :724-730. Detect `signal.sl is None` at the top of `_handle_open` and route to the existing skip-result stream with reason "Skipped: signal has no SL (D-08 requires a stop)". D-08 guard stays as the second backstop.

Also persist `signal_sl`/`signal_tp` on the rows created in this file's create-sites (the text-only `_handle_text_only_open` stage-1 row and any `_handle_open` rows) so Plan 02's read path has them — additive key on the existing `rows` dicts.

Purpose: risk_value becomes a true ceiling; display matches reality; SL-less signals skip instead of erroring.
Output: trade_manager.py percent split + SL-less skip + create-site SL/TP persistence; /staged contract verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-staged-entry-execution-correctness-and-direct-zone-multi-sta/13-CONTEXT.md
@.planning/phases/13-staged-entry-execution-correctness-and-direct-zone-multi-sta/13-RESEARCH.md
@.planning/phases/13-staged-entry-execution-correctness-and-direct-zone-multi-sta/13-VALIDATION.md
@.planning/phases/06-staged-entry-execution/06-CONTEXT.md
@$HOME/.claude/projects/-Users-murx-Developer-personal-telebot/memory/project_lot_semantics.md

<interfaces>
<!-- _execute_open_on_account percent branch (trade_manager.py:698-709): -->
<!--   risk_pct, max_lot, _ = _effective(self, acct) -->
<!--   lot_size = calculate_lot_size(risk_percent=risk_pct, sl_distance=..., ...)  ◄ no /max_stages -->
<!--   `staged`, `snapshot`, `stage_number`, `stage_row_id` are params of _execute_open_on_account. -->
<!-- stage_lot_size(snapshot) = snapshot.risk_value / snapshot.max_stages (trade_manager.py:121) — display value already split. -->
<!-- calculate_sl_distance(entry, signal.sl) at trade_manager.py:687 → abs(entry - None) TypeError when sl=None. -->
<!-- D-08 guard: jittered_sl is None or <= 0.0 → status=failed (trade_manager.py:724-730). -->
<!-- _handle_open (548-577): logs signal, loops accounts, calls _execute_open_on_account. -->
<!-- _handle_text_only_open builds a stage-1 rows dict — add signal_sl/signal_tp keys (orphan has no signal TP → signal_tp stays None; signal_sl is the default-SL price if known, else None). -->
<!-- api/stages.py _enrich_active reads target_lot straight from get_pending_stages row (no recompute). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split percent-mode risk by max_stages and verify the /staged contract</name>
  <files>trade_manager.py, api/stages.py, tests/test_stages_contract.py</files>
  <behavior>
    - percent mode, staged sequence, risk_value=2%, max_stages=4 → each stage submits volume sized at 0.5% (risk_value/max_stages).
    - Submitted per-stage volume == persisted target_lot (stage_lot_size).
    - v1.0 non-staged percent OPEN (staged=False) → unchanged full risk_pct (no division).
    - fixed_lot path unchanged (already splits via stage_lot_size).
  </behavior>
  <read_first>
    - trade_manager.py:679-712 (the sizing block; fixed_lot branch 693-695, percent branch 696-709 — the bug site)
    - trade_manager.py:121-128 (stage_lot_size — the already-correct per-stage split for display)
    - .planning/phases/13-.../13-RESEARCH.md "EXEC2-02 fix site" code example (per_stage_risk = risk_pct/stages if staged) and Pitfall 3
    - .planning/phases/13-.../13-CONTEXT.md D2-06/D2-07 (risk_value is a TOTAL ceiling split equally; never exceed; no redistribution) and D2-08 (display == submitted)
    - .planning/phases/06-staged-entry-execution/06-CONTEXT.md D-15 (equal risk split)
    - project_lot_semantics.md (fixed_lot risk_value = TOTAL across max_stages — do NOT change fixed_lot)
    - api/stages.py _enrich_active (target_lot read-through — confirm no recompute)
    - tests/test_staged_executor.py stub test_percent_splits_risk; tests/test_stages_contract.py stub test_target_lot_matches_volume
  </read_first>
  <action>
    In the percent branch of `_execute_open_on_account` (the `else` after the fixed_lot block), compute `stages = snapshot.max_stages if snapshot and snapshot.max_stages > 0 else 1` and `per_stage_risk = risk_pct / stages if staged else risk_pct`, then pass `risk_percent=per_stage_risk` to `calculate_lot_size`. Gate the division on the `staged` flag so the v1.0 single-signal non-staged path keeps full `risk_pct` (belt-and-suspenders; after EXEC2-06 standalone OPEN becomes staged and naturally splits). Do NOT touch the fixed_lot branch or `stage_lot_size`.

    For EXEC2-03: confirm `api/stages.py` reads `target_lot` straight from the persisted row (no recompute) — make no code change there unless a recompute is found; the contract test asserts equality. Implement `test_target_lot_matches_volume` in tests/test_stages_contract.py to assert the `/staged` `target_lot` equals the per-stage submitted volume for a percent-mode staged sequence.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_staged_executor.py -k percent_splits_risk -x` passes.
    - `pytest tests/test_stages_contract.py -k target_lot_matches_volume -x` passes.
    - trade_manager.py percent branch divides risk by max_stages: `grep -q "per_stage_risk" trade_manager.py` and the division references max_stages.
    - fixed_lot branch and stage_lot_size unchanged (no diff to those lines).
    - api/stages.py contains no `target_lot` recompute (display reads the persisted column).
    - Fast core green: `pytest tests/test_staged_executor.py tests/test_staged_safety_hooks.py -x`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_staged_executor.py -k percent_splits_risk -x -q && pytest tests/test_stages_contract.py -k target_lot_matches_volume -x -q && grep -q "per_stage_risk" trade_manager.py</automated>
  </verify>
  <done>percent mode submits risk_value/max_stages per stage; /staged target_lot equals submitted volume; fixed_lot untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SL-less standalone OPEN → clean skip; persist signal_sl/signal_tp at create-sites</name>
  <files>trade_manager.py, tests/test_trade_manager.py</files>
  <behavior>
    - OPEN with signal.sl is None → returns a skip-result ({status:"skipped", reason contains "no SL"}), no exception, no EXECUTION ERROR, calculate_sl_distance never called.
    - OPEN with a real SL → unchanged behavior.
    - D-08: a path that reaches sizing with sl<=0 still hard-rejects at trade_manager.py:724-730.
    - Text-only stage-1 rows created in this file carry signal_sl (default-SL price or None) and signal_tp (None for orphan) so Plan 02's read path has them.
  </behavior>
  <read_first>
    - trade_manager.py:548-577 (_handle_open entry — where the sl-is-None detect goes, before the account loop / sizing)
    - trade_manager.py:685-690 (calculate_sl_distance(entry, signal.sl) at :687 — the crash site) and :724-730 (D-08 guard backstop)
    - .planning/phases/13-.../13-RESEARCH.md "EXEC2-04 early skip" code example and Pitfall 4 (crash precedes the D-08 guard)
    - .planning/phases/13-.../13-CONTEXT.md D2-13 (detect before calculate_sl_distance; route to existing skip stream; D-08 stays backstop)
    - .planning/phases/06-staged-entry-execution/06-CONTEXT.md D-08
    - _handle_text_only_open rows-dict builder (trade_manager.py ~300-360) — where to add signal_sl/signal_tp keys
    - tests/test_trade_manager.py stub test_sl_less_open_skips_cleanly; existing test_default_sl_zero_hard_rejects_text_only (must stay green)
  </read_first>
  <action>
    At the very top of `_handle_open` (before logging the signal for execution / before the account loop and any sizing), add: `if signal.sl is None:` → log the signal with `action_taken="skipped"` and return a single skip-result `[{"account": "*", "status": "skipped", "reason": "Skipped: signal has no SL (D-08 requires a stop)"}]`. This guarantees `calculate_sl_distance(entry, None)` is never reached. Do NOT remove the D-08 `sl<=0` guard at :724-730 — it remains the second backstop for sl values that are present-but-non-positive.

    In the create-site row-builders in THIS file (the `_handle_text_only_open` stage-1 row, and any rows built in `_handle_open` for non-staged → not applicable until EXEC2-06), add `"signal_sl"` and `"signal_tp"` keys to each `rows` dict. For the text-only orphan: `signal_sl` = the computed default-SL price if available at row-build time, else None; `signal_tp` = None (orphan has no signal TP — Plan 04 attaches the protective TP later). This makes create_staged_entries (Plan 01) persist them so Plan 02's `_fire_zone_stage`/cascade can read them. The EXEC2-06 direct-zone create-site (Plan 05) sets signal_sl/signal_tp to the OPEN's real signal.sl/target_tp.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_trade_manager.py -k sl_less_open_skips_cleanly -x` passes.
    - `pytest tests/test_trade_manager.py -k test_default_sl_zero_hard_rejects_text_only -x` still passes (D-08 backstop intact).
    - trade_manager.py `_handle_open` contains `if signal.sl is None:` early skip BEFORE any `calculate_sl_distance` call (grep: the `signal.sl is None` line precedes the first `calculate_sl_distance` in the file's _handle_open path).
    - Text-only create-site rows dict includes `"signal_sl"` and `"signal_tp"` keys.
    - Fast core green: `pytest tests/test_staged_executor.py tests/test_staged_safety_hooks.py -x`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_trade_manager.py -k "sl_less_open_skips_cleanly or test_default_sl_zero_hard_rejects_text_only" -x -q && grep -q "signal.sl is None" trade_manager.py && grep -q '"signal_sl"' trade_manager.py</automated>
  </verify>
  <done>SL-less OPEN skips cleanly with no exception; D-08 backstop intact; text-only create-site persists signal_sl/signal_tp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (financial-safety framing)

| Boundary | Description |
|----------|-------------|
| signal risk_value → submitted volume | A missing /max_stages turns a 2% sequence into 8% deployed — N× capital at risk |
| SL-less signal → execution engine | Untrusted signal input lacking a stop must never reach the sizing/submit path |
| persisted target_lot → operator dashboard | The displayed per-stage lot must equal the real submitted volume or the operator mis-judges exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Denial of capital (N× over-exposure) | percent sizing branch | mitigate | Divide risk_pct by max_stages for staged sequences; risk_value is a never-exceed ceiling (D2-07); test_percent_splits_risk |
| T-13-07 | Tampering (order with no stop / crash alert) | SL-less OPEN | mitigate | Early `signal.sl is None` skip before calculate_sl_distance; D-08 sl<=0 guard remains backstop; test_sl_less_open_skips_cleanly + test_default_sl_zero_hard_rejects_text_only |
| T-13-08 | Repudiation / misinformed operator | /staged target_lot display | mitigate | Display reads persisted target_lot (already split); contract test target_lot_matches_volume asserts display==submitted |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — in-repo edits only (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest tests/test_trade_manager.py tests/test_staged_executor.py tests/test_stages_contract.py -x` green.
- D-08 hard-reject test green.
- Per-wave merge: `pytest tests/ -q` green.
</verification>

<success_criteria>
- percent mode submits risk_value/max_stages per stage; fixed_lot unchanged.
- /staged target_lot equals submitted volume.
- SL-less OPEN skips cleanly; D-08 backstop intact; create-sites persist signal_sl/signal_tp.
</success_criteria>

<output>
Create `.planning/phases/13-staged-entry-execution-correctness-and-direct-zone-multi-sta/13-03-SUMMARY.md` when done.
</output>
