---
phase: 11-live-money-pages-settings
plan: 05
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - frontend/src/routes/KillSwitchView.tsx
autonomous: true
requirements: [PAGE-07]
user_setup: []

must_haves:
  truths:
    - "The kill switch reads GET /api/v2/emergency/preview and renders a red card with the open-positions + pending-orders counts"
    - "Confirm runs a two-step flow: preview → CONFIRM CLOSE ALL (disabled-while-pending) → POST /api/v2/emergency/close"
    - "The confirm action is hidden when there is nothing to close (both counts == 0)"
    - "Resume Trading (POST /api/v2/emergency/resume) is shown only while paused"
  artifacts:
    - path: "frontend/src/routes/KillSwitchView.tsx"
      provides: "PAGE-07 two-step kill-switch preview→confirm + resume"
      min_lines: 40
      contains: "CONFIRM CLOSE ALL"
  key_links:
    - from: "frontend/src/routes/KillSwitchView.tsx"
      to: "GET /api/v2/emergency/preview"
      via: "useQuery preview"
      pattern: "emergency/preview"
    - from: "frontend/src/routes/KillSwitchView.tsx"
      to: "frontend/src/hooks/useEmergency.ts"
      via: "close + resume mutations"
      pattern: "useEmergency"
---

<objective>
Build the Emergency Kill Switch page (PAGE-07) at parity with the legacy preview card: a two-step preview → confirm flow that reads the counts of positions/orders that will be closed, requires an explicit CONFIRM CLOSE ALL (disabled-while-pending), and offers Resume Trading while paused. This is the most consequential destructive action — it closes ALL positions, cancels ALL orders, and pauses trading.

Purpose: deliver SC#4's kill-switch half (the partial-close half lives in 11-03). Light, single-page, single-concern — kept separate so it can build in parallel with Positions and Settings.
Output: KillSwitchView route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-money-pages-settings/11-RESEARCH.md
@.planning/phases/11-live-money-pages-settings/11-PATTERNS.md
@.planning/phases/11-live-money-pages-settings/11-UI-SPEC.md

<interfaces>
<!-- Contracts (RESEARCH §Overview/status + §Live-money mutations). -->
GET  /api/v2/emergency/preview → EmergencyPreview {open_positions:int, pending_orders:int, accounts:string[]}
GET  /api/v2/trading-status    → TradingStatus {paused:bool, status:"paused"|"running"}  (drives Resume visibility)
POST /api/v2/emergency/close   → EmergencyResult {results, ok}. No body.
POST /api/v2/emergency/resume  → {status:"resumed"}. No body.

<!-- Wave 1 hook -->
useEmergency() → {close, resume} mutations (invalidate overview/trading-status/positions on success; toasts wired)

<!-- Inherited primitives -->
Loading / ErrorPanel — frontend/src/components/state/*
button — frontend/src/components/ui/button.tsx (variant="destructive" for CONFIRM CLOSE ALL; variant="default" cyan for Resume)
StagedView read pattern — frontend/src/routes/StagedView.tsx
LoginView submit-disabled-while-pending — frontend/src/auth/LoginView.tsx:47-70
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KillSwitchView — preview read + two-step confirm + resume (PAGE-07)</name>
  <files>frontend/src/routes/KillSwitchView.tsx</files>
  <read_first>
    - frontend/src/routes/StagedView.tsx (lines 146-226 — read + Loading/ErrorPanel branch order, page shell)
    - frontend/src/auth/LoginView.tsx (lines 47-70 — submit disabled-while-pending discipline on confirm)
    - frontend/src/hooks/useEmergency.ts (Wave 1 — close + resume)
    - frontend/src/components/ui/button.tsx (destructive + default variants; size ≥ default for min-h-10)
    - .planning/phases/11-live-money-pages-settings/11-PATTERNS.md §"KillSwitchView.tsx" (lines 155-162)
    - .planning/phases/11-live-money-pages-settings/11-UI-SPEC.md §Copywriting "Kill switch" (page title, warning body, "CONFIRM CLOSE ALL"/"Closing all…", "Keep trading active", "Resume Trading"/"Resuming…") + §Color (destructive red exclusive list; Resume is cyan)
    - PARITY ref (structure only): templates/partials/kill_switch_preview.html
  </read_first>
  <behavior>
    - useQuery(["emergency-preview"], GET /api/v2/emergency/preview); isPending → Loading; isError → inline ErrorPanel
    - red card showing Open Positions count + Pending Orders count + warning copy
    - two-step confirm: a CONFIRM CLOSE ALL button (variant="destructive", disabled-while-pending → "Closing all…") calls useEmergency.close; "Keep trading active" cancels back without closing
    - the confirm button is HIDDEN when both open_positions == 0 and pending_orders == 0 (nothing to close)
    - useQuery(["trading-status"], GET /api/v2/trading-status); a "Resume Trading" button (cyan variant="default", disabled-while-pending → "Resuming…") via useEmergency.resume is shown ONLY while paused
  </behavior>
  <action>
    Create KillSwitchView. Read the preview via useQuery(["emergency-preview"], api("/api/v2/emergency/preview")) and the pause state via useQuery(["trading-status"], api("/api/v2/trading-status")) (StagedView Loading/ErrorPanel branch order; page title "Emergency Kill Switch" in red). Render a red --destructive card (kill_switch_preview.html parity STRUCTURE) with Open Positions + Pending Orders counts and the warning body "This will close ALL positions, cancel ALL pending orders, and pause trading. You must manually re-enable trading afterwards." Two-step confirm: a CONFIRM CLOSE ALL button (variant="destructive", size ≥ default for the min-h-10 floor, disabled={close.isPending} → "Closing all…") wired to useEmergency.close; a "Keep trading active" escape that returns to the preview/Overview without closing. HIDE the confirm button when open_positions == 0 && pending_orders == 0. When trading-status.paused, render a "Resume Trading" button (cyan variant="default", disabled-while-pending → "Resuming…") wired to useEmergency.resume. No optimistic state — the hooks invalidate overview/trading-status/positions on success.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/KillSwitchView.tsx` reads `/api/v2/emergency/preview` and `/api/v2/trading-status` via useQuery
    - contains "CONFIRM CLOSE ALL" on a `variant="destructive"` button that is `disabled` while pending
    - the confirm button is conditionally hidden when both counts are 0
    - Resume Trading is a cyan `variant="default"` button shown only while paused, wired to useEmergency.resume
    - no `setQueryData`; mutations via useEmergency (api()/CSRF)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Two-step kill-switch preview→confirm + resume shipped at parity; confirm disabled-while-pending and hidden when nothing to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → emergency/close | The most consequential destructive action (close ALL + pause) crosses to the broker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Tampering | CONFIRM CLOSE ALL | mitigate | Two-step preview→confirm (explicit second action) + disabled-while-pending prevents accidental/double trigger; no optimistic state (SC#1) — pause/close reflected only after server confirm via invalidate |
| T-11-18 | Spoofing | emergency close/resume | mitigate | Both via api() → X-CSRF-Token echoed; rejected 403 without it (Phase 8 test_api_csrf) |
| T-11-19 | DoS (operator error) | confirm when nothing to close | mitigate | Confirm button hidden when both counts == 0 — no empty/accidental close-all |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- grep: no `setQueryData` in KillSwitchView; CONFIRM CLOSE ALL present
- `pytest tests/test_api_csrf.py -x` green
- MANUAL (VPS + MT5 demo): preview → render counts → CONFIRM CLOSE ALL (disabled while pending) → resume; confirm hidden when no positions/orders
</verification>

<success_criteria>
- Kill switch at parity: two-step preview→confirm, confirm disabled-while-pending and hidden when nothing to close, Resume while paused
- CSRF-safe, server-confirmed (no optimistic) — SC#2/SC#1/SC#4
</success_criteria>

<output>
Create `.planning/phases/11-live-money-pages-settings/11-05-SUMMARY.md` when done
</output>
