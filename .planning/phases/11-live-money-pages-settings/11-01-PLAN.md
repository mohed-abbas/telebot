---
phase: 11-live-money-pages-settings
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vitest.config.ts
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/popover.tsx
  - frontend/src/lib/footgun.ts
  - frontend/src/lib/footgun.test.ts
  - frontend/src/lib/settingsSchema.ts
  - frontend/src/lib/settingsSchema.test.ts
autonomous: false
requirements: [SUX-02, SUX-03]
user_setup: []

must_haves:
  truths:
    - "A JS test runner (vitest) exists and `npx vitest run` executes the two pure-function units"
    - "The footgun calc multiplies risk_value × max_stages in percent mode and does NOT multiply in fixed_lot mode"
    - "The zod settings schema rejects percent risk_value > 5.0, fixed_lot risk_value > the per-account max_lot_size, and out-of-range ints"
    - "shadcn dialog/tooltip/select/badge/popover render opaque on the dark brand background"
  artifacts:
    - path: "frontend/src/lib/footgun.ts"
      provides: "Mode-aware compounded-exposure pure function (D-06/D-07)"
      exports: ["footgun"]
    - path: "frontend/src/lib/settingsSchema.ts"
      provides: "Mode-aware zod cap schema factory mirroring validate_settings_form"
      exports: ["makeSettingsSchema"]
    - path: "frontend/src/lib/footgun.test.ts"
      provides: "Pitfall-6 regression unit (percent multiplies; fixed_lot does not)"
    - path: "frontend/src/lib/settingsSchema.test.ts"
      provides: "SUX-03 cap-mirror unit"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest runner config (the only frontend framework gap)"
  key_links:
    - from: "frontend/src/lib/settingsSchema.ts"
      to: "dashboard.validate_settings_form caps"
      via: "hand-derived caps (percent ≤5.0, fixed_lot ≤ maxLotSize, ints 1-10/1-500/1-100)"
      pattern: "max\\(5|max\\(10|max\\(500|max\\(100|maxLotSize"
---

<objective>
Establish the Phase 11 frontend foundation: install the three new npm packages (react-hook-form, zod, @hookform/resolvers), add the vitest runner (the only test-framework gap), add the five shadcn components every later plan consumes (dialog, tooltip, select, badge, popover), and ship the two pure-function utilities — the mode-aware footgun calc (D-06/D-07) and the mode-aware zod cap schema (SUX-03) — each covered by a vitest unit. These are the load-bearing, deterministic, automated proofs for the whole phase.

Purpose: every Wave 1/2/3 plan depends on these packages, components, and pure functions. Building them first (and proving them with units) means the page plans never re-derive caps or footgun math.
Output: package.json deps, vitest config, 5 shadcn ui components, footgun.ts + test, settingsSchema.ts + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-money-pages-settings/11-RESEARCH.md
@.planning/phases/11-live-money-pages-settings/11-PATTERNS.md
@.planning/phases/11-live-money-pages-settings/11-UI-SPEC.md
@.planning/phases/11-live-money-pages-settings/11-VALIDATION.md

<interfaces>
<!-- Caps the zod schema MUST mirror (RESEARCH §Settings Caps, derived from dashboard.validate_settings_form, dashboard.py:658-790). Derive — do NOT invent. -->
risk_mode: enum "percent" | "fixed_lot"
risk_value (percent mode): float, > 0 AND <= 5.0
risk_value (fixed_lot mode): float, > 0 AND <= max_lot_size  (per-account; read from SettingsView values.max_lot_size at runtime)
max_stages: int, 1..10
default_sl_pips: int, 1..500
max_daily_trades: int, 1..100
max_open_trades: READ-ONLY — NOT in the form, do NOT add a cap, do NOT submit as changed.

<!-- Footgun (RESEARCH §Mode-aware footgun, lines 467-474; trade_manager.py:108-117 confirms fixed_lot risk_value is the TOTAL across stages) -->
percent mode:   total = risk_value * max_stages  → "{max_stages} entries at {risk_value}% risks up to {total}% per signal."
fixed_lot mode: do NOT multiply               → "This sizes up to {risk_value} total lots per signal across {max_stages} entries."

<!-- Existing pure-calc precedent (do NOT copy money formatting): frontend/src/lib/useElapsed.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages, add vitest, add the five shadcn components</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vitest.config.ts, frontend/src/components/ui/dialog.tsx, frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/select.tsx, frontend/src/components/ui/badge.tsx, frontend/src/components/ui/popover.tsx</files>
  <read_first>
    - frontend/package.json (current deps — confirm react-hook-form/zod/@hookform/resolvers/vitest absent)
    - frontend/components.json (shadcn config: new-york style, neutral base — drives `shadcn add`)
    - frontend/src/components/ui/ (Phase 9/10 already added button/input/label/card/sonner — match this dir + import style)
    - .planning/phases/11-live-money-pages-settings/11-RESEARCH.md §Standard Stack (versions: react-hook-form ^7.77, zod ^4, @hookform/resolvers ^5; the v4/v5 import caution) and §Package Legitimacy Audit (all three [ASSUMED] — slopcheck unavailable)
  </read_first>
  <action>
    In frontend/: install `react-hook-form` (^7.77), `zod` (^4), `@hookform/resolvers` (^5) as deps and `vitest` as a dev dep. Add a minimal `vitest.config.ts` (jsdom NOT required — these are pure-function units; use the default node environment plus the existing `@/` path alias from vite config) and a `"test": "vitest run"` script in package.json. Add the five shadcn components via the CLI: `npx shadcn@latest add dialog tooltip select badge popover` (new-york style per components.json) into frontend/src/components/ui/. NOTE the zod-v4/resolvers-v5 caution from RESEARCH: the resolver import is `import { zodResolver } from "@hookform/resolvers/zod"` — confirm it builds. Do NOT downgrade to zod v3 snippets.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` lists `react-hook-form`, `zod`, `@hookform/resolvers` in dependencies and `vitest` in devDependencies
    - `frontend/package.json` has a `"test"` script running `vitest run` (no watch flag)
    - `frontend/vitest.config.ts` exists and resolves the `@/` alias
    - `frontend/src/components/ui/{dialog,tooltip,select,badge,popover}.tsx` all exist
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Three packages + vitest installed; vitest config present; five shadcn components added; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Footgun pure function + vitest unit (D-06/D-07, Pitfall 6)</name>
  <files>frontend/src/lib/footgun.ts, frontend/src/lib/footgun.test.ts</files>
  <read_first>
    - .planning/phases/11-live-money-pages-settings/11-RESEARCH.md §"Mode-aware footgun (pure, testable)" (lines 464-475 — copy verbatim) and Pitfall 6 (lines 448-451)
    - frontend/src/lib/useElapsed.ts (the existing allowed-client-calc precedent — pure, off bare numerics)
    - .planning/phases/11-live-money-pages-settings/11-UI-SPEC.md §Copywriting Contract "Footgun — percent mode" / "Footgun — fixed_lot mode" (the exact copy strings)
  </read_first>
  <behavior>
    - footgun("percent", 2, 4) returns a string containing "8" (2 × 4 = 8%) — percent mode multiplies
    - footgun("fixed_lot", 0.4, 4) returns a string containing "0.4" and "4" but NOT "1.6" — fixed_lot does NOT multiply (Pitfall 6)
    - copy matches UI-SPEC: percent → "{max_stages} entries at {risk_value}% risks up to {total}% per signal."; fixed_lot → "This sizes up to {risk_value} total lots per signal across {max_stages} entries."
  </behavior>
  <action>
    Create `footgun(mode: "percent" | "fixed_lot", riskValue: number, maxStages: number): string` per the RESEARCH §Mode-aware footgun snippet. percent mode computes `riskValue * maxStages` (compounds). fixed_lot mode does NOT multiply — riskValue is the TOTAL across stages (operator-confirmed 2026-05-01; trade_manager.py:108-117). A single un-branched `risk_value * max_stages` is the Pitfall-6 bug. Render the exact UI-SPEC copy strings. Write footgun.test.ts asserting the percent-multiplies / fixed_lot-does-not behavior above (per D-07).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/footgun.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/footgun.ts` exports `footgun`; the fixed_lot branch contains NO `* maxStages` / `* max_stages`
    - footgun.test.ts asserts percent multiplies (2,4 → 8) AND fixed_lot does NOT (0.4,4 → no "1.6")
    - `cd frontend && npx vitest run src/lib/footgun.test.ts` passes
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Pure footgun fn shipped; vitest unit green proving the mode-aware (no-multiply-in-fixed_lot) contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mode-aware zod cap schema + vitest unit (SUX-03)</name>
  <files>frontend/src/lib/settingsSchema.ts, frontend/src/lib/settingsSchema.test.ts</files>
  <read_first>
    - .planning/phases/11-live-money-pages-settings/11-RESEARCH.md §"Settings Caps + Footgun" caps table (lines 333-340) and §"Mode-aware zod sketch" (lines 346-363 — copy, confirm v4 API at build) and the per-account `max_lot_size` mechanism note (lines 342-344)
    - frontend/src/lib/utils.ts (existing lib module style)
    - .planning/phases/11-live-money-pages-settings/11-VALIDATION.md §Wave 0 Requirements (the exact assertions the test must make)
  </read_first>
  <behavior>
    - makeSettingsSchema(0.5): percent mode rejects risk_value 6.0 (>5.0), accepts 2.0
    - makeSettingsSchema(0.5): fixed_lot mode rejects risk_value 0.9 (> maxLotSize 0.5), accepts 0.4
    - max_stages 0 and 11 rejected, 1 and 10 accepted; default_sl_pips 0 and 501 rejected; max_daily_trades 0 and 101 rejected
    - schema does NOT define a cap on max_open_trades (read-only field, not in the form)
  </behavior>
  <action>
    Create a `makeSettingsSchema(maxLotSize: number)` factory returning a zod object with: risk_mode `z.enum(["percent","fixed_lot"])`; risk_value positive; max_stages int 1-10; default_sl_pips int 1-500; max_daily_trades int 1-100; plus a `superRefine` that rejects percent risk_value > 5.0 and fixed_lot risk_value > maxLotSize (the per-account cap read at runtime from SettingsView values.max_lot_size). DERIVE the caps from validate_settings_form (RESEARCH caps table) — do NOT invent thresholds. Do NOT add a cap for max_open_trades. Confirm the zod-v4 `z.enum`/`superRefine`/`addIssue({code:"custom"})` signatures compile (the v4 API differs from v3 — npm run build catches a wrong signature). Write settingsSchema.test.ts asserting the behaviors above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/settingsSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/settingsSchema.ts` exports `makeSettingsSchema`
    - schema rejects percent risk_value > 5.0 and fixed_lot risk_value > the passed maxLotSize; ints bounded 1-10/1-500/1-100
    - no zod rule references max_open_trades
    - settingsSchema.test.ts covers percent cap, fixed_lot per-account cap, and the three int bounds
    - `cd frontend && npx vitest run src/lib/settingsSchema.test.ts` passes; `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Mode-aware zod schema shipped mirroring the server caps; vitest unit green covering SUX-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Three new npm packages (react-hook-form, zod, @hookform/resolvers — all [ASSUMED] in RESEARCH because slopcheck was unavailable in this environment) and five shadcn components installed.</what-built>
  <how-to-verify>
    1. Package legitimacy (T-11-SC): confirm the three packages are the genuine, official packages before relying on them:
       - npmjs.com/package/react-hook-form (official repo github.com/react-hook-form/react-hook-form)
       - npmjs.com/package/zod (official repo github.com/colinhacks/zod — confirm v4)
       - npmjs.com/package/@hookform/resolvers (official rhf org)
       Confirm none added a postinstall script (RESEARCH audit: none).
    2. Pitfall-9 opaque-render gate: render the new `dialog` and `select` over the dark brand background and confirm they are OPAQUE with correct dark-brand colors (no transparent-popover regression). This must pass BEFORE any later plan wires a live-money mutation into a dialog.
  </how-to-verify>
  <resume-signal>Type "approved" once the three packages are confirmed legitimate and the dialog/select render opaque, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package code enters the bundle at install (supply-chain) |
| operator input → zod schema | Settings field values cross from the form into validation (UX layer only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm installs (react-hook-form, zod, @hookform/resolvers) | mitigate | Blocking human checkpoint verifies each [ASSUMED] package against npmjs.com + official repo before reliance; RESEARCH audit confirms no postinstall scripts |
| T-11-01 | Elevation of Privilege | settingsSchema.ts client caps | accept | zod is UX-only defense-in-depth; the server `validate_settings_form` re-validates on confirm (authoritative) — a bypassed client cap cannot persist out-of-cap settings |
| T-11-02 | Information disclosure | footgun.ts | accept | Pure presentation calc off two bare numerics already on the page; no server internals, no PII |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0 (TS strict gate for the new packages + zod v4 API)
- `cd frontend && npx vitest run` runs and both unit files pass
- Package legitimacy checkpoint approved; dialog/select confirmed opaque
</verification>

<success_criteria>
- vitest runner present; `npx vitest run` green on footgun.test.ts + settingsSchema.test.ts
- footgun is mode-aware (no multiply in fixed_lot — Pitfall 6 covered)
- zod schema mirrors server caps (percent ≤5.0, fixed_lot ≤ per-account max_lot_size, ints bounded) — SUX-03 covered
- Five shadcn components added and render opaque (Pitfall 9 gate)
</success_criteria>

<output>
Create `.planning/phases/11-live-money-pages-settings/11-01-SUMMARY.md` when done
</output>
