---
phase: 11-live-money-pages-settings
plan: 06
type: execute
wave: 3
depends_on: ["11-03", "11-05"]
files_modified:
  - frontend/src/routes/OverviewView.tsx
  - frontend/src/routes/router.tsx
  - frontend/src/components/shell/Sidebar.tsx
autonomous: true
requirements: [PAGE-05]
user_setup: []

must_haves:
  truths:
    - "Overview polls overview + trading-status + positions + stages (~3s) and is the operator's live-money landing surface"
    - "A red TRADING PAUSED banner shows above the positions section whenever trading-status reports paused"
    - "Overview composes the positions table, a pending-stages card (top-5 via GET /api/v2/stages), per-account cards, and an Emergency Kill Switch entry"
    - "The /app index resolves to Overview; Positions and Settings are live nav links; the kill-switch route is reachable"
    - "An open Edit modal / drilldown on Overview survives ≥2 background refetch cycles (SC#3)"
  artifacts:
    - path: "frontend/src/routes/OverviewView.tsx"
      provides: "PAGE-05 overview landing surface with live polling"
      min_lines: 60
      contains: "TRADING PAUSED"
    - path: "frontend/src/routes/router.tsx"
      provides: "overview/positions/emergency/settings routes; index → overview"
      contains: "OverviewView"
    - path: "frontend/src/components/shell/Sidebar.tsx"
      provides: "Positions + Settings live nav links"
  key_links:
    - from: "frontend/src/routes/OverviewView.tsx"
      to: "GET /api/v2/overview + /trading-status + /positions + /stages"
      via: "useQuery per source, refetchInterval 3000"
      pattern: "refetchInterval"
    - from: "frontend/src/routes/router.tsx"
      to: "OverviewView / PositionsView / KillSwitchView / SettingsView"
      via: "child routes + index redirect to overview"
      pattern: "OverviewView|PositionsView|KillSwitchView|SettingsView"
---

<objective>
Build the Overview page (PAGE-05) — the operator's default live-money landing surface — and wire the four new pages into the router and sidebar. Overview composes (largely reusing already-built pieces): a red TRADING PAUSED banner driven by trading-status, per-account cards, the open-positions table (reusing the PositionsView table from 11-03), a pending-stages card (top-5 via the shipped GET /api/v2/stages — RESEARCH Open Question 2, no new endpoint), and an Emergency Kill Switch entry. Then flip the /app index from analytics to overview and make Positions + Settings live nav links.

Purpose: the capstone wave — Overview depends on the Positions table (11-03) and the kill-switch (11-05) existing. This plan also performs the routing/nav cutover that makes all four Phase-11 pages reachable.
Output: OverviewView route + router.tsx + Sidebar.tsx edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-money-pages-settings/11-RESEARCH.md
@.planning/phases/11-live-money-pages-settings/11-PATTERNS.md
@.planning/phases/11-live-money-pages-settings/11-UI-SPEC.md

<interfaces>
<!-- Contracts (RESEARCH §Overview/status). -->
GET /api/v2/overview        → OverviewMeta {trading_paused:bool, open_positions:int, accounts:AccountOverview[]}.
  AccountOverview carries _display twins for balance/equity/margin/free_margin/total_profit + daily_trades, max_daily_trades, daily_limit_pct, risk_percent, max_lot, connected, enabled.
GET /api/v2/trading-status  → TradingStatus {paused:bool, status:"paused"|"running"}  (TRADING PAUSED banner)
GET /api/v2/positions       → Position[]  (open-positions table — reuse the PositionsView table)
GET /api/v2/stages          → pending stages (top-5; the shipped StagedView contract — RESEARCH Open Question 2; NO new endpoint)

<!-- Already-built pieces to reuse -->
PositionsView table         — frontend/src/routes/PositionsView.tsx (11-03; reuse the table or a condensed DataTable — discretion)
KillSwitchView              — frontend/src/routes/KillSwitchView.tsx (11-05; the entry button links/navigates here or opens it)
StagedView                  — frontend/src/routes/StagedView.tsx (the pending-stages contract + StageCard layout)

<!-- Inherited primitives + existing router/sidebar shape -->
DataTable / DirectionBadge / Loading / Empty / ErrorPanel
router.tsx — createBrowserRouter, basename "/app", child routes WITHOUT the /app prefix; index currently <Navigate to="/analytics">
Sidebar.tsx — NAV_ENTRIES: "Overview" already to:"/" (index); "Positions" + "Settings" are disabled-visible (no `to`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OverviewView — multi-source poller + PAUSED banner + composition (PAGE-05)</name>
  <files>frontend/src/routes/OverviewView.tsx</files>
  <read_first>
    - frontend/src/routes/StagedView.tsx (lines 97-226 — StageCard/Field card layout + poll + Loading/ErrorPanel branch order)
    - frontend/src/routes/PositionsView.tsx (11-03 — the positions table to reuse/condense)
    - frontend/src/routes/KillSwitchView.tsx (11-05 — the kill-switch entry target)
    - frontend/src/components/data/DataTable.tsx + DirectionBadge.tsx
    - .planning/phases/11-live-money-pages-settings/11-PATTERNS.md §"OverviewView.tsx" (lines 128-144) — multi-query, account cards, PAUSED banner, positions table
    - .planning/phases/11-live-money-pages-settings/11-RESEARCH.md §Open Question 2 (reuse GET /api/v2/stages top-5; no new endpoint)
    - .planning/phases/11-live-money-pages-settings/11-UI-SPEC.md §Visual Hierarchy & Focal Point (open-positions headline = focal point; PAUSED banner = secondary anchor escalating above it) + §Copywriting "Overview" (all the section/empty/banner copy) + §Color (TRADING PAUSED = destructive red; account daily-trades yellow≥80%/red≥100%)
    - PARITY refs (structure only): templates/overview.html, templates/partials/overview_cards.html
  </read_first>
  <behavior>
    - one useQuery per source: ["overview"], ["trading-status"], ["positions"], ["stages"] — each refetchInterval 3000 (inherited bg-pause is free)
    - TRADING PAUSED banner (red --destructive text/border) above the positions section whenever trading-status.paused; copy "TRADING PAUSED" / "Kill switch active — no signals will be processed"
    - per-account cards (overview_cards.html parity): name + Connected/Offline chip; Balance/Equity/Open P&L (green/red)/Open Trades/Daily Trades (yellow≥80%/red≥100%)/Risk%; margin-used bar; render _display twins. Empty → "No accounts configured."
    - Open Positions section: reuse the PositionsView table (or a condensed DataTable — discretion). Empty → "No open positions" / "Positions will appear here as signals fill."
    - Pending Stages section: top-5 from GET /api/v2/stages (reuse the StagedView contract)
    - Emergency Kill Switch entry button (destructive) navigates to / opens the kill-switch
    - any open Edit modal / drilldown reused from PositionsView renders outside the polling subtree (SC#3 — inherited mechanism)
  </behavior>
  <action>
    Create OverviewView as the live-money landing surface. Use one useQuery per source (["overview"], ["trading-status"], ["positions"], ["stages"]) each with refetchInterval 3000 (StagedView poll shape; Loading/ErrorPanel branch order; page title "Overview"). Render a red --destructive TRADING PAUSED banner above the positions section when trading-status.paused (UI-SPEC: it escalates above the open-positions focal point). Render per-account cards from OverviewMeta.accounts (overview_cards.html parity structure: Connected/Offline chip, Balance/Equity/Open P&L green-red/Open Trades/Daily Trades with yellow≥80% red≥100% thresholds/Risk%, margin-used bar) via *_display twins; empty → "No accounts configured." Render the Open Positions section by reusing the PositionsView table (or a condensed DataTable — discretion); keep the Edit modal/drilldown outside the polling subtree (SC#3 — same local-state mechanism as 11-03). Render a Pending Stages section (top-5) from GET /api/v2/stages (Open Question 2 — reuse the shipped contract, NO new endpoint). Add an Emergency Kill Switch entry button (destructive) that routes to / opens the KillSwitchView. Sections separated per the 2xl spacing token. Never re-round money (Pitfall 5 — *_display only).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/OverviewView.tsx` issues useQuery for overview + trading-status + positions + stages, each with `refetchInterval`
    - contains "TRADING PAUSED" rendered conditionally on trading-status.paused with destructive styling
    - renders per-account cards (Connected/Offline + balance/equity/P&L via _display) and a pending-stages section sourced from `/api/v2/stages`
    - an Emergency Kill Switch entry exists routing to the kill switch
    - no `toFixed`/`Intl` on money/price; no new endpoint introduced
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Overview landing surface shipped: multi-source polling, PAUSED banner, account cards, positions + pending-stages, kill-switch entry — all poll-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Router + Sidebar wiring — index→overview, live Positions/Settings, kill-switch route</name>
  <files>frontend/src/routes/router.tsx, frontend/src/components/shell/Sidebar.tsx</files>
  <read_first>
    - frontend/src/routes/router.tsx (the full file — existing analytics/signals/history/stages child routes + the index Navigate to="/analytics")
    - frontend/src/components/shell/Sidebar.tsx (NAV_ENTRIES lines 27-34 + the generic `entry.to` live-link branch lines 100-108)
    - frontend/src/routes/{OverviewView,PositionsView,KillSwitchView,SettingsView}.tsx (the four pages to route)
    - .planning/phases/11-live-money-pages-settings/11-PATTERNS.md §"router.tsx (MODIFY)" (lines 247-249), §"Sidebar.tsx (MODIFY)" (lines 251-253)
  </read_first>
  <behavior>
    - router: add four child routes under path "/" mirroring the existing entries (paths WITHOUT /app prefix): "overview", "positions", "emergency", "settings"
    - router: flip the index element from <Navigate to="/analytics"> to render OverviewView (or Navigate to "/overview") — Overview is now the live landing (OQ2)
    - Sidebar: "Overview" stays to:"/" (index resolves to Overview); add to:"/positions" and to:"/settings" to those NAV_ENTRIES so they become live NavLinks via the generic branch
  </behavior>
  <action>
    Edit router.tsx: import OverviewView/PositionsView/KillSwitchView/SettingsView; add four child routes under the "/" App route mirroring the existing analytics/signals/history/stages entries (paths "overview", "positions", "emergency", "settings" — WITHOUT the /app prefix; basename adds it). Flip the index child from <Navigate to="/analytics" replace/> to render Overview (either set the index element to <OverviewView/> or keep an index redirect to "/overview" — pick the simplest that makes /app land on Overview). Edit Sidebar.tsx NAV_ENTRIES: add `to: "/positions"` to the Positions entry and `to: "/settings"` to the Settings entry so the generic `entry.to` branch renders them as live NavLinks (they were disabled-visible spans). Leave Overview's to:"/" (the index now resolves to Overview). Keep the kill switch reachable (the Overview entry button links to /emergency; no separate nav entry required per UI-SPEC).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/router.tsx` imports and routes OverviewView, PositionsView, KillSwitchView (at "emergency"), SettingsView; the /app index resolves to Overview (no longer redirects to analytics)
    - `frontend/src/components/shell/Sidebar.tsx` NAV_ENTRIES has `to:"/positions"` and `to:"/settings"` (Positions + Settings no longer disabled-visible)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>All four Phase-11 pages reachable; /app lands on Overview; Positions + Settings are live nav links; kill switch routed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background poll → open modal/drilldown on Overview | A 3s refetch could clobber typed input if state were shared |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-20 | UX-integrity | open Edit modal/drilldown on Overview under poll | mitigate | Reuses the PositionsView local-state-outside-poll-subtree mechanism (SC#3); refetch cannot clobber typed SL/TP/lots. Manual: type SL on Overview, survive ≥2×3s polls |
| T-11-21 | Tampering | Overview-initiated mutations (close, kill-switch entry) | mitigate | All mutations flow through the same api()/useMutation hooks (CSRF-echoed, server-confirmed, no optimistic) — inherited from 11-02/11-03/11-05 |
| T-11-22 | Information disclosure | account cards / positions render | accept | Reads only the server's _display-formatted data; no client re-derivation, no internals — same trust posture as the read-only pages (Phase 10) |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- /app index resolves to Overview; /app/positions and /app/settings reachable from the sidebar; /app/emergency reachable from the Overview entry button
- MANUAL (browser): TRADING PAUSED banner shows when paused; account cards + positions + pending-stages render at parity vs legacy /; SC#3 open modal/drilldown on Overview survives ≥2 refetch cycles
- full wave merge: `pytest tests/ -x && cd frontend && npm run build && npx vitest run`
</verification>

<success_criteria>
- Overview at parity with live polling: PAUSED banner + account cards + positions table + pending-stages + kill-switch entry (PAGE-05)
- /app lands on Overview; Positions + Settings live nav links; kill-switch reachable
- Open modal/drilldown survives background refetch (SC#3)
</success_criteria>

<output>
Create `.planning/phases/11-live-money-pages-settings/11-06-SUMMARY.md` when done
</output>
