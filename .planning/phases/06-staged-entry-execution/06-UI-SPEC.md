---
phase: 6
slug: staged-entry-execution
status: draft
shadcn_initialized: false
preset: not applicable (non-React stack)
created: 2026-04-19
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for the two Phase-6 UI surfaces (per-account Settings form, pending-stages panel/page). Generated by gsd-ui-researcher; verified by gsd-ui-checker. Stack and substrate are inherited from Phase 5 — this contract only specifies what Phase 6 adds on top.

---

## Scope & Surfaces

Two net-new UI surfaces land in this phase:

1. **Per-account Settings form** — rewrites the stub `templates/settings.html` to a Basecoat-tabbed form (one tab per account), with a two-step dangerous-change modal, audit-log timeline, and per-row revert button. Delivers SET-03 + D-26..D-31.
2. **Pending-stages panel + page** — compact partial on `/overview` (top 5 active sequences) plus a full `/staged` route (all active + collapsed "Recently resolved" section). Delivers STAGE-08 + D-32..D-36.

No Phase-7 work (full Basecoat restyle, mobile slide-over, positions drilldown) is in scope. Other dashboard pages (overview shell, positions, history, analytics) keep their v1.0 compat-shim look; only the two surfaces above are touched.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (shadcn not applicable — HTMX/Jinja stack, not React) | Phase 5 decision |
| Preset | not applicable | Phase 5 decision |
| Component library | Basecoat UI `basecoat-css@0.3.3` (vendored under `static/vendor/basecoat/`) | Phase 5 D-02 |
| CSS build | Tailwind standalone CLI v4.2.2 (`@import "tailwindcss"` + `@config "../../tailwind.config.js"`), content-hashed output via `asset_url()` manifest | Phase 5 D-04-REVISED / 05-05 |
| Compat shim | `static/css/_compat.css` — preserves v1.0 `.card`, `.btn`, `.btn-red/blue/green`, `.badge-buy/sell/connected/disconnected`, `.profit/.loss`, `.nav-active`, default `table` styling | Phase 5 D-02 |
| Dark mode | `class="dark"` on `<html>` (always-dark operator UI) | Phase 5 `templates/base.html:2` |
| Icon library | none — text-only UI; no icon packages imported this phase | Project convention |
| Font | system-ui stack (Tailwind default `font-sans`) — no webfont | Project convention |
| JS | HTMX 2.0.4 + Basecoat `basecoat.min.js` + `htmx_basecoat_bridge.js` (re-init after swap) | Phase 5 UI-05 |

**Rationale for no icon library:** Trading operator UI prioritizes information density and unambiguous textual state. Icons would add visual noise without signal. Status is conveyed by color + text label (e.g. `ENABLED` / `DISABLED`, `Active` / `Disconnected` badges).

---

## Spacing Scale

Tailwind v4 default spacing scale (`0.25rem` step; 1 unit = 4px). Declared values used by this phase's surfaces:

| Token | Tailwind class | Value | Usage in Phase 6 |
|-------|---------------|-------|------------------|
| xs | `p-1`, `gap-1` | 4px | Icon-less inline gaps (inline help text next to label) |
| sm | `p-2`, `gap-2` | 8px | Tight control spacing (between form field + hint); badge internal padding |
| md | `p-4`, `gap-4`, `space-y-4` | 16px | Default field-to-field vertical rhythm in settings form; table row vertical padding |
| lg | `p-6`, `gap-6`, `mb-6` | 24px | Card internal padding; section-to-section vertical gap |
| xl | `p-8`, `gap-8` | 32px | Modal dialog padding; page-header-to-content separation |
| 2xl | `mt-12`, `gap-12` | 48px | Major section breaks on `/staged` (active table → collapsed "Recently resolved") |
| 3xl | `p-16` | 64px | Not used this phase |

**Exceptions:**
- Compact table cell padding (`px-3 py-3` = 12px, matching existing `_compat.css` `thead th` / `tbody td`). 12px falls outside the 4/8/16/24 canonical steps but is the locked v1.0 table rhythm this phase must preserve for visual continuity with `/overview` and `/positions`.
- Sidebar nav row padding (`px-4 py-2.5` = 10px vertical): locked in `templates/base.html:20-25`; Phase 6 does not touch the sidebar.

---

## Typography

Three sizes, two weights (matches Tailwind v4 defaults + existing base.html usage). Body line-height 1.5 (Tailwind default for `text-sm`/`text-base`); heading line-height 1.25 (Tailwind default for `text-xl`/`text-2xl`).

| Role | Tailwind class | Size | Weight | Line Height | Usage in Phase 6 |
|------|---------------|------|--------|-------------|------------------|
| Helper | `text-xs` | 12px | 400 (normal) | 1.33 | Inline hints ("Changes apply to next signal only"), audit-log timestamps, sidebar status line, field-validation error text |
| Body | `text-sm` | 14px | 400 (normal) | 1.43 | All form labels, table cells, audit-log rows, pending-stages values, modal body copy |
| Emphasis | `text-sm font-semibold` | 14px | 600 (semibold) | 1.43 | Account tab labels, field labels emphasized in diff view, stage count "3 / 5" |
| Heading | `text-lg` or `text-2xl` | 18px / 24px | 700 (bold, heading-only) | 1.33 / 1.25 | `text-lg` for card headings ("Account: acc-01", "Recently resolved"); `text-2xl` for page `<h2>` ("Settings", "Pending Stages") |

**Font-family:** Tailwind default `font-sans` (system-ui stack). No `font-mono` new usage this phase — existing `.font-mono` on balance/equity cells is compat-shim territory, not Phase 6.

**Weight discipline:** Only 400 and 600 in form + panel body. 700 reserved for page-level `<h2>` to match `templates/login.html:7` and `templates/overview.html:4`. No other weights (no 300/500/800) introduced.

---

## Color

Inherited palette (all declared in `tailwind.config.js` custom `dark` namespace + Tailwind defaults — no new colors this phase):

| Role | Token / class | Value | Usage in Phase 6 |
|------|---------------|-------|------------------|
| Dominant (60%) | `bg-dark-900` | `#0f0f1a` | Page background (inherited from base.html body + login card backdrop) |
| Secondary (30%) | `bg-dark-800` / `bg-dark-700` | `#1a1a2e` / `#252542` | `.card` surfaces; tab panel bg; modal surface; sidebar nav |
| Accent (10%) | `text-indigo-400` / `border-indigo-400` / `btn-primary` | `#818cf8` | **Reserved for:** primary CTA buttons (Save Settings, Confirm Change, Revert), active tab indicator, focus ring, live-price up-tick flash (pending-stages price column), `.nav-active` left border |
| Destructive | `alert-destructive`, `bg-red-900/border-red-500`, `text-red-400` | Basecoat + custom | Kill-switch-drained stage status pill, dangerous-change warning banner inside modal, form hard-cap validation errors, "Cancel sequence" CTA |
| Success | `text-green-400` / `bg-emerald-900` | Tailwind default | Filled stage count `(3 / 5)` green when on-track, audit-log "reverted successfully" toast, `Active` account status badge (via compat `.badge-connected`) |
| Neutral text | `text-slate-200` / `text-slate-400` / `text-slate-500` / `text-slate-600` | Tailwind default | `text-slate-200` body, `text-slate-400` secondary, `text-slate-500` tertiary/helper, `text-slate-600` border/muted |

**Accent reserved-for list (explicit):**
1. Primary `Save Settings` button per tab
2. Primary `Confirm change` button inside the two-step modal
3. `Revert` button on each audit-log row
4. Active tab indicator underline / background on the Basecoat tabs
5. Focus ring on form inputs (Basecoat default via `ring-2 ring-indigo-400`)
6. Live current-price cell in pending-stages table (brief 150ms tint flash on tick update — no persistent color; returns to `text-slate-200`)
7. Left-border marker on active-tab state (`nav-active` parallel — Basecoat tab handles this)

Accent is **never** used for: table headers, secondary buttons (use slate), row striping, non-primary links, hover states on non-CTA elements.

**Destructive reserved-for list (explicit):**
1. `Cancel sequence` button on pending-stage rows (future-phase; Phase 6 surfaces cancellation status read-only)
2. Form validation error messages + field-error border (`border-red-500`)
3. `cancelled_by_kill_switch`, `cancelled_stage1_closed`, `abandoned_reconnect` status pills in "Recently resolved" section
4. Warning banner inside dangerous-change modal ("This changes how future signals size orders.")
5. Form-field diff "old value" strikethrough color in the confirmation modal

---

## Component Inventory

Components instantiated by Phase 6 from the Basecoat + compat-shim surface. Executor should use these exact class names / primitives.

### Settings form (`templates/settings.html`)

| Component | Source | Basecoat class / compat class | Count |
|-----------|--------|------------------------------|-------|
| Page heading | native | `<h2 class="text-2xl font-bold">Settings</h2>` | 1 |
| Tabs (one per account) | Basecoat | `<div role="tablist">` + `<button role="tab">` (Basecoat tabs primitive — see `basecoat.min.js` init) | N accounts |
| Tab panel | Basecoat | `<div role="tabpanel">` | N accounts |
| Card (form container) | compat-shim | `.card` + `.card-body` (Basecoat card class, already exported via `_compat.css` + Basecoat) | 1 per tab |
| Form label | Basecoat | `<label class="label">` | 5 per tab (risk_mode, risk_value, max_stages, default_sl_pips, max_daily_trades) |
| Select input | Basecoat | `<select class="input">` | 1 (risk_mode) |
| Number input | Basecoat | `<input type="number" class="input">` | 4 (risk_value, max_stages, default_sl_pips, max_daily_trades) |
| Inline field hint | native | `<p class="text-xs text-slate-500">` | 5 (one per field; "Changes apply to next signal only" on the form) |
| Field error | native + destructive | `<p class="text-xs text-red-400">` (populated by server render on 422) | 0–5 per render |
| Primary CTA | Basecoat | `<button class="btn btn-primary">Save Settings</button>` | 1 per tab |
| Two-step modal | Basecoat | `<dialog class="dialog">` with `role="dialog"` + `data-state` (Basecoat dialog primitive) | 1 (reused across tabs via HTMX swap) |
| Modal diff view | native | `<table>` with `old → new` rows; old = `text-slate-500 line-through`, new = `text-indigo-400 font-semibold` | 1 |
| Modal dry-run preview | native + `.card` inset | `<div class="bg-dark-900 p-4 rounded border border-dark-700">` — "Next typical signal would size X lots at Y% risk" | 1 |
| Modal warning banner | destructive | `<div class="alert alert-destructive">` | 1 |
| Confirm CTA | Basecoat | `<button class="btn btn-primary">Confirm change</button>` | 1 |
| Cancel CTA | compat-shim | `<button class="btn">Cancel</button>` (neutral, no color) | 1 |
| Audit-log timeline | native table | `<table>` (compat-shim default), with rows = `timestamp | field | old → new | Revert button` | 1 per tab |
| Revert button | compat-shim | `<button class="btn btn-blue">Revert</button>` | N rows |

### Pending-stages partial (`templates/partials/pending_stages.html`) + full page (`templates/staged.html`)

| Component | Source | Class / primitive | Count |
|-----------|--------|------------------|-------|
| Page heading | native | `<h2 class="text-2xl font-bold">Pending Stages</h2>` (full page only) | 1 |
| Section heading | native | `<h3 class="text-lg font-semibold mb-4">Active sequences</h3>` | 1 |
| Partial heading (overview) | native | `<h3 class="text-lg font-semibold mb-4">Pending Stages <span class="text-xs text-slate-500">(showing top 5)</span></h3>` | 1 |
| Card wrapper | compat-shim | `.card p-6` | 1 per section |
| Data table | compat-shim | default `<table>` from `_compat.css` | 1 |
| Columns (7) | native | account, symbol, direction, stages (`3 / 5`), price target band, current price + distance-to-next, elapsed | per D-33 |
| Direction badge | compat-shim | `.badge-buy` / `.badge-sell` | 1 per row |
| Stage progress text | native | `<span class="font-mono text-sm">3 / 5</span>` — green if on-track, slate if cap-limited | 1 per row |
| Current price cell | native + accent-flash | `<td class="font-mono" data-price-cell>` — 150ms `ring-1 ring-indigo-400/40` flash on tick via `htmx_basecoat_bridge.js` helper | 1 per row |
| Distance-to-next-band | native | `<span class="text-xs text-slate-500">− 4.2 pips</span>` sub-line below current price | 1 per row |
| Elapsed time | native | `<span class="font-mono text-xs text-slate-400">02:14</span>` | 1 per row |
| Empty state | Basecoat | `<div class="empty-state">` (Basecoat empty-state primitive) with heading + body — "No pending stages — all signals resolved." | 1 (shown only when zero rows) |
| "View all" link | native | `<a class="text-sm text-indigo-400 hover:underline">View all ({count}) →</a>` (overview partial only, when count > 5) | 0–1 |
| Collapsed section | Basecoat | `<details>` HTML native wrapped in `.card`, summary = "Recently resolved (N)" (full page only) | 1 |
| Status pill (resolved) | destructive/neutral | `<span class="badge-sell">Kill-switch drain</span>` or neutral slate pill for `cancelled_stage1_closed` | 1 per resolved row |
| Live-refresh mechanism | HTMX + SSE | Partial: `hx-ext="sse" sse-connect="/api/stream" sse-swap="pending_stages"`. Fallback on `/staged`: `hx-get="/partials/pending_stages?all=1" hx-trigger="every 5s"`. | 1 connection |

---

## Interaction Contract

### Settings form flow (SET-03, D-26..D-31)

1. **Enter page.** Tabs rendered with first account pre-selected. Form fields populated from `SettingsStore.effective(account)`. Audit log for that account rendered below the form.
2. **Edit field.** No inline validation flash; client echoes hard caps in `min`/`max` HTML attributes (D-29 Claude's discretion) but server is authoritative.
3. **Click `Save Settings`.** `hx-post="/settings/{account}"` with HTMX-header CSRF (D-31). Server validates hard caps.
   - If validation fails → server returns `422` + partial re-render of form with `text-red-400` field errors. No modal.
   - If validation passes → server returns modal HTML (`hx-swap="innerHTML" hx-target="#modal-root"`) containing diff + dry-run preview + Confirm/Cancel buttons.
4. **Modal opens.** Basecoat dialog with `role="dialog"`, focus-trapped (Basecoat handles), `Esc` closes (= Cancel).
   - Diff table shows every changed field; unchanged fields omitted.
   - Dry-run block shows computed effect on a "typical" signal (planner specifies the computation; UI just renders the string).
   - Warning banner: "This applies to signals received AFTER you confirm. In-flight staged sequences are unaffected."
5. **Click `Confirm change`.** `hx-post="/settings/{account}/confirm"` with the same CSRF header. Server persists change, writes audit row, returns full-page swap = tab re-rendered with fresh audit row at top + modal removed.
6. **Click `Cancel`.** Modal removed; form retains edited values (user may try again).
7. **Click `Revert` on audit row.** Same modal flow as a forward edit, pre-populated with the reverse diff. Confirm posts to `/settings/{account}/revert?audit_id={id}`. Success → audit row appended (the revert itself is audited).

### Pending-stages panel flow (STAGE-08, D-32..D-36)

1. **Overview partial.** Rendered inline under existing `Open Positions` section. SSE stream on existing `/api/stream` endpoint emits `pending_stages` events at 2s cadence (D-34). Partial auto-swaps via `sse-swap="pending_stages"`.
2. **Empty state.** When no active sequences, Basecoat `empty-state` primitive renders with copy "No pending stages — all signals resolved." No illustration / icon.
3. **View all link.** When `count > 5`, link to `/staged` appears below the truncated table.
4. **Full `/staged` page.** Full active-sequences table + collapsed `<details>` "Recently resolved (N)" section. Uses HTMX `hx-trigger="every 5s"` as SSE fallback if stream drops (detected via `hx-on::sse-error` on the outer element).
5. **No row-level actions in Phase 6.** Rows are read-only observability. (A per-row "Cancel sequence" button is deferred per CONTEXT Deferred Ideas.)
6. **Live price cell.** Each tick that changes the current-price value triggers a 150ms `ring-1 ring-indigo-400/40` flash on the `<td>`. Implemented via a tiny helper in `htmx_basecoat_bridge.js` that diffs the rendered price vs previous and adds/removes the class. No per-second animation; flash only on value-change.

### Keyboard + accessibility

| Contract | Requirement |
|----------|-------------|
| Focus order | Tab navigation follows DOM: tab-list → active form fields → Save → audit-log revert buttons |
| Focus ring | Basecoat default `ring-2 ring-indigo-400 ring-offset-2 ring-offset-dark-900` on all inputs/buttons — must remain visible (do not remove outlines) |
| Modal focus trap | Basecoat dialog primitive traps focus between Confirm and Cancel; `Esc` = Cancel |
| Escape routes | Every modal closeable via `Esc`, backdrop click, or Cancel button — all three are equivalent |
| Color-only state | Never rely on color alone: direction badges include "BUY"/"SELL" text, stage progress includes numeric "3 / 5", status pills include text label ("Kill-switch drain") |
| Live region | Pending-stages table wrapped in `<div role="region" aria-live="polite" aria-label="Pending staged entry sequences">` so SR users hear count changes |
| Form error announce | Server 422 renders inject `<p role="alert" class="text-xs text-red-400">` next to offending field |
| Contrast | All accent (`indigo-400 #818cf8`) on `bg-dark-800 #1a1a2e` passes WCAG AA for text ≥14px; all destructive (`red-400`) on `bg-dark-800` passes WCAG AA |

---

## Copywriting Contract

Operator-facing strings are functional and literal — no marketing voice, no metaphors, no exclamation marks. Present tense, second-person where action is implied.

### Settings form

| Element | Exact copy |
|---------|-----------|
| Page heading | `Settings` |
| Page subhead | `Per-account runtime configuration. Changes apply to the next signal received — in-flight staged sequences are unaffected.` |
| Tab label | `{account_name}` (plain string from `accounts.json`) |
| Field label — risk_mode | `Risk mode` |
| Field hint — risk_mode | `"percent" sizes each order as % of equity; "fixed_lot" uses a fixed lot size.` |
| Field label — risk_value | `Risk value` |
| Field hint — risk_value | `Percent mode: 0 < value ≤ 5.0. Fixed-lot mode: 0 < value ≤ max_lot_size.` |
| Field label — max_stages | `Max stages per signal` |
| Field hint — max_stages | `Total positions opened per correlated signal sequence. Range: 1–10.` |
| Field label — default_sl_pips | `Default SL (pips) for text-only signals` |
| Field hint — default_sl_pips | `Required non-zero SL applied to every text-only "now" signal. Range: 1–500.` |
| Field label — max_daily_trades | `Max daily trades` |
| Field hint — max_daily_trades | `One signal counts as one trade regardless of stage count. Range: 1–100.` |
| Primary CTA | `Save settings` |
| Modal heading | `Confirm settings change — {account_name}` |
| Modal body intro | `Review the change below. It will apply to signals received AFTER you confirm.` |
| Modal dry-run heading | `Effect on a typical signal` |
| Modal warning banner | `This applies to signals received AFTER you confirm. In-flight staged sequences are unaffected.` |
| Modal confirm CTA | `Confirm change` |
| Modal cancel CTA | `Cancel` |
| Audit-log section heading | `Change history` |
| Audit-log empty | `No changes recorded for this account.` |
| Audit-log row — revert button | `Revert` |
| Revert modal heading | `Revert change — {account_name}` |
| Revert modal body | `This will restore {field} to {old_value}. The revert itself is recorded as a new audit entry.` |
| Validation — out of range | `{field} must be between {min} and {max}.` |
| Validation — fixed-lot over max | `Risk value exceeds max_lot_size for this account.` |
| Validation — generic server error | `Could not save settings. Try again — if the error repeats, check the bot logs.` |
| Success toast (post-confirm) | `Settings saved. Effective for the next signal received.` |

### Pending-stages panel

| Element | Exact copy |
|---------|-----------|
| Page heading (full page) | `Pending Stages` |
| Page subhead | `Live view of in-flight staged entry sequences. Auto-refreshes every 2 seconds.` |
| Section heading — active | `Active sequences` |
| Overview partial heading | `Pending Stages` (with `(showing top 5)` muted suffix when truncated) |
| Column — account | `Account` |
| Column — symbol | `Symbol` |
| Column — direction | `Direction` |
| Column — stages | `Stages` |
| Column — band | `Target band` |
| Column — price | `Current price` |
| Column — elapsed | `Elapsed` |
| Stage cell format | `{filled} / {total}` (e.g. `3 / 5`) |
| Band cell format | `{low} – {high}` (e.g. `2041.20 – 2043.80`) |
| Distance-to-next subline | `{sign}{value} pips to next band` (e.g. `−4.2 pips to next band`) or `inside band` when price is within the active arming band |
| Elapsed format | `{mm}:{ss}` for <1h, `{hh}:{mm}:{ss}` for ≥1h |
| Empty state heading | `No pending stages` |
| Empty state body | `All signals resolved. New staged sequences will appear here automatically.` |
| View-all link | `View all ({count}) →` |
| Resolved section summary | `Recently resolved ({count})` |
| Resolved status — kill switch | `Kill-switch drain` |
| Resolved status — stage-1 closed | `Stage 1 exited` |
| Resolved status — reconnect | `Abandoned (reconnect)` |
| SSE disconnect fallback banner | `Live updates paused — retrying. Table auto-refreshes every 5 seconds.` |

### Destructive / dangerous actions — confirmation matrix

| Action | Confirmation approach | Copy |
|--------|----------------------|------|
| Save a changed `risk_value` | Two-step modal with diff + dry-run + warning banner (D-27) | See "Modal" copy above |
| Save a changed `max_stages` | Same two-step modal | Same; diff row for `max_stages` |
| Save a changed `default_sl_pips` | Same two-step modal | Same; diff row calls out SL impact |
| Save a changed `max_daily_trades` | Same two-step modal | Same |
| Save a changed `risk_mode` | Same two-step modal **with extra warning line** | Modal appends: `Switching risk mode affects how every future signal is sized. Review the dry-run below carefully.` |
| Revert an audit-log entry | Same modal flow, inverted diff | See "Revert modal" copy above |

No other destructive actions exist on either Phase-6 surface (pending-stages panel is read-only observability).

---

## Copywriting discipline

Locked rules for this phase:

1. **Second person implied, not stated.** "Review the change" not "You should review the change".
2. **No exclamation marks anywhere.**
3. **No emoji.** (Matches global project rule + operator-UI convention.)
4. **Numbers use plain digits** (`5`, `10`, not `five`, `ten`).
5. **Symbol ranges use en-dash** (`1–10`, not `1-10` or `1 to 10`).
6. **"Signal" singular, "signals" plural** — never "sig" / "msg" / "trade signal" (use `signal` alone).
7. **"Stage"** is the canonical noun for a single position in a sequence. Never "leg", "slice", "tranche", "entry" alone.
8. **"Sequence"** is the canonical noun for the grouping of stages sharing a `signal_id`. Never "chain", "group", "ladder".
9. **Account names are verbatim** from `accounts.json` — no title-casing, no prefixing.
10. **Time values are monospace** (`font-mono`) for elapsed and price cells to stabilize tabular rhythm.

---

## Registry Safety

Not applicable — no shadcn registries involved (React-only tool; this project is HTMX/Jinja). Basecoat UI was vendored locally in Phase 5 at a pinned version; no new vendor assets enter the tree this phase.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| (none — no shadcn / no third-party) | — | not applicable |

**Vendored assets audit (carry-over from Phase 5; no changes in Phase 6):**

| Asset | Version | Path | Source |
|-------|---------|------|--------|
| Basecoat CSS | v0.3.3 | `static/vendor/basecoat/basecoat.css` | hunvreus/basecoat (vendored Phase 5 D-02) |
| Basecoat JS | v0.3.3 | `static/vendor/basecoat/basecoat.min.js` | hunvreus/basecoat (vendored Phase 5 D-02) |
| HTMX | 2.0.4 | CDN `unpkg.com/htmx.org@2.0.4` | base.html:8 (unchanged) |

Phase 6 adds zero new vendor files.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS (N/A — no registries involved)

**Approval:** pending
