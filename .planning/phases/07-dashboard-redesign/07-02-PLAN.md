---
phase: 07-dashboard-redesign
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - templates/overview.html
  - templates/partials/overview_cards.html
  - templates/positions.html
  - templates/partials/positions_table.html
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "Overview page uses Basecoat card, btn, badge components"
    - "Positions page uses Basecoat table styling"
    - "Kill switch button uses Basecoat destructive button style"
    - "Tables transform to card lists on mobile (sm breakpoint)"
    - "No existing functionality regressed"
  artifacts:
    - path: "templates/overview.html"
      provides: "Restyled overview with Basecoat components"
      contains: "card"
    - path: "templates/positions.html"
      provides: "Restyled positions page"
      contains: "card"
    - path: "templates/partials/positions_table.html"
      provides: "Table + mobile card list"
      contains: "md:hidden"
  key_links:
    - from: "templates/overview.html"
      to: "templates/partials/overview_cards.html"
      via: "{% include %}"
      pattern: "include.*overview_cards"
    - from: "templates/positions.html"
      to: "templates/partials/positions_table.html"
      via: "{% include %}"
      pattern: "include.*positions_table"
---

<objective>
Restyle overview and positions pages with Basecoat components (DASH-01). Add responsive table-to-card transformation for mobile (DASH-02 support).

Purpose: First batch of page restyles following D-17 order (overview -> positions).
Output: Overview and positions pages match Basecoat/shadcn aesthetic, mobile-friendly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-dashboard-redesign/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign/07-UI-SPEC.md
@.planning/phases/07-dashboard-redesign/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle overview.html with Basecoat components</name>
  <read_first>
    - templates/overview.html (full file)
    - templates/partials/overview_cards.html (full file)
    - templates/partials/pending_stages.html (existing pattern)
    - 07-UI-SPEC.md (Color, Typography, Component Inventory sections)
  </read_first>
  <files>templates/overview.html, templates/partials/overview_cards.html</files>
  <action>
Restyle templates/overview.html with Basecoat components:

1. Page header: Use `text-2xl font-semibold` for heading per UI-SPEC typography.

2. Kill switch area: Replace custom button classes with Basecoat `.btn .btn-destructive`:
```html
<button
    hx-get="/api/emergency-preview"
    hx-target="#kill-switch-area"
    hx-swap="innerHTML"
    class="btn btn-destructive">
    Emergency Kill Switch
</button>
```

3. Resume Trading button: Use `.btn .btn-primary` for the green resume button in trading paused banner:
```html
<button
    hx-post="/api/resume-trading"
    hx-swap="none"
    hx-on::after-request="location.reload()"
    class="btn btn-primary">
    Resume Trading
</button>
```

4. Trading paused banner: Use Basecoat alert styling:
```html
<div class="rounded-lg border border-destructive/50 bg-destructive/10 p-4 mb-6 flex items-center justify-between">
```

5. Section headings: Use `text-lg font-semibold` per UI-SPEC.

Restyle templates/partials/overview_cards.html:

1. Replace `.card` with Basecoat card pattern:
```html
<div class="rounded-lg border bg-card text-card-foreground shadow-sm p-5">
```

2. Use semantic colors from UI-SPEC:
   - Profit: `text-green-400`
   - Loss: `text-red-400`
   - Muted labels: `text-muted-foreground`
   - Values: `font-semibold`

3. Card grid: Keep `grid grid-cols-1 md:grid-cols-3 gap-4` for responsiveness.

4. Badge styles for status indicators:
   - Connected: `<span class="badge badge-outline text-green-400">Connected</span>`
   - Disconnected: `<span class="badge badge-outline text-red-400">Disconnected</span>`
  </action>
  <verify>
    <automated>grep -n "btn btn-destructive\|btn btn-primary\|rounded-lg border bg-card" templates/overview.html templates/partials/overview_cards.html | head -15</automated>
  </verify>
  <acceptance_criteria>
    - templates/overview.html contains `class="btn btn-destructive"` for kill switch
    - templates/overview.html contains `class="btn btn-primary"` for resume button
    - templates/partials/overview_cards.html contains `rounded-lg border bg-card`
    - templates/partials/overview_cards.html uses `text-green-400` for profit
    - templates/partials/overview_cards.html uses `text-red-400` for loss
    - templates/overview.html contains `text-2xl font-semibold` for page heading
  </acceptance_criteria>
  <done>Overview page uses Basecoat components, kill switch styled correctly</done>
</task>

<task type="auto">
  <name>Task 2: Restyle positions.html with responsive table + mobile cards</name>
  <read_first>
    - templates/positions.html (full file)
    - templates/partials/positions_table.html (full file)
    - 07-RESEARCH.md (Pattern: Responsive Table-to-Card)
    - 07-UI-SPEC.md (Responsive Breakpoints, Table-to-Card Rules)
  </read_first>
  <files>templates/positions.html, templates/partials/positions_table.html</files>
  <action>
Restyle templates/positions.html:

1. Page header: Use `text-2xl font-semibold` per UI-SPEC:
```html
<div class="mb-6">
    <h2 class="text-2xl font-semibold">Open Positions</h2>
    <p class="text-muted-foreground text-sm mt-1">Manage trades across all accounts - auto-refreshes every 3s</p>
</div>
```

Restyle templates/partials/positions_table.html with dual layout:

1. Desktop table (hidden below md):
```html
<div class="hidden md:block">
    <div class="rounded-lg border bg-card overflow-x-auto">
        <table class="w-full text-sm">
            <thead>
                <tr class="border-b bg-muted/50">
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Account</th>
                    <!-- ... other headers -->
                </tr>
            </thead>
            <tbody>
                {% for p in positions %}
                <tr class="border-b hover:bg-muted/30">
                    <td class="px-4 py-3 font-semibold">{{ p.account }}</td>
                    <!-- ... other cells -->
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
```

2. Mobile card list (visible below md):
```html
<div class="md:hidden space-y-3">
    {% for p in positions %}
    <div class="rounded-lg border bg-card p-4">
        <div class="flex justify-between items-start mb-3">
            <div>
                <span class="font-semibold">{{ p.symbol }}</span>
                <span class="ml-2">
                    {% if p.direction == 'buy' %}
                        <span class="badge bg-green-500/20 text-green-400">BUY</span>
                    {% else %}
                        <span class="badge bg-red-500/20 text-red-400">SELL</span>
                    {% endif %}
                </span>
            </div>
            <span class="font-mono text-lg {% if p.profit >= 0 %}text-green-400{% else %}text-red-400{% endif %}">
                {{ "%+.2f"|format(p.profit) }}
            </span>
        </div>
        <div class="grid grid-cols-2 gap-2 text-sm text-muted-foreground">
            <div>Account: <span class="text-foreground">{{ p.account }}</span></div>
            <div>Volume: <span class="font-mono text-foreground">{{ "%.2f"|format(p.volume) }}</span></div>
            <div>Entry: <span class="font-mono text-foreground">{{ "%.2f"|format(p.open_price) }}</span></div>
            <div>SL: <span class="font-mono text-foreground">{{ "%.2f"|format(p.sl) if p.sl else '-' }}</span></div>
        </div>
        <div class="mt-3 flex flex-wrap gap-2">
            <button class="btn btn-destructive btn-sm" hx-post="/api/close/{{ p.account }}/{{ p.ticket }}" hx-confirm="Close #{{ p.ticket }}?">Close</button>
            <!-- Simplified mobile actions -->
        </div>
        <div id="result-{{ p.ticket }}" class="text-xs mt-1"></div>
    </div>
    {% endfor %}
    {% if not positions %}
    <div class="rounded-lg border bg-card p-8 text-center">
        <p class="text-muted-foreground">No open positions</p>
        <p class="text-sm text-muted-foreground mt-1">Positions will appear here when the bot opens trades.</p>
    </div>
    {% endif %}
</div>
```

3. Empty state for desktop table:
```html
{% if not positions %}
<tr><td colspan="9" class="text-center text-muted-foreground py-8">
    <p class="font-medium">No open positions</p>
    <p class="text-sm mt-1">Positions will appear here when the bot opens trades.</p>
</td></tr>
{% endif %}
```

4. Replace badge classes:
   - `badge-buy` -> `badge bg-green-500/20 text-green-400`
   - `badge-sell` -> `badge bg-red-500/20 text-red-400`

5. Replace button classes:
   - `btn btn-red` -> `btn btn-destructive btn-sm`
   - `btn btn-blue` -> `btn btn-secondary btn-sm`
  </action>
  <verify>
    <automated>grep -n "hidden md:block\|md:hidden\|rounded-lg border bg-card" templates/partials/positions_table.html | head -10</automated>
  </verify>
  <acceptance_criteria>
    - templates/partials/positions_table.html contains `class="hidden md:block"` for desktop table
    - templates/partials/positions_table.html contains `class="md:hidden space-y-3"` for mobile cards
    - templates/partials/positions_table.html contains `btn btn-destructive` for close button
    - templates/positions.html contains `text-2xl font-semibold` for heading
    - templates/partials/positions_table.html contains `rounded-lg border bg-card` for cards
    - templates/partials/positions_table.html contains empty state with helpful copy
  </acceptance_criteria>
  <done>Positions page uses Basecoat styling, tables transform to cards on mobile</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> dashboard | Browser requests to FastAPI routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | hx-post actions | mitigate | CSRF via hx-request header check (existing _verify_csrf) |
| T-07-04 | Spoofing | hx-confirm bypass | accept | Server-side authorization still validates; confirm is UX convenience |
</threat_model>

<verification>
1. `grep -n "btn btn-destructive" templates/overview.html templates/partials/positions_table.html` shows Basecoat button usage
2. `grep -n "md:hidden\|hidden md:block" templates/partials/positions_table.html` shows responsive layout
3. Visual inspection: Open /overview and /positions in browser at mobile and desktop widths
</verification>

<success_criteria>
- [ ] Overview page uses Basecoat card, btn-destructive, btn-primary
- [ ] Overview cards use semantic colors (green-400/red-400)
- [ ] Positions table hidden on mobile, card list shown instead
- [ ] Positions cards show key data (symbol, direction, P/L, account)
- [ ] All buttons use Basecoat btn classes
- [ ] Empty states have helpful copy per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-redesign/07-02-SUMMARY.md`
</output>
