---
phase: 07-dashboard-redesign
plan: 08
type: execute
wave: 4
depends_on: [07-02, 07-03, 07-04, 07-05, 07-06, 07-07]
files_modified:
  - static/css/_compat.css
  - static/css/input.css
  - templates/base.html
autonomous: false
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "Phase 5 compat shim CSS is removed"
    - "All pages render correctly without compat shim"
    - "No visual regressions on any page"
    - "CSS bundle size reduced"
  artifacts:
    - path: "static/css/input.css"
      provides: "Clean input.css without compat shim imports"
    - path: "static/css/_compat.css"
      provides: "File removed or emptied"
  key_links:
    - from: "templates/*.html"
      to: "static/css/app.css"
      via: "asset_url('app.css')"
      pattern: "asset_url"
---

<objective>
Remove Phase 5 compat shim after all pages restyled (D-16). Final cleanup and visual verification.

Purpose: Clean CSS with no legacy compatibility layer.
Output: Reduced CSS bundle, no compat shim dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-dashboard-redesign/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign/07-02-SUMMARY.md
@.planning/phases/07-dashboard-redesign/07-03-SUMMARY.md
@.planning/phases/07-dashboard-redesign/07-04-SUMMARY.md
@.planning/phases/07-dashboard-redesign/07-05-SUMMARY.md
@.planning/phases/07-dashboard-redesign/07-06-SUMMARY.md
@.planning/phases/07-dashboard-redesign/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit compat shim usage and prepare removal</name>
  <read_first>
    - static/css/_compat.css (full file - see what classes it defines)
    - static/css/input.css (check @import for _compat.css)
    - All templates/*.html files to verify no compat classes remain
  </read_first>
  <files>static/css/_compat.css, static/css/input.css</files>
  <action>
1. List all classes defined in `static/css/_compat.css` (e.g., `.card`, `.badge-buy`, `.badge-sell`, `.profit`, `.loss`, `.nav-active`, `.btn`, `.btn-red`, `.btn-blue`)

2. Search all templates for any remaining usage of these classes:
```bash
grep -r "badge-buy\|badge-sell\|btn-red\|btn-blue\|nav-active" templates/
```

3. If any compat classes are still in use, update those templates first:
   - `badge-buy` -> `badge bg-green-500/20 text-green-400`
   - `badge-sell` -> `badge bg-red-500/20 text-red-400`
   - `btn-red` -> `btn btn-destructive`
   - `btn-blue` -> `btn btn-secondary`
   - `nav-active` -> handled by `.sidebar-link.active` from Plan 01
   - `profit` -> `text-green-400`
   - `loss` -> `text-red-400`

4. Once all templates are updated, remove the `@import './_compat.css';` line from `static/css/input.css`

5. Delete or empty `static/css/_compat.css`

Note: The `.card` class may still be used but should be replaced with `rounded-lg border bg-card` or kept if it's now defined in main CSS.
  </action>
  <verify>
    <automated>grep -rn "badge-buy\|badge-sell\|btn-red\|btn-blue" templates/ ; echo "Exit code: $?"</automated>
  </verify>
  <acceptance_criteria>
    - No templates contain `badge-buy`, `badge-sell`, `btn-red`, `btn-blue`
    - static/css/input.css does NOT contain `@import './_compat.css'`
    - static/css/_compat.css is either deleted or contains only a comment explaining removal
  </acceptance_criteria>
  <done>Compat shim classes removed from all templates</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild CSS and verify bundle</name>
  <read_first>
    - scripts/build_css.sh (build command)
    - Dockerfile (CSS build step)
  </read_first>
  <files></files>
  <action>
1. Run the CSS build locally to verify no errors:
```bash
./scripts/build_css.sh
```

2. Check the output CSS file size:
```bash
ls -la static/css/app.*.css
```

3. Verify the manifest was updated:
```bash
cat static/css/manifest.json
```

4. If build succeeds and file size is reasonable (should be smaller without compat shim), the removal is complete.

Note: This task is verification only - no file modifications. If build fails, it indicates templates still reference compat classes.
  </action>
  <verify>
    <automated>./scripts/build_css.sh && echo "CSS build successful" && ls -la static/css/app.*.css</automated>
  </verify>
  <acceptance_criteria>
    - CSS build completes without errors
    - app.*.css file exists with content-hashed name
    - manifest.json updated with new filename
    - No warnings about missing classes in build output
  </acceptance_criteria>
  <done>CSS builds successfully without compat shim</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All dashboard pages restyled with Basecoat components:
- Mobile slide-over nav (Plan 01)
- Overview + positions (Plan 02)
- Positions drilldown (Plan 03)
- Trade history filters (Plan 04)
- Analytics time/source filters (Plan 05)
- Settings toasts + help text (Plan 06)
- Signals + staged pages (Plan 07)
- Compat shim removed (this plan)
  </what-built>
  <how-to-verify>
1. Start the dashboard locally: `python dashboard.py` or `docker-compose up`

2. Desktop verification (>768px viewport):
   - [ ] /overview: Cards display correctly, kill switch button styled
   - [ ] /positions: Table visible, drilldown expands on row click
   - [ ] /history: Filter bar visible, filters work
   - [ ] /analytics: Time tabs switch, source rows clickable
   - [ ] /settings: Form fields have help text, save shows toast
   - [ ] /signals: Table displays correctly
   - [ ] /staged: Cards show pending stages
   - [ ] Sidebar: 224px fixed, nav links work

3. Mobile verification (<768px viewport, use browser devtools):
   - [ ] Hamburger icon visible in sticky header
   - [ ] Click hamburger: drawer slides in from left
   - [ ] Tap nav link: navigates and drawer closes
   - [ ] /positions: Card list instead of table
   - [ ] /history: Cards instead of table, filters stack
   - [ ] All pages: Content readable, no horizontal overflow

4. Functionality verification:
   - [ ] Save a setting: Toast appears bottom-right
   - [ ] Click position row: Drilldown expands with fill history
   - [ ] Apply history filter: Table updates, URL changes
   - [ ] Click analytics source row: Metrics filter to that source
   - [ ] Verify no console errors (F12 -> Console tab)

5. CSS verification:
   - [ ] View source: CSS link is content-hashed (e.g., app.abc123.css)
   - [ ] No 404s in Network tab for CSS files

Report any issues or confirm all checks pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | CSS cleanup only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Denial of Service | CSS build | accept | Build failure is caught in CI/deploy; doesn't affect running system |
</threat_model>

<verification>
1. `grep -r "badge-buy" templates/` returns no matches
2. CSS build succeeds
3. All pages render correctly at desktop and mobile widths
</verification>

<success_criteria>
- [ ] No compat shim classes in templates
- [ ] _compat.css import removed from input.css
- [ ] CSS builds without errors
- [ ] All pages render correctly (human verified)
- [ ] Mobile nav works (human verified)
- [ ] Drilldown, filters, toasts work (human verified)
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-redesign/07-08-SUMMARY.md`
</output>
