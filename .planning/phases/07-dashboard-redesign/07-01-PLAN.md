---
phase: 07-dashboard-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db.py
  - bot.py
  - trade_manager.py
  - templates/base.html
  - static/css/input.css
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "Signals table has source_name column for per-source analytics"
    - "New signals are logged with Telegram channel name"
    - "Mobile users see hamburger icon below md breakpoint"
    - "Clicking hamburger opens slide-over drawer with nav links"
    - "Desktop sidebar unchanged above md breakpoint"
  artifacts:
    - path: "db.py"
      provides: "source_name column on signals, updated log_signal()"
      contains: "source_name TEXT"
    - path: "bot.py"
      provides: "group_name passed to trade_manager"
      contains: "group_name="
    - path: "templates/base.html"
      provides: "Mobile drawer + sticky header + desktop sidebar"
      contains: "sidebar"
  key_links:
    - from: "bot.py"
      to: "trade_manager.py"
      via: "group_name parameter"
      pattern: "group_name="
    - from: "templates/base.html"
      to: "basecoat.min.js"
      via: "CustomEvent('basecoat:sidebar')"
      pattern: "basecoat:sidebar"
---

<objective>
Add source_name tracking to signals table for per-source analytics (DASH-04 prerequisite) and implement mobile navigation with slide-over drawer (DASH-02 per D-01, D-02, D-03).

Purpose: Foundation for per-source analytics drill-down and mobile-responsive layout.
Output: Signals track source, mobile nav works, desktop sidebar unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-redesign/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign/07-RESEARCH.md
@.planning/phases/07-dashboard-redesign/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source_name column to signals + update log_signal()</name>
  <read_first>
    - db.py (lines 80-95 for signals CREATE TABLE, lines 251-280 for log_signal())
    - trade_manager.py (lines 269-280, 473-485 for log_signal() call sites)
    - bot.py (lines 287-310 for group_name resolution and handler)
  </read_first>
  <files>db.py, trade_manager.py, bot.py</files>
  <action>
1. In db.py, add `ALTER TABLE signals ADD COLUMN IF NOT EXISTS source_name TEXT` after the CREATE TABLE block (around line 96). This is additive-only: nullable column, no data migration needed.

2. Update `log_signal()` signature at line 251:
   - Add parameter: `source_name: str = ""`
   - Add to INSERT columns list: `source_name`
   - Add to VALUES: `$12` (shift existing $11 details to $12, add source_name as $11)

3. In trade_manager.py, update all `log_signal()` call sites to accept `source_name`:
   - Line 269: Add `source_name=source_name` parameter
   - Line 473: Add `source_name=source_name` parameter
   - Lines 820, 853, 889, 934: Add `source_name=source_name` parameter
   - Update method signatures for `_handle_open`, `_handle_open_text_only`, `_handle_correlated_followup`, `_handle_close`, `_handle_close_partial`, `_handle_modify_sl`, `_handle_modify_tp` to accept `source_name: str = ""` parameter
   - Update `execute_signal()` to accept `source_name` and pass it down

4. In bot.py, pass `group_name` to `executor.execute_signal()`:
   - Line 387: Change `await executor.execute_signal(signal)` to `await executor.execute_signal(signal, source_name=group_name)`
   - Update `executor.py::execute_signal()` to accept `source_name` and pass to trade_manager

Note: Historical signals will have NULL/empty source_name. Analytics query will coalesce to "Unknown" for display.
  </action>
  <verify>
    <automated>grep -n "source_name" db.py trade_manager.py bot.py | head -30</automated>
  </verify>
  <acceptance_criteria>
    - db.py contains `ALTER TABLE signals ADD COLUMN IF NOT EXISTS source_name TEXT`
    - db.py log_signal() has `source_name: str = ""` parameter
    - trade_manager.py has `source_name` in execute_signal signature
    - bot.py line ~387 contains `source_name=group_name`
  </acceptance_criteria>
  <done>Signals table tracks source_name, new signals logged with Telegram channel name</done>
</task>

<task type="auto">
  <name>Task 2: Implement mobile slide-over drawer (D-01, D-02, D-03)</name>
  <read_first>
    - templates/base.html (full file - current sidebar structure)
    - static/vendor/basecoat/basecoat.css (lines 1-100 for sidebar classes)
    - static/js/htmx_basecoat_bridge.js (re-init pattern)
    - 07-RESEARCH.md (Pattern 1: Mobile Slide-Over Drawer)
    - 07-UI-SPEC.md (Mobile Navigation section)
  </read_first>
  <files>templates/base.html, static/css/input.css</files>
  <action>
Replace templates/base.html with mobile-responsive layout per D-01, D-02, D-03:

```html
<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Telebot Dashboard</title>
    <link rel="stylesheet" href="{{ asset_url('app.css') }}">
    <script src="https://unpkg.com/htmx.org@2.0.4"></script>
    <script src="https://unpkg.com/htmx-ext-sse@2.2.2/sse.js"></script>
    <script defer src="/static/vendor/basecoat/basecoat.min.js"></script>
    <script defer src="/static/js/htmx_basecoat_bridge.js"></script>
</head>
<body class="min-h-screen bg-background text-foreground">
    <!-- Mobile Header (D-02: sticky, visible below md) -->
    <header class="md:hidden fixed top-0 left-0 right-0 bg-sidebar border-b border-sidebar-border h-12 flex items-center px-4 z-30">
        <button class="btn-icon-ghost -ml-2" onclick="document.dispatchEvent(new CustomEvent('basecoat:sidebar'))">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" x2="20" y1="12" y2="12"/><line x1="4" x2="20" y1="6" y2="6"/><line x1="4" x2="20" y1="18" y2="18"/></svg>
        </button>
        <span class="ml-3 font-semibold text-sm">{{ page_title|default('Dashboard') }}</span>
    </header>

    <!-- Sidebar (D-01: drawer on mobile, D-03: fixed 224px on desktop) -->
    <aside class="sidebar" id="main-sidebar" aria-hidden="true">
        <nav aria-label="Main navigation">
            <section class="scrollbar">
                <div class="p-4 border-b border-sidebar-border">
                    <h1 class="text-lg font-bold text-primary">Telebot</h1>
                    <p class="text-xs text-muted-foreground mt-1">Trading Dashboard</p>
                </div>
                <div role="group" class="py-2">
                    <ul>
                        <li><a href="/overview" class="sidebar-link {% if page == 'overview' %}active{% endif %}">Overview</a></li>
                        <li><a href="/positions" class="sidebar-link {% if page == 'positions' %}active{% endif %}">Positions</a></li>
                        <li><a href="/history" class="sidebar-link {% if page == 'history' %}active{% endif %}">Trade History</a></li>
                        <li><a href="/signals" class="sidebar-link {% if page == 'signals' %}active{% endif %}">Signal Log</a></li>
                        <li><a href="/analytics" class="sidebar-link {% if page == 'analytics' %}active{% endif %}">Analytics</a></li>
                        <li><a href="/staged" class="sidebar-link {% if page == 'staged' %}active{% endif %}">Pending Stages</a></li>
                        <li><a href="/settings" class="sidebar-link {% if page == 'settings' %}active{% endif %}">Settings</a></li>
                    </ul>
                </div>
            </section>
            <footer class="border-t border-sidebar-border p-4">
                <a href="/logout" class="sidebar-link text-muted-foreground">Sign out</a>
                <div class="mt-4 text-xs text-muted-foreground flex items-center">
                    <span id="status-dot" class="w-2 h-2 rounded-full {% if trading_enabled %}bg-green-500{% else %}bg-red-500{% endif %} mr-2"></span>
                    {% if trading_enabled %}
                        {% if dry_run %}DRY-RUN{% else %}LIVE{% endif %}
                    {% else %}
                        DISABLED
                    {% endif %}
                </div>
            </footer>
        </nav>
    </aside>

    <!-- Main content -->
    <main class="md:ml-56 pt-12 md:pt-0 p-4 md:p-6 min-h-screen">
        {% block content %}{% endblock %}
    </main>

    <!-- Toaster container (for Phase 7 SEED-001) -->
    <div id="toaster" class="toaster" hx-preserve="true"></div>
</body>
</html>
```

Add custom sidebar-link styles to static/css/input.css (append after existing content):

```css
/* Phase 7: Sidebar link styles */
.sidebar-link {
  @apply block px-4 py-2.5 text-sm text-foreground hover:bg-accent rounded-md mx-2 transition-colors;
}
.sidebar-link.active {
  @apply bg-accent text-accent-foreground font-medium;
}
```
  </action>
  <verify>
    <automated>grep -n "sidebar\|basecoat:sidebar\|md:hidden" templates/base.html | head -20</automated>
  </verify>
  <acceptance_criteria>
    - templates/base.html contains `class="md:hidden fixed top-0"` (mobile header)
    - templates/base.html contains `class="sidebar"` and `id="main-sidebar"`
    - templates/base.html contains `basecoat:sidebar` CustomEvent dispatch
    - templates/base.html contains `md:ml-56 pt-12 md:pt-0` on main element
    - templates/base.html contains toaster div with `hx-preserve="true"`
    - static/css/input.css contains `.sidebar-link` styles
  </acceptance_criteria>
  <done>Mobile drawer slides in on hamburger click, desktop sidebar unchanged, toaster ready for SEED-001</done>
</task>

<task type="auto">
  <name>Task 3: Update dashboard routes to pass page_title</name>
  <read_first>
    - dashboard.py (route handlers around lines 200-500)
  </read_first>
  <files>dashboard.py</files>
  <action>
Update each page route to include `page_title` in the template context:

1. `/overview` route: Add `"page_title": "Overview"` to TemplateResponse context
2. `/positions` route: Add `"page_title": "Open Positions"` to context
3. `/history` route: Add `"page_title": "Trade History"` to context
4. `/signals` route: Add `"page_title": "Signal Log"` to context
5. `/analytics` route: Add `"page_title": "Analytics"` to context
6. `/staged` route: Add `"page_title": "Pending Stages"` to context
7. `/settings` route: Add `"page_title": "Settings"` to context

Example pattern for each route:
```python
return templates.TemplateResponse("overview.html", {
    "request": request,
    "page": "overview",
    "page_title": "Overview",  # Add this line
    # ... existing context
})
```
  </action>
  <verify>
    <automated>grep -n "page_title" dashboard.py | head -15</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.py contains `"page_title": "Overview"` in overview route
    - dashboard.py contains `"page_title": "Open Positions"` in positions route
    - dashboard.py contains `"page_title": "Trade History"` in history route
    - dashboard.py contains `"page_title": "Signal Log"` in signals route
    - dashboard.py contains `"page_title": "Analytics"` in analytics route
    - dashboard.py contains `"page_title": "Pending Stages"` in staged route
    - dashboard.py contains `"page_title": "Settings"` in settings route
  </acceptance_criteria>
  <done>Mobile header shows correct page title on each route</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> dashboard | Untrusted browser requests to FastAPI routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | source_name parameter | mitigate | source_name derived from server-side group_name resolution, not client input |
| T-07-02 | Information Disclosure | sidebar nav | accept | nav links are authenticated-only routes; no sensitive data exposed |
</threat_model>

<verification>
1. `grep -n "source_name" db.py` shows column and parameter
2. `grep -n "basecoat:sidebar" templates/base.html` shows mobile toggle
3. `grep -n "md:hidden" templates/base.html` shows mobile-only header
4. Run `python -c "import db; print('schema OK')"` to verify module loads
</verification>

<success_criteria>
- [ ] Signals table has source_name column
- [ ] New signals logged with Telegram channel name
- [ ] Mobile header visible below md breakpoint with hamburger
- [ ] Clicking hamburger opens Basecoat sidebar drawer
- [ ] Desktop sidebar unchanged (fixed 224px)
- [ ] All page routes include page_title in context
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-redesign/07-01-SUMMARY.md`
</output>
