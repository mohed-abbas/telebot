---
phase: 07-dashboard-redesign
plan: 07
type: execute
wave: 3
depends_on: [07-01]
files_modified:
  - templates/signals.html
  - templates/staged.html
  - templates/partials/pending_stages.html
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "Signals page uses Basecoat table styling"
    - "Signals page has responsive table-to-card layout"
    - "Staged page uses Basecoat components"
    - "Staged page card list works on mobile"
    - "Empty states have helpful copy per UI-SPEC"
  artifacts:
    - path: "templates/signals.html"
      provides: "Restyled signal log page"
      contains: "hidden md:block"
    - path: "templates/staged.html"
      provides: "Restyled pending stages page"
      contains: "rounded-lg border bg-card"
    - path: "templates/partials/pending_stages.html"
      provides: "Restyled pending stages partial"
      contains: "rounded-lg"
  key_links:
    - from: "templates/staged.html"
      to: "templates/partials/pending_stages.html"
      via: "{% include %}"
      pattern: "include.*pending_stages"
---

<objective>
Restyle signals and staged pages with Basecoat components (DASH-01). Complete the D-17 restyle order: signals -> staged.

Purpose: Final pages in the restyle sequence before compat shim removal.
Output: Signals and staged pages match Basecoat aesthetic with mobile support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-dashboard-redesign/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle signals.html with responsive table</name>
  <read_first>
    - templates/signals.html (current structure)
    - 07-UI-SPEC.md (Typography, Color, Responsive Breakpoints)
  </read_first>
  <files>templates/signals.html</files>
  <action>
Restyle templates/signals.html:

```html
{% extends "base.html" %}
{% block content %}
<div class="mb-6">
    <h2 class="text-2xl font-semibold">Signal Log</h2>
    <p class="text-muted-foreground text-sm mt-1">Last 100 signals received from Telegram</p>
</div>

{# Desktop table #}
<div class="hidden md:block">
    <div class="rounded-lg border bg-card overflow-x-auto">
        <table class="w-full text-sm">
            <thead>
                <tr class="border-b bg-muted/50">
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Time</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Type</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Symbol</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Direction</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Zone</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">SL</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">TP</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Action</th>
                    <th class="px-4 py-3 text-left font-medium text-muted-foreground">Details</th>
                </tr>
            </thead>
            <tbody>
                {% for s in signals %}
                <tr class="border-b hover:bg-muted/30">
                    <td class="px-4 py-3 text-muted-foreground text-xs">
                        {{ s.timestamp.strftime('%Y-%m-%d %H:%M:%S') if s.timestamp else '-' }}
                    </td>
                    <td class="px-4 py-3">
                        {% if s.signal_type == 'open' %}
                            <span class="text-primary">OPEN</span>
                        {% elif s.signal_type == 'open_text_only' %}
                            <span class="text-primary">OPEN (NOW)</span>
                        {% elif s.signal_type == 'close' %}
                            <span class="text-red-400">CLOSE</span>
                        {% elif s.signal_type == 'close_partial' %}
                            <span class="text-amber-400">PARTIAL</span>
                        {% elif s.signal_type == 'modify_sl' %}
                            <span class="text-amber-400">MOD SL</span>
                        {% elif s.signal_type == 'modify_tp' %}
                            <span class="text-amber-400">MOD TP</span>
                        {% else %}
                            <span class="text-muted-foreground">{{ s.signal_type }}</span>
                        {% endif %}
                    </td>
                    <td class="px-4 py-3 font-mono">{{ s.symbol or '-' }}</td>
                    <td class="px-4 py-3">
                        {% if s.direction == 'buy' %}
                            <span class="badge bg-green-500/20 text-green-400">BUY</span>
                        {% elif s.direction == 'sell' %}
                            <span class="badge bg-red-500/20 text-red-400">SELL</span>
                        {% else %}
                            -
                        {% endif %}
                    </td>
                    <td class="px-4 py-3 font-mono text-xs">
                        {% if s.entry_zone_low and s.entry_zone_high %}
                            {{ "%.2f"|format(s.entry_zone_low) }}-{{ "%.2f"|format(s.entry_zone_high) }}
                        {% else %}
                            -
                        {% endif %}
                    </td>
                    <td class="px-4 py-3 font-mono">{{ "%.2f"|format(s.sl) if s.sl else '-' }}</td>
                    <td class="px-4 py-3 font-mono">{{ "%.2f"|format(s.tp) if s.tp else '-' }}</td>
                    <td class="px-4 py-3">
                        {% if s.action_taken == 'executed' or s.action_taken == 'staged' %}
                            <span class="text-green-400">{{ s.action_taken }}</span>
                        {% elif s.action_taken == 'skipped' %}
                            <span class="text-amber-400">{{ s.action_taken }}</span>
                        {% elif s.action_taken == 'failed' %}
                            <span class="text-red-400">{{ s.action_taken }}</span>
                        {% else %}
                            <span class="text-muted-foreground">{{ s.action_taken }}</span>
                        {% endif %}
                    </td>
                    <td class="px-4 py-3 text-xs text-muted-foreground max-w-xs truncate">
                        {{ s.details or (s.raw_text[:80] if s.raw_text else '') }}
                    </td>
                </tr>
                {% endfor %}
                {% if not signals %}
                <tr>
                    <td colspan="9" class="text-center py-8">
                        <p class="font-medium text-muted-foreground">No signals yet</p>
                        <p class="text-sm text-muted-foreground mt-1">Signals will appear here when received from Telegram.</p>
                    </td>
                </tr>
                {% endif %}
            </tbody>
        </table>
    </div>
</div>

{# Mobile cards #}
<div class="md:hidden space-y-3">
    {% for s in signals %}
    <div class="rounded-lg border bg-card p-4">
        <div class="flex justify-between items-start mb-2">
            <div>
                <span class="font-mono font-medium">{{ s.symbol or 'N/A' }}</span>
                {% if s.direction == 'buy' %}
                    <span class="badge bg-green-500/20 text-green-400 ml-2">BUY</span>
                {% elif s.direction == 'sell' %}
                    <span class="badge bg-red-500/20 text-red-400 ml-2">SELL</span>
                {% endif %}
            </div>
            <span class="text-xs {% if s.action_taken == 'executed' or s.action_taken == 'staged' %}text-green-400{% elif s.action_taken == 'failed' %}text-red-400{% else %}text-muted-foreground{% endif %}">
                {{ s.action_taken }}
            </span>
        </div>
        <div class="text-xs text-muted-foreground space-y-1">
            <div>{{ s.timestamp.strftime('%m-%d %H:%M:%S') if s.timestamp else '-' }}</div>
            <div>
                Type: 
                {% if s.signal_type == 'open_text_only' %}OPEN (NOW){% else %}{{ s.signal_type|upper }}{% endif %}
            </div>
            {% if s.entry_zone_low and s.entry_zone_high %}
            <div>Zone: {{ "%.2f"|format(s.entry_zone_low) }}-{{ "%.2f"|format(s.entry_zone_high) }}</div>
            {% endif %}
        </div>
    </div>
    {% endfor %}
    {% if not signals %}
    <div class="rounded-lg border bg-card p-8 text-center">
        <p class="font-medium text-muted-foreground">No signals yet</p>
        <p class="text-sm text-muted-foreground mt-1">Signals will appear here when received from Telegram.</p>
    </div>
    {% endif %}
</div>
{% endblock %}
```
  </action>
  <verify>
    <automated>grep -n "hidden md:block\|md:hidden\|rounded-lg border bg-card" templates/signals.html | head -10</automated>
  </verify>
  <acceptance_criteria>
    - templates/signals.html contains `class="hidden md:block"` for desktop table
    - templates/signals.html contains `class="md:hidden space-y-3"` for mobile cards
    - templates/signals.html contains `rounded-lg border bg-card` for card styling
    - Table uses Basecoat patterns (bg-muted/50, hover:bg-muted/30)
    - Empty state has helpful copy
    - Signal types include "open_text_only" handling
  </acceptance_criteria>
  <done>Signals page restyled with responsive layout</done>
</task>

<task type="auto">
  <name>Task 2: Restyle staged.html and pending_stages.html partial</name>
  <read_first>
    - templates/staged.html (current structure)
    - templates/partials/pending_stages.html (current partial)
    - 07-UI-SPEC.md (Empty state copy for pending stages)
  </read_first>
  <files>templates/staged.html, templates/partials/pending_stages.html</files>
  <action>
1. Restyle templates/staged.html:

```html
{% extends "base.html" %}
{% block content %}
<div class="mb-6">
    <h2 class="text-2xl font-semibold">Pending Stages</h2>
    <p class="text-muted-foreground text-sm mt-1">Live view of in-flight staged entry sequences. Auto-refreshes every 2 seconds.</p>
</div>

<h3 class="text-lg font-semibold mb-4">Active sequences</h3>
<div id="pending-stages-root"
     hx-ext="sse"
     sse-connect="/stream"
     sse-swap="pending_stages"
     hx-get="/partials/pending_stages?all=1"
     hx-trigger="every 5s"
     hx-on::sse-error="this.classList.add('sse-fallback')">
    {% with stages=active %}
        {% include "partials/pending_stages.html" %}
    {% endwith %}
</div>

{% if resolved %}
<div class="mt-12">
    <details class="rounded-lg border bg-card">
        <summary class="p-4 cursor-pointer flex items-center justify-between">
            <h3 class="text-lg font-semibold">Recently resolved</h3>
            <span class="badge bg-muted text-muted-foreground">{{ resolved|length }}</span>
        </summary>
        <div class="border-t overflow-x-auto">
            <table class="w-full text-sm">
                <thead>
                    <tr class="border-b bg-muted/50">
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Account</th>
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Symbol</th>
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Direction</th>
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Stage</th>
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Status</th>
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Reason</th>
                        <th class="px-4 py-3 text-left font-medium text-muted-foreground">Time</th>
                    </tr>
                </thead>
                <tbody>
                    {% for r in resolved %}
                    <tr class="border-b hover:bg-muted/30">
                        <td class="px-4 py-3 font-semibold">{{ r.account_name }}</td>
                        <td class="px-4 py-3 font-mono">{{ r.symbol }}</td>
                        <td class="px-4 py-3">
                            {% if r.direction == "buy" %}
                                <span class="badge bg-green-500/20 text-green-400">BUY</span>
                            {% else %}
                                <span class="badge bg-red-500/20 text-red-400">SELL</span>
                            {% endif %}
                        </td>
                        <td class="px-4 py-3 font-mono">{{ r.stage_number }}</td>
                        <td class="px-4 py-3">
                            {% if r.status in ("cancelled_by_kill_switch","abandoned_reconnect","failed") %}
                                <span class="text-red-400">{{ r.status_label }}</span>
                            {% elif r.status == "filled" %}
                                <span class="text-green-400">{{ r.status_label }}</span>
                            {% else %}
                                <span class="text-muted-foreground">{{ r.status_label }}</span>
                            {% endif %}
                        </td>
                        <td class="px-4 py-3 text-xs text-muted-foreground">{{ r.cancelled_reason or "—" }}</td>
                        <td class="px-4 py-3 font-mono text-xs text-muted-foreground">
                            {{ (r.filled_at or r.created_at).strftime("%H:%M:%S") }}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </details>
</div>
{% endif %}
{% endblock %}
```

2. Restyle templates/partials/pending_stages.html:

```html
{# Pending stages partial (STAGE-08, D-32..D-36) #}
{% if stages %}
<div class="space-y-3">
    {% for s in stages %}
    <div class="rounded-lg border bg-card p-4">
        <div class="flex justify-between items-start mb-3">
            <div>
                <span class="font-mono font-semibold">{{ s.symbol }}</span>
                {% if s.direction == "buy" %}
                    <span class="badge bg-green-500/20 text-green-400 ml-2">BUY</span>
                {% else %}
                    <span class="badge bg-red-500/20 text-red-400 ml-2">SELL</span>
                {% endif %}
            </div>
            <span class="text-xs text-muted-foreground">{{ s.account_name }}</span>
        </div>
        
        <div class="grid grid-cols-2 md:grid-cols-4 gap-3 text-sm">
            <div>
                <span class="text-xs text-muted-foreground block">Stages</span>
                <span class="font-mono">{{ s.filled_count }}/{{ s.total_stages }}</span>
            </div>
            <div>
                <span class="text-xs text-muted-foreground block">Target Band</span>
                <span class="font-mono text-xs">{{ "%.2f"|format(s.band_low) }} - {{ "%.2f"|format(s.band_high) }}</span>
            </div>
            <div>
                <span class="text-xs text-muted-foreground block">Current Price</span>
                <span class="font-mono" data-price-cell>{{ "%.2f"|format(s.current_price) if s.current_price else '-' }}</span>
            </div>
            <div>
                <span class="text-xs text-muted-foreground block">Elapsed</span>
                <span class="text-muted-foreground">{{ s.elapsed }}</span>
            </div>
        </div>
        
        {% if s.distance_to_band %}
        <div class="mt-3 text-xs">
            <span class="text-muted-foreground">Distance to band:</span>
            <span class="font-mono {% if s.distance_to_band < 0 %}text-green-400{% else %}text-amber-400{% endif %}">
                {{ "%+.1f"|format(s.distance_to_band) }} pips
            </span>
        </div>
        {% endif %}
    </div>
    {% endfor %}
</div>
{% else %}
<div class="rounded-lg border bg-card p-8 text-center">
    <p class="font-medium text-muted-foreground">No pending stages</p>
    <p class="text-sm text-muted-foreground mt-1">All staged entries have been filled or cancelled. Check the Trade History for recent activity.</p>
</div>
{% endif %}
```
  </action>
  <verify>
    <automated>grep -n "rounded-lg border bg-card\|No pending stages\|Recently resolved" templates/staged.html templates/partials/pending_stages.html | head -15</automated>
  </verify>
  <acceptance_criteria>
    - templates/staged.html contains `rounded-lg border bg-card` for cards
    - templates/staged.html contains `<details>` for collapsed resolved section
    - templates/partials/pending_stages.html uses card layout for each stage
    - Empty state shows "No pending stages" with helpful copy per UI-SPEC
    - Resolved table uses Basecoat patterns (bg-muted/50, hover:bg-muted/30)
    - Status badges use appropriate colors (green for filled, red for cancelled)
  </acceptance_criteria>
  <done>Staged pages restyled with Basecoat components</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> dashboard | Page requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | signal raw_text | mitigate | raw_text truncated in display; authenticated route only |
</threat_model>

<verification>
1. `grep -n "hidden md:block" templates/signals.html` shows responsive layout
2. `grep -n "rounded-lg border bg-card" templates/staged.html templates/partials/pending_stages.html` shows card styling
3. Visual test: Check signals and staged pages at mobile and desktop widths
</verification>

<success_criteria>
- [ ] Signals page has responsive table/card layout
- [ ] Signals empty state shows helpful copy
- [ ] Staged page uses Basecoat cards
- [ ] Staged empty state shows "No pending stages" copy
- [ ] Resolved section uses collapsible details
- [ ] All status badges use semantic colors
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-redesign/07-07-SUMMARY.md`
</output>
