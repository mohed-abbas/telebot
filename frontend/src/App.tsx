// Trivial placeholder. The boot guard (GET /api/v2/auth/me) and the app shell + router
// land in Plans 03/04; providers (QueryClientProvider / RouterProvider) are added in Plan 04.
function App() {
  return <div>Telebot</div>;
}

export default App;
